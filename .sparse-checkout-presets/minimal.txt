# TeachAny · Sparse Checkout Preset · minimal ⭐ 推荐
# 只下载制作器内核，约 15MB
# 适用场景：做非地图类课件（语文/数学/物理/化学/生物/英语等）
# Hero 图片通过 CDN (jsDelivr) 按需加载，不随 skill 下载
# 社区课件在服务器上访问，不随 skill 下载
#
# 用法：
#   git sparse-checkout set --from-file .sparse-checkout-presets/minimal.txt

# 必需：制作器本体
skill/
# skill/assets 下大块资源按需下载（地图/图库默认不包）
!skill/assets/image-vault/
!skill/assets/historical-china/
!skill/assets/historical-world/
!skill/assets/hillshade/
# 保留 skill/assets/image-registry.json 和 timelines 等小文件
scripts/
references/
docs/

# 必需：知识树数据 + 制作配置（但剔除大 excerpts）
data/
!data/.venv/
!data/excerpts/

# 必需：根目录关键文件（仅小文件）
.gitignore
.gitattributes
LICENSE
MANIFEST
manifest.json
.nojekyll

# 不需要：图片图床（通过 CDN 访问，见 skill/assets/image-registry.json）
!teachany-images/

# 不需要：地图资产（按需下载，见 full-maps.txt / standard.txt）
!assets/maps/

# 不需要：社区课件（服务器在线访问，不下载）
!community/

# 不需要：示例课件、截图
!examples/
!screenshots/

# 不需要：根目录大文件（test-hero 图片 ~3MB）
!test-hero-*.png
