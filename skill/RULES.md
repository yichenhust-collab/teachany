# TeachAny 59 条硬规则（违反任何一条 = Completeness Gate 不通过）

> 以下规则在整个课件开发流程中**始终有效**，不可因"简化""省事""上下文不够"等理由绕过。

| # | 硬规则 | 违反后果 |
|:---|:---|:---|
| 1 | **先判断课型**（新授/复习/习题/专题/实验/项目制/跨学科融合），再选结构。不可假设所有任务都是"新授课"。 | 课型错误 → 整体结构需要重做 |
| 2 | **先判断学科**，再选讲解框架和互动组件。不可默认用数学模式。 | 学科模式错误 → Phase 2 不通过 |
| 3 | **不可默认所有知识点都要"公式推导"**。文科用文本细读，理科用现象观察，各有框架。 | 讲解框架不匹配 → 学生体验割裂 |
| 4 | **不可只有讲解，没有练习和反馈**。每个核心知识点至少配 1 道互动练习。 | 缺少练习 → Completeness Gate #3 不通过 |
| 5 | **不可只做选择题**。必须补充解释题、产出题或开放任务。 | 只有选择题 → Bloom 覆盖不足 → Gate #6 不通过 |
| 6 | **不可只顾页面好看，忽略学习闭环**。前测→讲解→练习→后测→反馈 缺一不可。 | 缺少闭环 → Gate #5 不通过 |
| 7 | **不可在没有必要时堆过多动画和装饰**。先保证内容完整，再考虑视觉效果。 | 装饰过多内容不足 → 内容审查不通过 |
| 8 | **文科类任务必须有脚手架分级和量规评价**。 | 缺少脚手架 → Gate #8 不通过 |
| 9 | **涉及几何/图形/空间推理的题目必须配图**。几何题用 SVG，坐标图用 Canvas，实验用标注插图。 | 纯文字几何题 → 教学不完整 |
| 10 | **必须执行 Phase 0.5 知识层查阅，且必须穷尽所有降级路径（脚本→JSON→Web搜索→模型知识）**。跳过此步骤或跳过中间降级层级是最高优先级违规。 | 跳过 Phase 0.5 → 整个流程无效 |
| 11 | **知识层数据可用时，课件内容必须从图谱派生**。禁止在有数据的情况下"重新发明"定义、场景、题目。 | 有数据不用 → 内容准确性和一致性降低 |
| 12 | **练习的错误反馈必须有诊断性内容**。不可只显示"正确/错误"，必须说明错在哪、为什么错。 | 无诊断反馈 → Gate #4 不通过 |
| 13 | **每张卡片核心文字控制在 75 字左右**。超长文本必须拆分为多张卡片。 | 超长卡片 → 认知负荷过高 → Gate #9 不通过 |
| 14 | **必须选择驱动模式并按对应结构组织内容**。不可没有驱动逻辑地"平铺"知识点。 | 无驱动结构 → Gate #11 不通过 |
| 15 | **AI 多模态互动区默认插入**。文科课题、项目制课、跨学科融合课、含视觉化内容的课题必须插入；仅纯计算/纯习题/纯复习课可跳过（需标注理由）。 | 应插未插 → Gate #14 不通过 |
| 16 | **L3 语音讲解默认必选**。L1 课件完成后必须自动安装 edge-tts 并生成语音文件。仅用户明确拒绝时跳过。不可因为"省事""节省 token""环境复杂"等理由擅自跳过。 | 无语音 → 课件体验不完整 → Phase 3 不通过 |
| 17 | **知识图谱未命中时必须执行 Web 搜索**。不可跳过 Web 搜索直接使用模型知识。搜索关键词需包含"课标""前置知识""易错点"等教学相关术语。 | 跳过搜索 → Phase 0.5 不通过 |
| 18 | **课件打包默认执行**。L1 课件完成后必须生成 manifest.json 并执行打包（Phase 3.5）。打包脚本不可用时至少生成 manifest.json。 | 无打包 → 课件无法被 Gallery 导入 |
| 19 | **默认仅生成中文课件**。除非用户明确要求"双语"或"英文版"，否则只生成 `index.html`（中文）。用户要求双语时同时生成 `index_en.html`（英文）。 | 不必要的英文版 → 浪费生成时间 |
| 20 | **理科实验交互必须使用生活化场景**（见 Section 6.4）。物理/化学/生物的实验类交互组件禁止使用纯抽象示意图，必须置于学生日常可感知的生活场景中。类比参照物必须贴近学生认知。 | 纯抽象实验 → 学生无法建立直觉 → 教学效果打折 |
| 21 | **课件必须使用标准 HTML 骨架模板**（见 Section 10.2.1）。必选 section（Hero、学习目标、前测、知识模块≥3个、综合任务、后测、小结）不可删除。导航必须使用 Sticky 顶部导航+前后翻页，禁止 Tab 切换或多页 HTML。 | 版式不一致 → Gate #18 不通过 |
| 22 | **文科课件 ABT 导入必须包含 AI 生成的情境插图**。语文、历史、地理、美术等文科课件的 ABT 情境导入模块，AI 必须主动调用 `image_gen` 生成配图并嵌入 HTML（见 Section 10.4.1）。生图不可用时降级为 SVG 代码生成的信息图（第三级降级），但不可省略整个视觉区域。 | 文科课件缺图 → Gate #19 不通过 |
| 23 | **支持多 Agent 协作的环境必须使用并行模式**。当运行环境支持 `task` 工具时，AI 必须按 Section 10.5 的协作架构分发任务（中文课件 + 英文课件 + 插图 + TTS 并行），不可在有条件时仍用单线程串行。协作模式必须在 Generation Gate 中标注。 | 有条件不并行 → 效率低下 → Gate #20 不通过 |
| 24 | **课件必须包含交互式知识图谱**（见 Section 10.2.3）。每个课件的 `#knowledge-graph` section 不可删除，`knowledgeGraphData` 必须从 `_graph.json` 注入。当前节点高亮、有课件节点可点击跳转、无课件节点虚线框。数据不可用时必须走降级路径（Web搜索→空图谱），绝不省略整个 section。 | 无知识图谱 → Gate #21 不通过 |
| 25 | **视频必须嵌入到对应知识模块的 section 内部，优先使用交互动画**（见 Section 10.2.4）。过程性变化优先用 CSS/JS/Canvas/SVG 交互动画实现；仅交互无法覆盖时使用 HTML `<video>` 标签静态嵌入，必须有 `controls`、`preload="metadata"`、`playsinline` 属性，外包 `.video-player` 容器。**禁止**仅用 JavaScript 动态创建视频元素。 | 无 `<video>` 标签或视频位置错误 → Gate #22 不通过 |
| 26 | **L3 语音必须有完整播放器 UI**（见 Section 10.2.5）。L3 生成后必须注入 `audioPlaylist` 数组（每个条目含 `sectionId` 关联对应 section），课件中必须有底部悬浮控制条（播放/暂停+进度条+调速+字幕）+ IntersectionObserver 滚动自动播放。**禁止**只添加隐藏 `<audio>` 标签而无任何播放控件。 | 无播放器 UI → Gate #23 不通过 |
| 27 | **Remotion 渲染必须安装中文字体**（见 Section 15.2 步骤 2.5）。L2 执行时，AI 必须检测并安装中文字体（Linux 下安装 fonts-noto-cjk）。SubtitleTrack.tsx 的 fontFamily 必须包含 `'Noto Sans SC'` 降级。 | 中文字幕乱码 → Gate #24 不通过 |
| 28 | **严禁使用 Web Speech API（`window.speechSynthesis`）**。浏览器语音合成质量极低、设备不一致、无法离线。L3 必须使用 edge-tts 生成独立 mp3 文件。任何情况都不得将 Web Speech API 作为降级方案。 | 使用 Web Speech API → L3 直接不通过 |
| 29 | **必须执行 Phase 0.5 步骤 0 版本检测**。每次运行 Skill 时必须先执行 `bash scripts/check-skill-update.sh` 检测并拉取最新版本。检测失败时静默降级（不阻断课件生成），但不可跳过执行。更新成功后必须重新读取 `SKILL_CN.md` 和数据文件。 | 跳过版本检测 → 可能使用过期知识库 → 课件质量风险 |
| 30 | **多版本学科必须询问用户教材版本**（见 Phase 0 第 7 步）。读取 `data/editions/registry.json`，统编学科自动使用人教版，多版本学科（数学/物理/化学/生物学/英语/地理等）必须列出可选版本并询问用户。确认版本后在 Phase 0.5 步骤 3.6 中从 ChinaTextbook 仓库获取真实课本内容注入课件。注册表不存在或用户拒绝时静默降级，不阻断流程。 | 未确认版本 → 课件可能使用错误版本的例题和章节编排 |
| 31 | **基线强制 · Edge-TTS 语音**（见 Section 0）。每个课件必须生成独立 mp3 + 字幕 + 完整播放器 UI，仅用户明确拒绝时跳过并在 Gate 中记录。严禁以"页面已经够丰富"为由省略。 | 无 TTS 且无用户拒绝记录 → Gate 直接不通过 |
| 32 | **基线强制 · Remotion 教学动画**（见 Section 0）。每个课件必须含 ≥1 段**真实 Remotion 渲染的 mp4 视频**用于演示过程性变化（含文科的意境动画、诵读节奏动画、历史演变动画等）。⛔ Canvas/SVG/CSS 时间线动画**不得作为替代**，只能是附加增强。唯一例外：Node 环境完全不可用且无法安装时，必须在 Gate 中声明并由用户书面豁免。 | 无真实 Remotion mp4 → Gate 直接不通过 |
| 33 | **基线强制 · Canvas 互动组件**（见 Section 0）。每个课件必须含 ≥1 个原生 `<canvas>` 互动组件（拖拽、画板、参数滑块、实时绘图任一）。若主题确实无 Canvas 场景（如纯文言字词），必须用 SVG 交互动画替代并在 Gate 中说明。 | 无 Canvas 且无说明替代 → Gate 直接不通过 |
| 34 | **基线强制 · AI 生图 ≥2 张**（见 Section 0）。每个课件必须调用 `image_gen` 生成 ≥2 张情境/意境/过程插图并嵌入 HTML。文科、科学、工程、社科课件**一律不得跳过**。仅纯计算题课可在 Gate 中附理由跳过。**生图来源铁律**（v5.34.12 新增）：只允许走**宿主 IDE 原生提供的 `image_gen` 工具**（WorkBuddy / CodeBuddy 等会自动代理到其内部图像服务）；⛔ 严禁直连 OpenAI / Gemini / Replicate / nano-banana / Tripo / Hunyuan 等任何第三方生图 API；⛔ 严禁读取 `.env` / memory 中用户私人 API Key 做隐式生图；⛔ 严禁在 `scripts/export-pptx.py` 或其他发布类脚本里注入任何生图逻辑（PPTX 是 HTML 的派生件，不单独生图）。**唯一例外**：用户在**当前对话**中**明确要求**用其私人 key 生图——即便用户曾在以往对话中提供过 key，本次若未重新明确，也不得使用。 | 文/科/工/社科课件缺生图 / 脚本中出现 requests → api.openai.com 等第三方生图调用 / 读取用户 key 做隐式生图 → Gate 直接不通过 |
| 35 | **地图基线 · 底图必须是 Web Mercator XYZ 瓦片，与 GeoJSON 原生对齐**（v5.22 重大修订，见 Section 18.5.3）。教学地图底图**必须用 `L.tileLayer('https://.../{z}/{x}/{y}.png')`** 标准 XYZ 瓦片（Web Mercator 投影），Leaflet 原生对齐 `L.geoJSON`。推荐双层叠加：`cartodb-basemaps-{s}.global.ssl.fastly.net/dark_all/{z}/{x}/{y}.png`（深色底图，提供海陆+国界+地名）+ `server.arcgisonline.com/ArcGIS/rest/services/World_Shaded_Relief/MapServer/tile/{z}/{y}/{x}`（半透明地形浮雕，opacity 0.35-0.45）。⛔ **严禁用 `L.imageOverlay` 铺全球 equirectangular 静态大图到 `[[-90,-180],[90,180]]`**——Leaflet 默认 Web Mercator 投影会按 Mercator 坐标解读纬度，equirectangular 源图在高纬度会被垂直拉伸，与 WGS84 GeoJSON 在中高纬度（含地中海以北）必定错位。⛔ **严禁用 ECharts `graphic:[{type:'image'}]`**——graphic 是 DOM 绝对定位覆盖层，不跟随 geo 组件变换，同样必定错位。 | 用 imageOverlay 全球铺底图 / 用 ECharts graphic 铺底图 → Gate 直接不通过 |
| 36 | **地图基线 · 初始视图必须聚焦教学核心区域**（见 Section 18.2 / 18.5.1）。地图初始化后必须立即调用 `map.fitBounds(coreBounds, {padding:[20,20]})` 或 `map.setView([lat,lng], zoom)` 将视口对准该课件的教学核心区域（讲希腊→爱琴海+伯罗奔尼撒；讲秦朝→战国七雄辖区；讲长江经济带→中下游沿岸）。禁止停在默认 `[0,0]` 世界中心或显示大片无关海洋/空白。 | 初始视图未聚焦核心区域 → Gate 直接不通过 |
| 37 | **发布基线 · 必须跑 `rebuild-index.py` 三件套才算发布成功**（见 Phase 3.6，v7.7 强化）。仅 `git push` 课件目录而不运行 `python3 scripts/rebuild-index.py` 不构成发布成功。v7.7 起 `rebuild-index.py` 会自动串联 `sync-community-index.py` 和 `build-teachany-kg-manifest.py`，产出的 `registry.json` / `community/index.json` / `scripts/teachany-kg-manifest.json` / `data/trees/**/*.json` 必须全部被 commit；脚本输出的 `⚠️ 文件存在但知识树未引用` **就是真发布失败信号**（v5.20 纠正：v5.19 的"假报警"结论错误），警告未消禁止 push。 | 仅 push 不跑 rebuild-index / 只跑 registry 不同步 community/index 或 kg-manifest / 警告未处理就 push → Gate 直接不通过 |
| 38 | **发布基线 · `manifest.json` 的 `node_id` 必须是 `data/trees/*.json` 旧 schema 中真实存在的节点 ID**（见 Section 17.2 + v5.20 重大纠正）。发布前必须用 `grep -rn "\"id\":\s*\"<NODE_ID>\"" data/trees/<subject>-<level>.json` 强制校验，⛔ 严禁编造节点 ID，⛔ 严禁用 `data/<subject>/<branch>/_graph.json` 新 schema 的节点 ID 代替（`tree.html` 只加载旧 schema，新 schema 节点 ID 挂了也不会显示在知识地图上）。若校验失败必须当场修正 manifest、用 Python 脚本把课件 ID 注入对应节点的 `courses[]` 数组、重跑 rebuild-index 直到警告清零，才能 push。 | node_id 不存在于 `data/trees/*.json` / 只挂 `_graph.json` 不挂 `data/trees/*.json` → Gate 直接不通过 |
| 39 | **地图资源基线 · GeoJSON 必须用"本地相对路径 + jsDelivr CDN 回退"双路径**（v5.21 新增 / v5.22 修订范围，见 Section 18.5.2）。GitHub Pages 对 `data/geography/` 下的大型 `.geojson` / `.jpg` 存在**跳过部署**现象（即便文件已 tracked 并 push 到 main，raw.githubusercontent.com 能 200，但 `<user>.github.io/<repo>/data/geography/xxx.geojson` 返回 404）。必须使用本 SKILL 提供的 `geoFetchJson()` 工具函数：先尝试 `../../data/geography/...` 本地路径（本地预览 + 其他 host 场景），失败则自动回退到 `https://cdn.jsdelivr.net/gh/<user>/<repo>@main/data/geography/...`。⛔ 严禁直接 `fetch('../../data/...')` 硬编码单一路径。发布前必须用 `curl -I https://<user>.github.io/<repo>/data/geography/world/countries.geojson` 验证线上 200 或确认课件 HTML 使用了双路径回退，否则**不得声称"地图已上线"**。注：v5.22 修订后 `.jpg hillshade` 已不再作为底图（改用 XYZ 瓦片，见 #35），本规则主要覆盖 GeoJSON 和其他必要的静态数据文件。 | GeoJSON 线上 404 且未用双路径回退 → Gate 直接不通过 |
| 40 | **选择题基线 · `handleQuiz(el, qid, correctVal, selectedVal)` 每个选项的 `selectedVal` 必须等于该选项自身 `data-val`**（v5.23 新增）。选择题 onclick 调用签名为 `handleQuiz(this, '<quizId>', '<correctVal>', '<selectedVal>')`，其中第 4 参 `selectedVal` **必须等于该 `<div>` 元素的 `data-val` 属性值**（A/B/C/D），同一题 4 个选项的 `selectedVal` 必须互不相同。⛔ 严禁将所有选项的 `selectedVal` 硬编码为同一固定值（如全部传 `'A'`）——否则无论用户点哪个选项，系统都判定为同一选择值，导致答题逻辑全部失效。⛔ `correctVal`（第 3 参）必须与 `quizAnswers[qid].correct` 保持一致；如不一致，以 `quizAnswers` 对象为权威、修改 HTML onclick。**发布前校验方法**：`grep -oP "handleQuiz\(this,'(\w+)','(\w)','(\w)'\)" index.html` 提取全部调用，检查同一 `qid` 下第 4 参是否覆盖 A/B/C/D 四个不同值，且第 3 参与 `quizAnswers` 一致。 | selectedVal 硬编码固定值 / correctVal 与 quizAnswers 不一致 → Gate 直接不通过 |
| 41 | **知识树基线 · `data/trees/*.json` 严禁"吸星式"污染前置、英文域名、grade 类型混用、同义节点重复**（v5.24 新增，见 Section 17.3）。**污染模式定义**：同一棵树内有单一节点被 ≥4 个其他节点作为**唯一** `prerequisites` 依赖（俗称"吸星大法"，所有无关节点都被错误挂到该节点之下）；`domain.name` 为纯英文（如 `"Grammar"` / `"Classical Chinese"`）而未翻译中文；同一树内 `grade` 字段同时存在整数 `1` 和字符串 `"1"`；同一 domain 内存在同义名重复节点（如 `chn-e-char-structure` 与 `chn-e-character-structure`）。**发布/修改知识树前必须运行 `python3 scripts/clean-tree-pollution.py` 做 dry-run 扫描**，任意一项不通过必须用 `--apply` 清污后才能提交。⛔ 严禁手动编辑知识树后直接 push 而不跑清污脚本。⛔ 严禁在同一 AI 扩展会话里批量把几十个节点的 `prerequisites` 写成同一个节点 id（这是 AI 扩展脚本最易产生的坏数据模式）。**清污脚本 5 项职责**：①检测集中前置阈值 ≥4 → 清空；②`grade` 字符串数字转 int（区间保留）；③英文域名按词典翻译中文；④同义节点按 (domain, normalized_name) 去重，保留 id 更短/挂课件的节点；⑤删除指向已删节点的孤儿引用 & 自环。 | 吸星前置未清 / 英文域名未翻 / grade 类型混用 / 重复节点未去重 / 未跑清污脚本 → Gate 直接不通过 |
| 42 | **课件挂载基线 · `manifest.grade` / `manifest.subject` 必须与 `manifest.node_id` 的前缀约定严格一致**（v5.27 新增，v5.34.6 补 `sci`）。**前缀约定**：节点 id 形如 `<subject>-<level>-<topic>`，其中 `<subject>` 为学科前缀（`chn` 语文 / `math` 数学 / `eng` 英语 / `phy` 物理 / `chem` 化学 / `bio` 生物 / `hist` 历史 / `geo` 地理 / `it` 信息技术 / `sci` 小学科学），`<level>` 为学段前缀（`e` elementary G1-6 / `m` middle G7-9 / `h` high G10-12）。**校验规则**：(a) `manifest.grade` 落在的学段必须与 `node_id` 的 level 前缀一致（如 `grade: 10` 挂到 `chem-h-xxx` 合法，挂到 `chem-m-xxx` 必定违规）；(b) `manifest.subject` 必须与 `node_id` 的 subject 前缀对应（`subject: chemistry` 必须挂到 `chem-*` 节点）；(c) HTML `<title>` / `<meta name="course-id">` 中若含"高中/必修X/高一/高二/高三"则 grade 必须 10-12，"初中/七/八/九年级/中考" → 7-9，"小学/一/二/.../六年级" → 1-6。⛔ 严禁仅凭 `manifest.grade` 数字决定挂载节点 —— 例如曾错把一个 `grade:9` 但 HTML title 写明"**高中化学必修一**"的课件挂到初中化学树上，根因是 manifest 标签错误而非真的属于初中。**发布前必须运行 `python3 scripts/validate-courseware.py` 通过零错误**，任何学段/学科前缀与 manifest 冲突会让校验脚本退出码 1，阻断 rebuild-index 与 git push。修复原则：**HTML title 中的"高中必修X"等权威信号优先于 manifest 数字** —— 如有冲突以 HTML 为准修正 manifest。 | manifest.grade 学段与 node_id 前缀不一致 / HTML title 显式学段与 manifest.grade 冲突 / 未跑 validate-courseware 就 push → Gate 直接不通过 |
| 43 | **课件标识基线 · 统一的 title 格式 + 强制 `manifest.teachany_version` + Gallery 版本徽章**（v5.28 新增）。**标准 title 格式**：`《课件名》 · 《学段》《学科》 G{grade} · TeachAny v{version}`，例如 `减数分裂与受精过程 · 高中生物 G10 · TeachAny v5.27` / `复韵母乐园 · 小学语文 G1 · TeachAny v5.27`，三段用全角分隔符 ` · ` 连接。⛔ 严禁使用 "《课件名》- TeachAny 互动课件" / "《课件名》· 教学课件" / "《课件名》— 七年级生物互动课件" 等不含学段学科的简写格式，这种 title 在自动扫描时无法识别学段，导致挂载校验产生模糊空间。**manifest 必填字段**：`teachany_version`（字符串，形如 `"5.27"`），标记课件制作时使用的 TeachAny SKILL 版本，随 SKILL 升级时课件 re-publish 应更新此字段；该字段由 `rebuild-index.py` 收集进 `registry.json`，Gallery 卡片会渲染为渐变金色徽章 `⚡ TeachAny v5.27`。**发布前校验**：`validate-courseware.py` 对每个课件检查 4 项——① title 包含 `"TeachAny v"` 标识；② title 包含学段中文（小学/初中/高中）；③ title 包含年级 `G{n}` 或 `{n}年级` 格式；④ manifest 有非空 `teachany_version`；任一不满足报错阻断发布。**批量迁移工具**：`scripts/normalize-titles.py --apply` 会自动把所有课件改为标准格式并写入 manifest。 | title 不含 TeachAny 版本 / 不含学段 / 不含年级 / manifest 缺 teachany_version / 未跑 validate-courseware 就 push → Gate 直接不通过 |
| 44 | **节点挂载基线 · 同一 `node_id` 最多 1 份 official 课件（community 允许多份并按 `likes` 排序）**（v5.29 新增，v5.29.1 修订）。**核心语义**：`data/trees/*.json` 的每个节点代表一个课标知识点；官方课件是该知识点的**权威示范**，故同一节点最多 1 份 `status=official`；社区课件（`status=community`）可以**多份共存**，Gallery 会按"心标"（`likes` 字段，来自 localStorage + `community/index.json`）**降序展示**，同分时 official 优先。这是 `scripts/courseware-hub.js` 已实现的排序规则：先按 `likes` 降序，再按源优先级（`official > community_registry > community_shared > user`）。⛔ 严禁在同一节点挂 ≥2 份 official 课件——这会导致用户在同一知识点卡片看到两份"权威"版本互相冲突（如曾发生 `phy-m-pressure` 同时挂 community 单页简版和 official 深色完整版，两者实为不同设计稿）。⛔ 严禁在 examples/ 下保留"内容相同 / 仅目录名不同"的课件目录——这类是重命名历史遗留，应直接删除一份。**发布前校验**：`scripts/validate-courseware.py` 末尾跨课件扫描阶段，若某 node_id 下 `len(officials) > 1` 立即报错退出 1；若是多份 community 课件则仅打印 `ℹ️ 信息`（非错误，帮助观察 Gallery 展示状态）。**冲突解决规则**：(a) 两份 official 内容完全相同 → 保留目录名更规范（不带 `teachany-` 冗余前缀）且 TTS 时间戳更新的一份；(b) 两份 official 内容不同 → 评审后合并为一份，或把次要版本降级为 `status=community`（允许共存）；(c) 其中一份仅是孤儿音频/源文件包（无 `index.html` 或 `manifest.json`） → 直接删除。⛔ 社区课件可有多份，但**同 `course_id` 的 examples 目录**仍必须唯一，不可通过复制目录伪造"多份课件"。 | 同 node_id 挂 ≥2 份 official / 同目录复制伪造 / 删课件后未 rebuild-index / 未跑 validate-courseware 就 push → Gate 直接不通过 |
| 45 | **AI 学伴基线 · 每个 HTML 课件必须内置右下角 AI 学伴悬浮球**（v5.34 新增，见 Section 10.2.6）。**强制要求**：(a) `<head>` 引入 `./ai-tutor.css`；(b) `</body>` 前引入 `./ai-tutor.js`（带 `defer`）；(c) `<script>` 最前面注入 `window.__TEACHANY_TUTOR_CONFIG__` 对象，必须包含 `courseTitle` / `subject` / `grade` / `learningObjectives[]` / `getContext()`；(d) 打包 `.teachany` 时必须把 `ai-tutor.css` 与 `ai-tutor.js` 一起打进包内（相对路径 `./ai-tutor.css` / `./ai-tutor.js`）。**安全红线**：⛔ 严禁把 API Key 以任何形式（明文/base64/obfuscate/window 变量/data-attr）硬编码到课件 HTML/JS/manifest 中；⛔ 严禁把 API Key 上传到任何后端或第三方分析服务；⛔ 严禁省略配置面板的"Key 仅保存在本浏览器"的明示提示；⛔ 严禁只注入 FAB 而不实现问答逻辑（静态假球），或只注入 `<script>` 而不渲染 UI。**答复难度**：AI 回答必须按 `grade` 调节长度与语体——小学 2-3 句口语化、初中 3-5 句结构化、高中 5-8 句可含专业术语和公式；严禁小学课件出现"学术长段"或高中课件出现"只说对的！"。**发布前校验**：`scripts/validate-courseware.py` 新增检查——① HTML 必须含 `ai-tutor.css` 引用；② HTML 必须含 `ai-tutor.js` 引用；③ HTML 必须含 `__TEACHANY_TUTOR_CONFIG__`；④ HTML 中不得出现 `sk-xxx` 形式的 API Key 字符串。 | HTML 缺 ai-tutor 引用 / HTML 缺 TUTOR_CONFIG / HTML 硬编码 API Key / 打包包内缺 ai-tutor.* 文件 → Gate 直接不通过 |
| 46 | **PPTX 导出基线 · 仅当 `output_formats` 包含 `"pptx"` 时触发，HTML 始终为主交付物**（v5.34 新增，见 Phase 3.7 + Section 12 L5；**v5.34.12 版式重写**：参考 `skill/pptx-design-guide.md`）。**触发规则**：Phase 0 第 8 步中用户明确选"HTML + PPTX 双格式"，或任务文本命中关键词（PPT/PPTX/幻灯片/投影版/讲义版/离线讲课版/打印讲义）。**执行要求**：(a) 在 L1 HTML 交付之后、Completeness Gate 之前执行 `python3 scripts/export-pptx.py <课件目录>`；(b) 产出 `<课件目录>/<课件名>.pptx` 与 `index.html` 同目录；(c) 幻灯片按 section 切分（Hero / 学习目标 / 前测 / 每个知识模块 / 综合任务 / 后测 / 小结），保留标题层级与关键 `<img src="./assets/*.png">`；(d) 互动组件（Canvas / 知识图谱 / 音频播放器 / AI 学伴）在 PPTX 中**降级为"扫码或点击链接回到 HTML 版"占位页**，不得删除整节内容；(e) `python-pptx` 安装失败或脚本执行失败时，HTML 正常交付，最终报告中标注"PPTX 导出失败"。**v5.34.12 版式升级**：导出脚本升级为"**图文美观版**"——封面左文右大图、每新模块前插节首图卡、无图 section 自动切双栏 bullet 或高亮卡片、题目独立成卡片、互动 section 用全屏占位卡片；配色走浅色主题（品牌紫 + 琥珀点缀），禁止 v5.34.0 的"顶部大蓝块 + 左文右小图"办公风。⛔ 严禁把 PPTX 当作 HTML 的替代品（PPTX 是派生件）；⛔ 严禁跳过 `output_formats` 检查而擅自生成或擅自不生成 PPTX；⛔ 严禁在 PPTX 中保留互动代码占位符（应降级为二维码或 URL）；⛔ **严禁在 `scripts/export-pptx.py` 中调用任何第三方生图接口**——PPTX 只消费 HTML 已有的 assets/ 图，不单独生图。**文件命名**：`<课件目录名>.pptx`（如 `math-m-vertex-form.pptx`），与 manifest.json 中 `course_id` 对齐。 | 用户要求 pptx 但未生成 / 用户未要求但擅自生成 pptx / PPTX 缺 section 切分 / 互动组件未降级 / PPTX 与 HTML 矛盾（如 PPTX 缺关键模块）/ export-pptx.py 偷偷加了生图调用 → Gate 直接不通过（仅适用于 output_formats 含 pptx 的课件） |
| 47 | **PPTX 含图基线 · 文件大小 ≥ 100KB 且含图 ≥ 1，图片数/幻灯片数 ≥ 30%**（v5.34.6 新增，硬规则 #46 的量化补充）。**根因**：此前导出的 PPTX 常沦为"只有文字的简陋版"，原因是 HTML 本身就没有 `<img src="./assets/*.png">`，`export-pptx.py` 的 `find_first_image()` 抓不到任何图——这时候 PPTX 文件只有 40-50KB 且 0 张图，用户看到完全交付不出。**强制执行流程**：(a) 触发 PPTX 导出前，**必须先确保 HTML 里 `<img src="./assets/">` 引用 ≥ 2 处**（由硬规则 #34 保证），否则先调用 `image_gen` 补图、改 HTML、再导 PPTX；(b) `scripts/export-pptx.py` 抓图时 HTML 的 `<img>` 相对路径必须指向实际存在的文件，**`find_first_image()` 返回 None 时必须在日志中 `⚠️ WARN: no image found in section xxx` 报出**，不能静默跳过；(c) 导出完成后执行 `scripts/validate-courseware.py` 对 PPTX 做三项检查：①文件大小 < 100KB 报 error；②`ppt/media/` 空且幻灯片 > 2 报 error；③图/slide 比 < 30% 报 warn。**发布前校验**：`python3 scripts/validate-courseware.py <course_id>` 扫到任一 error 即 exit 1，rebuild-index 必须跳过该课件、`git push` 脚本必须拒绝执行。 | PPTX < 100KB / PPTX 0 张图 / HTML 无 assets 引用却强制出 PPTX / export-pptx.py 默认静默不报图警 → Gate 直接不通过（仅 output_formats 含 pptx 时适用） |
| 48 | **社区自动提交基线 · 零配置 + 质检驱动自动合并**（v5.34.9 重写，迭代自 v5.34.8 双轨制）。**完整链路**：AI 生成课件 → `community/drafts/<course-id>/` 落地 → AI 询问用户意向 → 用户选"提交社区"→ AI 跑 `python3 scripts/submit-to-community.py <course-id>` → POST 到 TeachAny 官方 Cloudflare Worker（`DEFAULT_WORKER_URL`）→ Worker 用 Bot Token 调 GitHub `repository_dispatch` → `community-submit.yml` 自动创建分支 + `community/pending/*.teachany` + `*.json` + 开 PR → `validate.yml` 自动跑 `validate-courseware.py` → 0 错误打 `passed-validation` 标签 + PR 评论展示检测结果 → `auto-merge.yml` 监听 `passed-validation + community-courseware` 双标签 → squash merge 到 main → `community-publish.yml` 注册到 registry + Gallery → GitHub Pages 自动部署 → 5-10 分钟后课件在 Gallery 可见。**用户零门槛**：不需要 GitHub 账号、不需要 token、不需要任何配置文件，只需要"做完课件 + 说一声提交"。**质检严守**：validate-courseware.py 覆盖的硬规则就是发布闸门，缺音频/缺图/缺 AI 学伴/node_id 错挂都会被自动 `needs-revision` 打回，PR 评论直接列出具体错误让用户自己修，不需要管理员人工审核。**心标排序**：社区课件（status=community）允许多份挂同一 node_id，按 localStorage 心标数降序展示（unified-loader.js + courseware-hub.js 已实现）。**审核豁免**：自 v5.34.9 起"人工审核"**不是**默认路径——质检通过即合并，由机器校验 + 社区心标双层过滤保证质量。**"质检通过"= "发布成功"**：v5.34.9 起对"审核"的定义从"人工看"升级为"validate-courseware.py 机器校验"，质检通过即视为审核通过。**AI 绝对禁止**：(a) 未询问用户就直接 push（硬规则一票否决）；(b) 默认把课件写入 `examples/`（任何新课件必须落 `community/drafts/`）；(c) 跳过 submit-to-community.py 直接调 `git push`（绕过质检）；(d) 要求用户自己配 GitHub token（Worker 是唯一官方入口）；(e) 新课件 registry.json 自己打 `status=official`——该状态位是由硬规则 #49 定义的"管理员独立升级通道"唯一写入的。 | AI 未询问用户就 push / 默认写入 examples/ / 跳过 submit-to-community.py / 让用户自己配 token / 新课件 registry 标 official → Gate 直接不通过（发布失败不是警告是 exit 1） |
| 49 | **官方课件写入基线 · `examples/` 禁止任何形式的直推，只能通过"用户 skill 上传"或"管理员独立升级命令"落入**（v5.34.10 新增）。**强制规则**：(a) 创建、修改、删除 `examples/<id>/` 下的任何文件都被视为"官方课件写入"；(b) 上述写入**只允许**由以下两条合法路径产生：① `github-actions[bot]` 自动产生的 merge / publish commit（来自 community PR auto-merge 链路）；② commit message 末尾含 Git trailer `TeachAny-Promote: <course-id>` 的 commit（由未来独立的管理员升级命令写入）；(c) 以上两条之外的 commit 统称"直推"，一律拒绝。**双层护栏**：① 本地 `scripts/pre-push.sh` 在 `git push` 前检查，对本次 push 中涉及 `examples/` 的每个 commit 做白名单校验，命中直推立即 exit 1 拒绝 push；② 服务端 `.github/workflows/block-direct-push.yml` 在 push 到 main 后再次校验，发现直推则打红叉 + 自动开 `direct-push-violation` 标签 issue 提醒回滚。**紧急绕过条件**：仅在非课件改动（README / CI / 脚本本身）的极端 hotfix 场景下，owner 可用 `TEACHANY_ADMIN_BYPASS=1 git push` 绕过本地 hook，但服务端 workflow 仍会独立校验——若该 push 仍动到 `examples/` 则照样打红叉。**AI 绝对禁止**：(a) 教用户 `TEACHANY_ADMIN_BYPASS=1`；(b) 手动在 commit message 里伪造 `TeachAny-Promote:` trailer（只有管理员升级命令能合法生成）；(c) 在未安装 hook 的环境下直接 `git push` 含 `examples/` 变更；(d) 告诉用户"关掉 hook"或改 `.git/hooks/pre-push` 脚本；(e) 向 CI workflow 添加 `paths-ignore: ['examples/**']`。**hook 安装校验**：AI 在执行任何涉及 git push 的自动化流程前，若发现 `.git/hooks/pre-push` 不是指向 `scripts/pre-push.sh` 的软链，必须先执行 `ln -sf ../../scripts/pre-push.sh .git/hooks/pre-push && chmod +x scripts/pre-push.sh` 安装；安装失败时必须停止 push 并报告用户。**协议稳定性承诺**：`TeachAny-Promote:` trailer 是本仓库公开的管理员升级协议，任何实现管理员升级命令的工具（CLI / slash command / admin skill）都应在 commit message 里追加该 trailer 才算合法。 | 直推 examples/（无 trailer、非 bot commit）/ 伪造 trailer / 教用户绕过 hook / 卸载或篡改 pre-push hook / 给 block-direct-push workflow 加 paths-ignore → Gate 直接不通过（本地 hook exit 1 + 服务端 workflow 打红叉 + 自动开 violation issue） |
| 50 | **工具链自检基线 · Phase 0 必须先跑 `preflight-check.py`，工具不齐禁止进入 Phase 1**（v5.34.11 新增，专治国产模型静默跳过 L2/L3/image_gen）。**执行要求**：(a) Phase 0 开头（读完 SKILL_CN.md 之后，进入 Phase 0.5 知识层查阅之前）AI **必须**执行 `python3 scripts/preflight-check.py --json`，读取 `.teachany-preflight.json` 中的 `capabilities` 字段，据此宣告本次生成的实际能力范围；(b) `exit_code in {20, 30}` 时**必须**立刻停止并报告用户，绝不擅自进入 Phase 1；(c) `exit_code == 10` 时已自动安装部分依赖，必须在生成报告中列出"本次自愈安装了哪些工具"；(d) 无法自动安装的工具，AI 必须给出**具体一行命令**（从 `preflight-check.py` 输出的 `hint` 字段中取）让用户 copy-paste，而不是一句"请你自己装一下 Node"。**image_gen 探针**：Phase 0.5 结束前 AI **必须**调用 `image_gen` 工具生成一张最小探针图（prompt 可简单为 `"test pixel art 8x8"`），成功后写入 `.teachany-image-gen-probe.json`；连续 3 次失败方可按硬规则 #34 降级为占位符，并在 Generation Gate 中显式声明"image_gen 不可用降级"。**bootstrap 自愈**：当 `preflight-check.py` 报告多个缺失时，AI 可以先跑一次 `bash scripts/bootstrap-tools.sh` 做跨平台一键装齐，再重跑 preflight。**严禁静默跳过**：(a) ⛔ 严禁看到 "edge-tts 不可用" 就跳 L3 而不报告用户；(b) ⛔ 严禁看到 "Node 不可用" 就用 Canvas/SVG 冒充 Remotion（Canvas 只能是 L1 补充，不能顶替 L2）；(c) ⛔ 严禁看到 "image_gen 探针失败" 就把插图全换成 emoji/占位色块而不进 Gate 声明；(d) ⛔ 严禁把 `preflight-check.py` 报告的 error 降级为 warn 然后继续；(e) ⛔ 严禁在 `--json` 输出之外自己硬写 `capabilities` 值。**validator 硬校验**：`validate-courseware.py` 已同步增强——Canvas 必选（#33）、Remotion mp4（#32 warn）、知识图谱 section（#24 warn）、地图 XYZ 瓦片（#35 warn + L.imageOverlay 全球铺图 error）、video 标签规范（#25 warn），这些校验与 preflight 的 capabilities 字段形成闭环：preflight 宣告"L2 可用"却交付无 mp4 课件 → validator warn；preflight 宣告"地图 CDN 可用"却用了 L.imageOverlay 全球铺图 → validator error。 | AI 未跑 preflight 就 Phase 1 / AI 把 exit 20/30 报告当 warn 忽略 / AI 用 Canvas 冒充 Remotion mp4 / AI 见 image_gen 不可用就静默换 emoji / 把 preflight 的 error 硬降为 warn 继续 → Gate 直接不通过（含 validator error 即阻断发布） |
| 51 | **Pillow 生图字体基线 · 含化学/数学公式符号的本地生图必须使用支持 Unicode 上下标的字体**（v6.1 新增，见 Section 10.4.3）。**强制规则**：(a) AI 使用 Pillow 生成含 Unicode 上下标字符（₂₃⁺⁻↑↓等）的教学配图时，**必须使用 Arial Unicode MS 或 Noto Sans CJK** 字体，禁止使用 Hiragino Sans GB / STHeiti / Heiti SC（这些字体对上下标字符渲染为 .notdef 方框 ⊠）；(b) 生图脚本必须使用 Section 10.4.3 定义的字体降级链 `FONT_FALLBACK_CHAIN_STEM`，按优先级查找可用字体，降级链全部失败时必须报错终止（不可静默使用不支持的字体）；(c) `check_baseline.sh` 的 B-3a+ 检查会对 PNG 图片做 `.notdef` 方框抽检——若检测到大量方框像素，判定为字体不兼容，WARN。**典型故障**：使用 `Hiragino Sans GB` 生成含 `Al₂O₃`、`H₂O`、`Fe²⁺` 等化学公式的图片，所有下标/上标显示为方框，用户反馈"图片有乱码"。 | Pillow 生图含方框乱码 / 使用禁用字体清单中的字体生成含公式图片 / 字体降级链全部失败仍静默继续 → Gate 直接不通过 |
| 52 | **严谨度铁律 · 红线一·闭环验证**（v6.2 新增，见 SKILL_CN Section 0.4）。声称"已完成"、"已修复"、"应该没问题"之前**必须实际跑命令并贴出执行输出作为证据**。课件开发场景的强制证据清单：(a) 生成 HTML → `head` 输出 + 浏览器/puppeteer 打开成功的截图；(b) Remotion mp4 → `ffprobe -show_streams` 确认含 `codec_type=audio` 和 `codec_type=video` 双轨；(c) Edge-TTS → `ls -la tts/*.mp3` + `file tts/*.mp3` 显示 `MPEG audio` + `wc -l tts/*.srt` ≥ 段落数；(d) image_gen → `ls -la assets/illustrations/*.png` + 至少 1 张人工 review 截图；(e) 批量改 N 个文件 → `find ... -exec md5 -q {} \\; \| sort -u \| wc -l` = 1 验证内容一致；(f) 修 ai-tutor.js → curl 实测 API + Node 模拟流式解析 + 浏览器 Console 实测；(g) 推送到 Git → `git log -1` + `git ls-remote origin main` + 本地 HEAD 三个 hash 一致。⛔ 没有输出证据的"完成"叫自嗨，不是交付——用户接手的第一件事就是发现你撒谎。 | 声称完成但无证据输出 / Gate 各项检查无对应工具输出截图 / 批量改后未做 md5 一致性验证 → Gate 直接不通过 |
| 53 | **严谨度铁律 · 红线二·事实驱动**（v6.2 新增）。说"可能是 X 问题"、"也许是 Y 原因"、"应该是 Z 导致"之前**必须先用工具验证**。课件场景禁止猜测 → 强制验证清单：(a) "可能是浏览器缓存" → 让用户贴 Console 第一行版本号 / 用 curl 模拟浏览器 fetch；(b) "API key 应该过期" → `curl -H "Authorization: Bearer ..." {endpoint}` 实测；(c) "这个模型不支持流式" → curl 加 `-N` 实测 SSE 输出；(d) "字体应该装上了" → `fc-list \| grep <fontname>` 验证；(e) "ffmpeg 应该可以用" → `ffmpeg -version` 验证；(f) "这个 prompt 能让模型理解" → 实际跑一次 image_gen / chat 看结果；(g) "课件应该可以在手机打开" → puppeteer 设 viewport 375×667 截图；(h) "改完所有课件了" → 用 `grep -L` 找漏改的。⛔ 未经验证的归因 = 甩锅，不是诊断——猜的越多，用户来回的次数越多，浪费的时间越多。**先工具，再开口。** | 未验证就给用户归因 / 把猜测当结论给用户 / 同一问题反复猜测多个原因都不验证 → Gate 直接不通过 |
| 54 | **严谨度铁律 · 红线三·穷尽一切**（v6.2 新增）。声称"无法解决"、"建议你手动处理"、"已尝试所有方法"、"超出能力范围"之前**必须走完通用方法论 5 步**：①闻味道（列出已试方案找共同模式）；②揪头发（用 web_search 搜官方文档原文 + read_file 读源码上下文 50 行 + 反转假设）；③照镜子（是否在重复？是否该搜文档却凭记忆？）；④执行**本质不同**的新方案（详见硬规则 #55）；⑤复盘（详见硬规则 #56）。⛔ 步骤 1-4 完成前禁止向用户提问——除非需求本身模糊，那先澄清再执行。⛔ 训练你的算力很高，没走完 5 步说"我无法解决" = **缺乏韧性**。 | 未走完 5 步就放弃 / 未读官方文档原文就声称"已尝试所有方法" / 把"超出能力范围"当推卸借口 → Gate 直接不通过 |
| 55 | **严谨度铁律 · 红线四·失败 2 次必换"本质不同"的方案**（v6.2 新增）。**反复改同一处的参数 = 原地打转**，不是"尝试新方案"。课件开发的典型反例 → 应换思路的本质不同方案：(a) Remotion 渲染失败反复改 fps/durationInFrames → 改用 `<Sequence>` 重构时间轴 / 拆成多个短 Composition / 改 headless 渲染；(b) 字体不显示反复改 CSS `font-family` → 用 `@font-face` 显式注册 / base64 内嵌 / 验证 fc-list 路径；(c) TTS 朗读断句不对反复改 voice → 在文本里加 SSML `<break>` 标签 / 拆成多段录 / 换 voice；(d) API 报 400 反复改 prompt 措辞 → 验证 model id / 检查请求 body schema / 看 official 错误码文档；(e) ECharts 地图错位反复调 `geo.zoom`/`center` → 换实现方案：从 `geo` 改用 `Leaflet imageOverlay`（同硬规则 #35）；(f) OpenRouter 返回空反复改 reasoning 参数 → 直接换非推理模型 / 换非流式 / 换服务商。⛔ **第 2 次失败立即停下换思路；第 3 次还失败必须读官方源码或文档原文。** | 第 2 次失败仍改同一处参数 / 反复换同类型方案不换思路 / 第 3 次失败仍未读官方文档 → Gate 直接不通过 |
| 56 | **严谨度铁律 · 红线五·修一个 bug 顺手扫同类问题**（v6.2 新增）。发现并修复 X 后**必须立即扫描同类问题**一次性全部修掉，"一个问题进来，一类问题出去"。课件场景标准动作 → 必扫的同类清单：(a) 一个课件的 ai-tutor.js 有 bug → 313 个课件全部 `find ... -exec cp` 同步 + md5 验证一致；(b) 一个课件的中文标题导致 fetch header 报错 → 检查所有 header 字段（Authorization / Referer / Title）+ Base URL / API Key 是否同样含中文；(c) 一个课件的 Remotion mp4 是哑片 → 用 `ffprobe` 批量扫所有 `assets/video/*.mp4` 找出全部哑片；(d) 一个课件的 image alt 缺失 → grep 全部 `<img>` 找无 alt 的；(e) 一个课件 SSE 流式解析有问题 → 同步检查非流式 fallback 分支 + 错误处理分支；(f) 一处发现 localStorage 坏数据 → 加自动迁移逻辑（启动检测 + 读取清洗 + 保存拒绝）三层防御；(g) 一段 Edge-TTS 朗读速度不对 → 全部 mp3 用 `ffprobe -show_format` 检查时长是否合理。⛔ "修完一个就交付" = 留坑给下一次——P8 格局是一次性把同类问题扫干净。 | 修完单点未扫同类 / 同类问题用户二次反馈 / 批量任务只改一处不全量同步 → Gate 直接不通过 |
| 57 | **Hero 图基线 · 先查后生四层降级 · 每个课件必须有 hero 封面图**（v6.3 新增，见 Section 0.5）。**核心原则：先复用已有 hero，找不到再生图**。**四层降级链（按顺序尝试，命中即停）**：(a) **L1 本地优先**：课件 `assets/*hero*.{png,jpg,webp,svg}` 已存在 → 直接用，**严禁重新生成**；(b) **L2 teachany-images 图床**：`git@github.com:weponusa/teachany-images.git` 按 `<subject>/<topic>-hero.png` 查找 → 命中后复制到课件 `assets/` 或用 jsDelivr CDN 引用；(c) **L3 hero-review 精选版**：`hero-review/<subject>_grade<N>_<course-id>.png` 精确匹配 → 复制到课件 `assets/<course-id>-hero.png`；(d) **L4 image_gen 兜底**：仅在 L1-L3 全未命中时才调用，按学段差异化 prompt（小学=温暖卡通/初中=半写实信息图/高中=学术几何），**生成后必须回写 teachany-images 仓库**供后续同主题课件 L2 命中。**HTML 引用**：Hero section 必须用 `<img class="hero-cover-img" src="...">` 或 `background-image: url(...)` 真实引用，**禁止**仅 alt 占位或纯 emoji 替代。**发布前校验**：`python3 scripts/find-hero.py <课件目录>` 必须输出某层命中或 L4 生成成功；`python3 scripts/check-hero.py community/` 必须 0 错误。⛔ 严禁跳过 L1-L3 直接调 image_gen（浪费 token 且破坏视觉一致性）；⛔ 严禁 HTML 引用不存在的路径（404 broken image）；⛔ 严禁 Hero section 仅文字无配图；⛔ L4 生成失败必须重试 ≥3 次，仍失败需用户书面豁免。 | 跳过 L1-L3 直接生图 / 课件无 hero 文件 / HTML 引用 broken 路径 / Hero section 仅文字 / image_gen 失败未重试就跳过 → Gate 直接不通过 |
| 58 | **真实交互与连续音频基线 · 禁止用图片伪装交互模块**（v7.4 新增）。任何写着"互动/探究/实验/画布/地图/拖拽/跟读"的区域，必须包含真实可操作控件（`<button>`/`<input>`/`<canvas>`/拖拽事件/地图事件/音频播放器等）和状态反馈；⛔ 严禁把静态 PNG/SVG/data:image 当作"互动模块"展示。图片只能作为插图或封面，不能替代交互。语音课、拼音课、英语课、口语/朗读课必须提供**独立连续音频播放器**：`audioPlaylist` 至少覆盖导学 + 关键发音/段落，`<audio controls>` 或完整悬浮播放器必须可见，播放结束能自动进入下一段；单个点击发音按钮不能替代连续音频。发布前必须浏览器验证：至少点击 1 个交互控件、拖动 1 个滑块/画布或完成 1 次拖拽，并检查 Console 0 error。 | 图片式假交互 / 只有视频无独立音频 / 只有单点音效无连续播放 / 互动控件无反馈 / 未浏览器验证交互 → Gate 直接不通过 |
| 59 | **标准知识图谱模块基线 · 必须使用 `scripts/teachany-knowledge-graph.js` 渲染图谱**（v7.5 新增，v7.6 强化）。课件只能通过一行容器声明挂载图谱：`<div data-teachany-kg="<node_id>"><canvas class="tkg-fallback-canvas" width="720" height="120"></canvas></div>`，并引入 `scripts/teachany-knowledge-graph.{css,js}`。模块自动读 `scripts/teachany-kg-manifest.json` 渲染本节点+前序+后续+同域，视觉复用 tree.html（域色渐变节点、实线/虚线边、悬停+drop-shadow、tooltip）。**位置要求**：图谱 section 必须放在课件正文的**最底部**——footer 之前、AI 学伴脚本之后，作为收尾模块。**有/无课件区分**：manifest 只收录 `courses[].path` 实际存在 index.html 的课件；有课件节点实心高亮 + 点击直接打开课件（`window.open(target, "_top")`），无课件节点虚线框 + 仅作图谱导航。⛔ 严禁再在 `#knowledge-graph` 里手写 SVG / d3 / 静态图片 / 纯 div 网格“伪装图谱”。⛔ 不允许修改模块源文件来“定制”，风格跟随只能通过覆盖 CSS 变量（`--kg-primary` `--kg-bg` `--kg-card` `--kg-border` 等）。发布前必须跑 `python3 scripts/build-teachany-kg-manifest.py` 刷新索引。浏览器校验时，模块根元素应挂 `.tkg-root`，渲染出 ≥3 个 `.tkg-node-group`，有/无课件节点的 `stroke-dasharray` 不同。 | `#knowledge-graph` 内自定义图谱而没有 `data-teachany-kg` / 缺模块脚本或样式 / 节点 id 不在 manifest / 图谱不在 footer 之前 / 未区分有/无课件 / 修改了模块源文件 → Gate 直接不通过 |
