# 基本类型 / Primitive types

<!-- TeachAny KP: kp_id=kp-cs-ap-a-primitive-types node_id=cs-ap-a-primitive-types subject=cs stage=high -->

## 元数据

| 字段 | 值 |
| --- | --- |
| kp_id | `kp-cs-ap-a-primitive-types` |
| node_id | `cs-ap-a-primitive-types` |
| 知识点 | 基本类型 |
| 英文名 | Primitive types |
| 学科 | 计算机 (cs) |
| 学段 | 高中 (high) |
| 年级 | 10 |
| 课标系统 | AP 大学先修 |
| 领域 | Unit 1-2 基础与对象 Primitives & Objects |

## 课标原文（AP Course Description）

### 课标原文

1.2.B.1
The three primitive data types used in this course are int, double, and boolean. An int value is an integer. A double value is a real number. A boolean value is either true or false.

1.2.B.2
A variable is a storage location that holds a value, which can change while the program is running. Every variable has a name and an associated data type. A variable of a primitive type holds a primitive value from that type.

1.4.A.1
Every variable must be assigned a value before it can be used in an expression. That value must be from a compatible data type. A variable is initialized the first time it is assigned a value. Reference types can be assigned a new object or null if there is no object.

1.4.A.2
The assignment operator = allows a program to initialize or change the value stored in a variable. The value of the expression on the right is stored in the variable on the left.

## 课标摘要

1.2.B.1
The three primitive data types used in this course are int, double, and boolean. An int value is an integer. A double value is a real number. A boolean value is either true or false.；1.2.B.2
A variable is a storage location that holds a value, which can change while the program is running. Every variable has a name and an associated data type. A variable of a primitive type holds ...；1.4.A.1
Every variable must be assigned a value before it can be used in an expression. That value must be from a compatible data type. A variable is initialized the first time it is assigned a value....；1.4.A.2
The assignment operator = allows a program to initialize or change the value stored in a variable. The value of the expression on the right is stored in the variable on the left.
