# 递归搜索与排序 / Recursive search/sort

<!-- TeachAny KP: kp_id=kp-cs-ap-a-recursive-search node_id=cs-ap-a-recursive-search subject=cs stage=high -->

## 元数据

| 字段 | 值 |
| --- | --- |
| kp_id | `kp-cs-ap-a-recursive-search` |
| node_id | `cs-ap-a-recursive-search` |
| 知识点 | 递归搜索与排序 |
| 英文名 | Recursive search/sort |
| 学科 | 计算机 (cs) |
| 学段 | 高中 (high) |
| 年级 | 12 |
| 课标系统 | AP 大学先修 |
| 领域 | Unit 9-10 继承与递归 Inheritance & Recursion |

## 课标原文（AP Course Description）

### 课标原文

1.15.B.2
String
Develop code to call A object has index values
methods on string objects from 0 to one less than the length
and determine the result of of the string. Attempting to access
calling these methods. indices outside this range will result in a
StringIndexOutOfBoundsException
.

1.15.B.1
String
Develop code to call A object has index values
methods on string objects from 0 to one less than the length
and determine the result of of the string. Attempting to access
calling these methods. indices outside this range will result in a
StringIndexOutOfBoundsException
.

1.15.B
String
Develop code to call A object has index values
methods on string objects from 0 to one less than the length
and determine the result of of the string. Attempting to access
calling these methods. indices outside this range will result in a
StringIndexOutOfBoundsException
.

## 知识图谱关系

### 前驱知识

- `cs-ap-a-recursion`
- `cs-ap-a-array-algorithms`

## 课标摘要

1.15.B.2
String
Develop code to call A object has index values
methods on string objects from 0 to one less than the length
and determine the result of of the string. Attempting to access
calling thes...；1.15.B.1
String
Develop code to call A object has index values
methods on string objects from 0 to one less than the length
and determine the result of of the string. Attempting to access
calling thes...；1.15.B
String
Develop code to call A object has index values
methods on string objects from 0 to one less than the length
and determine the result of of the string. Attempting to access
calling these ...
