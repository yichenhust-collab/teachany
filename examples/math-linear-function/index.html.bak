<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>一次函数 · 初中数学 G8 · TeachAny v5.27</title>
  <!-- TeachAny 元信息 -->
  <meta name="teachany-node" content="linear-function">
  <meta name="teachany-subject" content="math">
  <meta name="teachany-domain" content="function">
  <meta name="teachany-grade" content="8">
  <meta name="teachany-prerequisites" content="proportional-function,coordinate-system">
  <meta name="teachany-difficulty" content="3">
  <meta name="teachany-version" content="3.0">
  <meta name="teachany-author" content="weponusa">
  <style>
    /* ═══ 初中模板配色 ═══ */
    :root {
      --bg: #f8fafc;
      --primary: #3b82f6;
      --secondary: #06b6d4;
      --accent: #f59e0b;
      --teal: #14b8a6;
      --card: #ffffff;
      --text: #1e293b;
      --text-secondary: #64748b;
      --border: #e2e8f0;
      --success: #22c55e;
      --danger: #ef4444;
      --purple: #8b5cf6;
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Microsoft YaHei', system-ui, sans-serif; background: var(--bg); color: var(--text); line-height: 1.8; }
    html { scroll-behavior: smooth; }

    /* ═══ 进度条 ═══ */
    .progress-bar { position: fixed; top: 0; left: 0; height: 3px; z-index: 200; background: var(--primary); transition: width 0.3s; }

    /* ═══ Sticky 导航栏 ═══ */
    .nav-bar {
      position: sticky; top: 0; z-index: 100;
      display: flex; align-items: center; gap: 8px;
      padding: 12px 24px; background: var(--card);
      box-shadow: 0 2px 8px rgba(0,0,0,0.06);
      overflow-x: auto; white-space: nowrap;
    }
    .nav-bar .brand { font-weight: 700; margin-right: 12px; font-size: 16px; }
    .nav-bar a {
      text-decoration: none; padding: 6px 14px; border-radius: 20px;
      font-size: 14px; color: var(--text); transition: all 0.2s;
    }
    .nav-bar a:hover, .nav-bar a.active { background: var(--primary); color: #fff; }

    /* ═══ Section 通用样式 ═══ */
    .section { max-width: 900px; margin: 40px auto; padding: 0 24px; }
    .section-title {
      font-size: 1.6rem; font-weight: 700; margin-bottom: 20px;
      padding-left: 16px; border-left: 4px solid var(--primary);
    }

    /* ═══ Hero 区 ═══ */
    .hero {
      text-align: center; padding: 60px 24px 40px;
      background: linear-gradient(135deg, var(--primary), var(--secondary));
      color: #fff; border-radius: 0 0 24px 24px;
    }
    .hero h1 { font-size: 2.2rem; margin-bottom: 12px; }
    .hero .subtitle { font-size: 1.1rem; opacity: 0.9; margin-bottom: 16px; }
    .hero .tags { display: flex; justify-content: center; gap: 8px; flex-wrap: wrap; }
    .hero .tag {
      background: rgba(255,255,255,0.2); padding: 4px 14px; border-radius: 20px; font-size: 13px;
    }

    /* ═══ 卡片 ═══ */
    .card {
      background: var(--card); border-radius: 14px; padding: 24px;
      margin-bottom: 16px; box-shadow: 0 2px 12px rgba(0,0,0,0.04);
      border-top: 3px solid var(--primary);
    }
    .card.blue { border-top-color: var(--primary); }
    .card.teal { border-top-color: var(--teal); }
    .card.amber { border-top-color: var(--accent); }
    .card.purple { border-top-color: var(--purple); }

    .card h3 { font-size: 1.15rem; margin-bottom: 12px; }
    .card p, .card li { font-size: 15px; color: var(--text); }
    .card ul { padding-left: 20px; }
    .card ul li { margin-bottom: 6px; }

    /* ═══ ABT 引入 ═══ */
    .abt-box {
      background: linear-gradient(135deg, #eff6ff, #ecfeff);
      border-radius: 14px; padding: 24px; margin-bottom: 16px;
      border-left: 4px solid var(--primary);
    }
    .abt-box .and { color: var(--teal); font-weight: 700; }
    .abt-box .but { color: var(--danger); font-weight: 700; }
    .abt-box .therefore { color: var(--primary); font-weight: 700; }

    /* ═══ 情境角色 ═══ */
    .role-badge {
      display: inline-block; background: var(--primary); color: #fff;
      padding: 4px 16px; border-radius: 20px; font-size: 13px; font-weight: 600; margin-bottom: 12px;
    }

    /* ═══ Canvas 画板 ═══ */
    .canvas-container {
      background: #fff; border: 1px solid var(--border); border-radius: 12px;
      padding: 16px; margin: 16px 0; text-align: center;
    }
    .canvas-container canvas { border-radius: 8px; max-width: 100%; }
    .canvas-controls {
      display: flex; align-items: center; gap: 16px; margin-top: 12px;
      flex-wrap: wrap; justify-content: center;
    }
    .canvas-controls label { font-size: 14px; font-weight: 600; }
    .canvas-controls input[type="range"] { width: 140px; accent-color: var(--primary); }
    .canvas-controls .val {
      font-family: 'Times New Roman', serif; font-size: 16px;
      font-weight: 700; color: var(--primary); min-width: 40px;
    }
    .canvas-caption { font-size: 14px; color: var(--text-secondary); margin-top: 8px; }

    /* ═══ 公式框 ═══ */
    .formula-box {
      background: linear-gradient(135deg, #fefce8, #fff7ed);
      border: 2px solid var(--accent); border-radius: 12px;
      padding: 20px; margin: 16px 0; text-align: center;
      font-family: 'Times New Roman', 'STIX Two Math', serif;
      font-size: 1.3rem; font-weight: 600; color: var(--text);
    }

    /* ═══ 推导步骤 ═══ */
    .derivation { margin: 16px 0; }
    .derivation .step {
      display: flex; align-items: flex-start; gap: 12px; margin-bottom: 12px;
    }
    .derivation .step-num {
      width: 28px; height: 28px; border-radius: 50%; background: var(--primary);
      color: #fff; display: flex; align-items: center; justify-content: center;
      font-size: 14px; font-weight: 700; flex-shrink: 0;
    }
    .derivation .step-text { font-size: 15px; padding-top: 3px; }

    /* ═══ 练习区 ═══ */
    .quiz-option {
      display: block; width: 100%; text-align: left; padding: 14px 18px;
      margin: 8px 0; border: 2px solid var(--border); border-radius: 10px;
      background: var(--card); cursor: pointer; font-size: 15px; transition: all 0.2s;
    }
    .quiz-option:hover { border-color: var(--primary); transform: translateX(3px); }
    .quiz-option.correct { border-color: var(--success); background: #f0fdf4; }
    .quiz-option.wrong { border-color: var(--danger); background: #fef2f2; }
    .quiz-option:disabled { cursor: default; transform: none; }
    .feedback { padding: 16px; border-radius: 10px; margin-top: 12px; display: none; font-size: 15px; }
    .feedback.show { display: block; }
    .feedback.correct { background: #f0fdf4; color: #166534; border: 1px solid #bbf7d0; }
    .feedback.wrong { background: #fef2f2; color: #991b1b; border: 1px solid #fecaca; }

    /* ═══ 学习记录单 ═══ */
    .record-sheet {
      background: #f0f9ff; border: 2px solid #bae6fd; border-radius: 12px;
      padding: 20px; margin: 16px 0;
    }
    .record-sheet h4 { color: var(--primary); margin-bottom: 12px; }
    .record-sheet textarea {
      width: 100%; border: 1px solid var(--border); border-radius: 8px;
      padding: 10px; font-size: 14px; font-family: inherit; resize: vertical;
      min-height: 60px;
    }
    .record-sheet .submit-btn {
      margin-top: 8px; padding: 8px 20px; background: var(--primary);
      color: #fff; border: none; border-radius: 8px; cursor: pointer; font-size: 14px;
    }
    .record-sheet .submit-btn:hover { background: #2563eb; }
    .record-sheet .reference { display: none; margin-top: 10px; padding: 12px; background: #ecfdf5; border-radius: 8px; font-size: 14px; color: #166534; }
    .record-sheet .reference.show { display: block; }

    /* ═══ 前后翻页 ═══ */
    .page-nav {
      display: flex; justify-content: space-between; align-items: center;
      max-width: 900px; margin: 30px auto; padding: 0 24px;
    }
    .page-nav button {
      padding: 10px 24px; border-radius: 10px; border: 2px solid var(--primary);
      background: transparent; color: var(--primary); font-size: 15px; cursor: pointer;
      transition: all 0.2s;
    }
    .page-nav button:hover { background: var(--primary); color: #fff; }
    .page-nav .current { font-size: 14px; color: var(--text-secondary); }

    /* ═══ 三段式作业标签 ═══ */
    .level-tag { display: inline-block; padding: 2px 10px; border-radius: 12px; font-size: 12px; font-weight: 600; margin-right: 6px; }
    .level-tag.l1 { background: #dbeafe; color: #1e40af; }
    .level-tag.l2 { background: #fef3c7; color: #92400e; }
    .level-tag.l3 { background: #fce7f3; color: #9d174d; }

    /* ═══ 知识图谱（三列布局） ═══ */
    #kg-svg .kg-node { cursor: default; transition: filter .2s; }
    #kg-svg .kg-node.has-cw { cursor: pointer; }
    #kg-svg .kg-node.has-cw:hover rect { filter: brightness(1.15); }
    #kg-svg .kg-node.no-cw rect { stroke-dasharray: 6 3; }
    #kg-svg .kg-edge { fill: none; stroke-width: 1.5; opacity: 0.55; }
    #kg-svg .kg-chain { fill: none; stroke-width: 1.2; opacity: 0.5; }

    /* ═══ 音频播放器 ═══ */
    .audio-bar {
      position: fixed; bottom: 0; left: 0; right: 0; z-index: 300;
      background: var(--card); border-top: 1px solid var(--border);
      box-shadow: 0 -4px 16px rgba(0,0,0,0.08);
      display: none; align-items: center; gap: 10px;
      padding: 10px 18px; height: 56px;
    }
    .audio-bar.active { display: flex; }
    .audio-bar .audio-track-title { font-size: 13px; font-weight: 600; min-width: 80px; max-width: 200px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
    .audio-bar .audio-ctrl-btn { background: none; border: none; font-size: 18px; cursor: pointer; color: var(--text); padding: 4px; flex-shrink: 0; }
    .audio-bar .progress-track { flex: 1; height: 4px; background: #e2e8f0; border-radius: 2px; position: relative; cursor: pointer; min-width: 60px; }
    .audio-bar .progress-fill { height: 100%; background: var(--primary); border-radius: 2px; transition: width 0.1s; }
    .audio-bar .time-display { font-size: 12px; min-width: 40px; text-align: center; }
    .audio-bar .speed-btn { background: var(--primary); color: #fff; border: none; border-radius: 12px; font-size: 12px; padding: 2px 8px; cursor: pointer; font-weight: 600; }
    .audio-bar .audio-subtitle { font-size: 12px; color: var(--text-secondary); max-width: 240px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
    @media (max-width: 600px) {
      .audio-bar { flex-wrap: wrap; height: auto; padding: 8px 12px; gap: 6px; }
      .audio-bar .audio-subtitle { max-width: 100%; order: 10; width: 100%; }
    }
    body.audio-playing { padding-bottom: 64px; }

    /* ═══ 响应式 ═══ */
    @media (max-width: 600px) {
      .hero h1 { font-size: 1.5rem; }
      .section { padding: 0 16px; margin: 24px auto; }
      .nav-bar { padding: 10px 12px; }
      .canvas-controls { gap: 8px; }
    }
  </style>

  <link rel="stylesheet" href="./ai-tutor.css">
</head>
<body>
  <!-- 进度条 -->
  <div class="progress-bar" id="progressBar" style="width: 0%"></div>

  <!-- Sticky 导航栏 -->
  <nav class="nav-bar" id="navbar">
    <span class="brand">🎓 TeachAny</span>
    <a href="#hero">首页</a>
    <a href="#objectives">目标</a>
    <a href="#pretest">前测</a>
    <a href="#module-1">模块1</a>
    <a href="#module-2">模块2</a>
    <a href="#module-3">模块3</a>
    <a href="#module-4">模块4</a>
    <a href="#synthesis">综合</a>
    <a href="#posttest">后测</a>
    <a href="#summary">小结</a>
    <a href="#knowledge-graph">图谱</a>
  </nav>

  <!-- ═══ Hero 区 ═══ -->
  <section class="hero" id="hero">
    <h1>📐 一次函数</h1>
    <p class="subtitle">用一条直线描述世界的变化规律</p>
    <div class="tags">
      <span class="tag">数学</span>
      <span class="tag">八年级</span>
      <span class="tag">新授课</span>
      <span class="tag">问题驱动</span>
    </div>
    <img src="assets/linear-hero.png" alt="一次函数直线图象" style="display:block;max-width:680px;margin:24px auto 0;border-radius:14px;box-shadow:0 4px 24px rgba(0,0,0,0.10)">
  </section>
<!-- ═══ 学习目标 ═══ -->
<section class="section" id="objectives">
    <h2 class="section-title">🎯 学习目标</h2>
    <div class="card blue">
      <ul>
        <li><strong>识别</strong>：能判断一个函数是否为一次函数，区分一次函数与正比例函数</li>
        <li><strong>画图</strong>：能用两点法画出一次函数的图像</li>
        <li><strong>理解</strong>：能解释 k 和 b 的几何意义，说明它们如何影响图像位置和走向</li>
        <li><strong>应用</strong>：能用待定系数法求一次函数解析式，建立简单实际问题的函数模型</li>
      </ul>
    </div>
  </section>

  <!-- ═══ 前测 ═══ -->
  <section class="section" id="pretest">
    <h2 class="section-title">📋 前置知识检测</h2>
    <div class="card teal">
      <h3>第 1 题：正比例函数回顾</h3>
      <p style="margin-bottom:12px;">正比例函数 y=kx（k≠0）的图像是什么形状？过哪个特殊点？</p>
      <button class="quiz-option" onclick="checkPre1(this,'A')">A. 直线，过原点 (0,0)</button>
      <button class="quiz-option" onclick="checkPre1(this,'B')">B. 抛物线，过原点 (0,0)</button>
      <button class="quiz-option" onclick="checkPre1(this,'C')">C. 直线，不过原点</button>
      <button class="quiz-option" onclick="checkPre1(this,'D')">D. 双曲线，过原点 (0,0)</button>
      <div class="feedback" id="pre1-fb"></div>
    </div>
    <div class="card teal">
      <h3>第 2 题：坐标系基础</h3>
      <p style="margin-bottom:12px;">点 (0, 3) 在坐标系的什么位置？</p>
      <button class="quiz-option" onclick="checkPre2(this,'A')">A. 在 y 轴正半轴上</button>
      <button class="quiz-option" onclick="checkPre2(this,'B')">B. 在 x 轴正半轴上</button>
      <button class="quiz-option" onclick="checkPre2(this,'C')">C. 在第一象限</button>
      <button class="quiz-option" onclick="checkPre2(this,'D')">D. 在第二象限</button>
      <div class="feedback" id="pre2-fb"></div>
    </div>
  </section>

  <!-- ═══ 模块 1：一次函数的定义 ═══ -->
  <section class="section" id="module-1">
    <h2 class="section-title">📖 模块 1：什么是一次函数</h2>

    <!-- ABT 引入 -->
    <div class="abt-box">
      <div class="role-badge">🧭 城市出行规划师</div>
      <p><span class="and">And</span> 你已经会用 y=kx 描述"单价×数量"这类正比例关系了。</p>
      <p><span class="but">But</span> 乘坐出租车时，费用不只是"里程×单价"——还有一笔"起步价"！手机话费也不只是"通话分钟×单价"——还有"月租费"！这些费用用 y=kx 描述不了。</p>
      <p><span class="therefore">Therefore</span> 我们需要一种新的函数：既能表示"按量计费"的部分，又能加上"固定费用"——这就是一次函数 y=kx+b。</p>
    </div>

    <!-- 核心讲解 -->
    <div class="card blue">
      <h3>📝 一次函数的定义</h3>
      <div class="formula-box">y = kx + b （k ≠ 0）</div>
      <ul>
        <li><strong>k</strong>：斜率，决定直线的"坡度"和方向</li>
        <li><strong>b</strong>：截距，直线与 y 轴交点的纵坐标</li>
        <li>k ≠ 0 是关键条件——若 k=0，则 y=b 变成常数函数，不是一次函数</li>
      </ul>
      <p style="margin-top:12px; color:var(--text-secondary); font-size:14px;">💡 <strong>记忆锚点</strong>：k 是坡度，b 是起点。就像爬山——k 决定坡有多陡，b 决定从哪个高度出发。正比例函数是"不收起步价"，一次函数是"有起步价"。</p>
    </div>

    <!-- 深层理解：比较 -->
    <div class="card teal">
      <h3>🔍 一次函数 vs 正比例函数</h3>
      <table style="width:100%; border-collapse:collapse; font-size:14px;">
        <tr style="background:#f0f9ff;">
          <th style="padding:10px; border:1px solid var(--border); text-align:left;">对比项</th>
          <th style="padding:10px; border:1px solid var(--border);">正比例函数 y=kx</th>
          <th style="padding:10px; border:1px solid var(--border);">一次函数 y=kx+b</th>
        </tr>
        <tr>
          <td style="padding:10px; border:1px solid var(--border);">条件</td>
          <td style="padding:10px; border:1px solid var(--border); text-align:center;">k≠0，b=0</td>
          <td style="padding:10px; border:1px solid var(--border); text-align:center;">k≠0，b 为任意实数</td>
        </tr>
        <tr>
          <td style="padding:10px; border:1px solid var(--border);">图像</td>
          <td style="padding:10px; border:1px solid var(--border); text-align:center;">过原点的直线</td>
          <td style="padding:10px; border:1px solid var(--border); text-align:center;">直线（不一定过原点）</td>
        </tr>
        <tr>
          <td style="padding:10px; border:1px solid var(--border);">关系</td>
          <td style="padding:10px; border:1px solid var(--border); text-align:center;" colspan="2">正比例函数是 b=0 时的一次函数（特殊情形）</td>
        </tr>
      </table>
    </div>

    <!-- 即刻练习 -->
    <div class="card amber">
      <h3>✏️ 立刻练习：判断一次函数</h3>
      <p style="margin-bottom:12px;">下面哪些是一次函数？</p>
      <button class="quiz-option" onclick="checkM1(this,'A')">A. y = 2x + 3</button>
      <button class="quiz-option" onclick="checkM1(this,'B')">B. y = x²</button>
      <button class="quiz-option" onclick="checkM1(this,'C')">C. y = 5/x</button>
      <button class="quiz-option" onclick="checkM1(this,'D')">D. y = 5</button>
      <button class="quiz-option" onclick="checkM1(this,'E')">E. y = -3x</button>
      <div class="feedback" id="m1-fb"></div>
    </div>
  </section>

  <div class="page-nav">
    <button onclick="scrollToSection('pretest')">← 前测</button>
    <span class="current">模块 1 / 4</span>
    <button onclick="scrollToSection('module-2')">模块 2 →</button>
  </div>

  <!-- ═══ 模块 2：一次函数的图像 ═══ -->
  <section class="section" id="module-2">
    <h2 class="section-title">📖 模块 2：一次函数的图像怎么画</h2>

    <div class="abt-box">
      <p><span class="and">And</span> 你知道正比例函数 y=kx 的图像是一条过原点的直线，只要再找一个点就能画出。</p>
      <p><span class="but">But</span> 一次函数 y=kx+b 不过原点了，怎么画？</p>
      <p><span class="therefore">Therefore</span> 用"两点法"：分别令 x=0 和 y=0，找到直线与两个坐标轴的交点，连起来就是。</p>
    </div>

    <!-- 核心讲解 -->
    <div class="card blue">
      <h3>📝 两点法画图步骤</h3>
      <div class="derivation">
        <div class="step">
          <div class="step-num">1</div>
          <div class="step-text">令 <strong>x = 0</strong>，得 y = b，画出点 <strong>(0, b)</strong> —— 与 y 轴的交点</div>
        </div>
        <div class="step">
          <div class="step-num">2</div>
          <div class="step-text">令 <strong>y = 0</strong>，得 0 = kx + b，解出 x = -b/k，画出点 <strong>(-b/k, 0)</strong> —— 与 x 轴的交点</div>
        </div>
        <div class="step">
          <div class="step-num">3</div>
          <div class="step-text">过这两个点画一条直线，即为 y=kx+b 的图像</div>
        </div>
      </div>
    </div>

    <!-- 交互式 Canvas 画板 -->
    <div class="card purple">
      <h3>🎨 交互画板：拖动滑块观察直线变化</h3>
      <div class="canvas-container">
        <canvas id="graphCanvas1" width="560" height="400"></canvas>
        <div class="canvas-controls">
          <label>k =</label>
          <input type="range" id="sliderK1" min="-5" max="5" step="0.5" value="2">
          <span class="val" id="valK1">2</span>
          <label>b =</label>
          <input type="range" id="sliderB1" min="-5" max="5" step="0.5" value="1">
          <span class="val" id="valB1">1</span>
        </div>
        <p class="canvas-caption">拖动滑块改变 k 和 b 的值，观察直线如何变化</p>
      </div>
    </div>

    <!-- 深层理解：看见 -->
    <div class="card teal">
      <h3>🔍 观察发现</h3>
      <div class="record-sheet">
        <h4>📝 学习记录单：图像观察</h4>
        <p>拖动上面的滑块，填写你的发现：</p>
        <p style="margin-top:8px;"><strong>我观察到：</strong></p>
        <textarea id="obs1" placeholder="当 k 变大时，直线变得…当 k 从正变负时，直线…"></textarea>
        <p style="margin-top:8px;"><strong>我的猜想：</strong></p>
        <textarea id="obs2" placeholder="k 控制直线的___，b 控制直线的___"></textarea>
        <button class="submit-btn" onclick="showRef('obs-ref')">提交并查看参考答案</button>
        <div class="reference" id="obs-ref">
          ✅ 参考答案：<br>
          k 控制直线的<strong>倾斜方向和程度</strong>：k&gt;0 从左到右上升，k&lt;0 从左到右下降；|k| 越大越陡。<br>
          b 控制直线与 y 轴的<strong>交点位置</strong>：b&gt;0 交 y 轴正半轴，b&lt;0 交 y 轴负半轴，b=0 过原点。
        </div>
      </div>
    </div>

    <!-- 练习 -->
    <div class="card amber">
      <h3>✏️ 立刻练习：画图</h3>
      <p style="margin-bottom:12px;">用两点法画 y = 2x - 4 的图像，应选取哪两个点？</p>
      <button class="quiz-option" onclick="checkM2(this,'A')">A. (0, -4) 和 (2, 0)</button>
      <button class="quiz-option" onclick="checkM2(this,'B')">B. (0, 4) 和 (-2, 0)</button>
      <button class="quiz-option" onclick="checkM2(this,'C')">C. (0, -4) 和 (-2, 0)</button>
      <button class="quiz-option" onclick="checkM2(this,'D')">D. (0, 4) 和 (2, 0)</button>
      <div class="feedback" id="m2-fb"></div>
    </div>
  </section>

  <div class="page-nav">
    <button onclick="scrollToSection('module-1')">← 模块 1</button>
    <span class="current">模块 2 / 4</span>
    <button onclick="scrollToSection('module-3')">模块 3 →</button>
  </div>

  <!-- ═══ 模块 3：k 和 b 的几何意义 ═══ -->
  <section class="section" id="module-3">
    <h2 class="section-title">📖 模块 3：k 和 b 的秘密</h2>

    <div class="abt-box">
      <p><span class="and">And</span> 你会画一次函数的图像了。</p>
      <p><span class="but">But</span> 光会画还不够——你能从图像"读出" k 和 b 是正还是负吗？给你 k 和 b 的符号，你能画出大致图像吗？</p>
      <p><span class="therefore">Therefore</span> 理解 k 和 b 的几何意义，就能在"式"和"图"之间自由切换。</p>
    </div>

    <!-- 核心讲解：k 的意义 -->
    <div class="card blue">
      <h3>📐 k 决定方向和坡度</h3>
      <ul>
        <li><strong>k &gt; 0</strong>：直线从左到右<strong>上升</strong>（上坡 🏔️）</li>
        <li><strong>k &lt; 0</strong>：直线从左到右<strong>下降</strong>（下坡 🎿）</li>
        <li><strong>|k| 越大</strong>：直线越<strong>陡</strong></li>
      </ul>
      <p style="margin-top:12px; color:var(--text-secondary); font-size:14px;">💡 记忆口诀：k 是坡度——正数上坡，负数下坡。手指从左往右划，k&gt;0 手指往上抬，k&lt;0 手指往下落。</p>
    </div>

    <!-- 核心讲解：b 的意义 -->
    <div class="card teal">
      <h3>📍 b 决定起点位置</h3>
      <ul>
        <li><strong>b &gt; 0</strong>：直线与 y 轴交于<strong>正半轴</strong>，即从 (0, b) 出发（b&gt;0 时 b 点在原点上方）</li>
        <li><strong>b &lt; 0</strong>：直线与 y 轴交于<strong>负半轴</strong>，即从 (0, b) 出发（b&lt;0 时 b 点在原点下方）</li>
        <li><strong>b = 0</strong>：直线过原点——就是正比例函数</li>
      </ul>
      <p style="margin-top:12px; color:var(--danger); font-size:14px;">⚠️ 常见错误：b 是"纵截距"，不是与 x 轴的交点！令 x=0 得 y=b，所以交点是 (0,b)。</p>
    </div>

    <!-- 交互：象限判断 -->
    <div class="card purple">
      <h3>🎨 交互：k 和 b 的符号与象限</h3>
      <div class="canvas-container">
        <canvas id="graphCanvas2" width="560" height="400"></canvas>
        <div class="canvas-controls">
          <label>k =</label>
          <input type="range" id="sliderK2" min="-5" max="5" step="0.5" value="-1">
          <span class="val" id="valK2">-1</span>
          <label>b =</label>
          <input type="range" id="sliderB2" min="-5" max="5" step="0.5" value="2">
          <span class="val" id="valB2">2</span>
        </div>
        <p class="canvas-caption">调整 k 和 b，观察直线经过哪些象限</p>
        <p id="quadrantInfo" style="margin-top:8px; font-weight:600; color:var(--primary); font-size:15px;"></p>
      </div>
    </div>

    <!-- 纠错练习 -->
    <div class="card amber">
      <h3>✏️ 纠错练习</h3>
      <p style="margin-bottom:12px;">小明说："y=-2x+3 的图像从左到右上升，与 y 轴交于 (3,0)。"他说对了吗？</p>
      <button class="quiz-option" onclick="checkM3(this,'A')">A. 全说对了</button>
      <button class="quiz-option" onclick="checkM3(this,'B')">B. 方向说错了：k=-2&lt;0 应该下降</button>
      <button class="quiz-option" onclick="checkM3(this,'C')">C. 交点说错了：应该交于 (0,3)</button>
      <button class="quiz-option" onclick="checkM3(this,'D')">D. 两个都说错了</button>
      <div class="feedback" id="m3-fb"></div>
    </div>
  </section>

  <div class="page-nav">
    <button onclick="scrollToSection('module-2')">← 模块 2</button>
    <span class="current">模块 3 / 4</span>
    <button onclick="scrollToSection('module-4')">模块 4 →</button>
  </div>

  <!-- ═══ 模块 4：求一次函数解析式 ═══ -->
  <section class="section" id="module-4">
    <h2 class="section-title">📖 模块 4：用待定系数法求解析式</h2>

    <div class="abt-box">
      <p><span class="and">And</span> 你已经理解了 k 和 b 怎么影响图像。</p>
      <p><span class="but">But</span> 如果只告诉你直线经过哪两个点，你能反推出 y=kx+b 的具体表达式吗？</p>
      <p><span class="therefore">Therefore</span> 学会"待定系数法"——设出 k 和 b，代入已知点，解方程组就能确定。</p>
    </div>

    <!-- 核心讲解 -->
    <div class="card blue">
      <h3>📝 待定系数法步骤</h3>
      <div class="derivation">
        <div class="step">
          <div class="step-num">1</div>
          <div class="step-text"><strong>设</strong>：设 y = kx + b</div>
        </div>
        <div class="step">
          <div class="step-num">2</div>
          <div class="step-text"><strong>代</strong>：把两个已知点的坐标分别代入，得到两个方程</div>
        </div>
        <div class="step">
          <div class="step-num">3</div>
          <div class="step-text"><strong>解</strong>：解这个二元一次方程组，求出 k 和 b</div>
        </div>
        <div class="step">
          <div class="step-num">4</div>
          <div class="step-text"><strong>写</strong>：写出函数解析式</div>
        </div>
      </div>
    </div>

    <!-- 例题演示 -->
    <div class="card teal">
      <h3>📝 例题：已知直线过 (1, 5) 和 (3, 9)</h3>
      <div class="derivation">
        <div class="step">
          <div class="step-num">1</div>
          <div class="step-text">设 y = kx + b</div>
        </div>
        <div class="step">
          <div class="step-num">2</div>
          <div class="step-text">代入 (1,5)：k + b = 5 …①<br>代入 (3,9)：3k + b = 9 …②</div>
        </div>
        <div class="step">
          <div class="step-num">3</div>
          <div class="step-text">② - ①：2k = 4，所以 k = 2<br>代入①：2 + b = 5，所以 b = 3</div>
        </div>
        <div class="step">
          <div class="step-num">4</div>
          <div class="step-text">所以 <strong>y = 2x + 3</strong></div>
        </div>
      </div>
      <p style="margin-top:8px; color:var(--danger); font-size:14px;">⚠️ 常见错误：两个方程相减时符号搞错。建议用"上减下"统一方法，仔细处理符号。</p>
    </div>

    <!-- 脚手架分级练习 -->
    <div class="card amber">
      <h3>✏️ 递进练习 ① <span class="level-tag l1">⭐ 全支架</span></h3>
      <p>直线过 (2, 7) 和 (4, 11)，填写过程：</p>
      <div style="margin-left:16px; font-size:14px;">
        <p>设 y = kx + b</p>
        <p>代入 (2,7)：2k + b = ___ …①</p>
        <p>代入 (4,11)：___k + b = ___ …②</p>
        <p>② - ①：___k = ___ → k = ___</p>
        <p>代入①：b = ___ → y = ___</p>
        <button style="margin-top:8px; padding:6px 16px; background:var(--teal); color:#fff; border:none; border-radius:8px; cursor:pointer; font-size:13px;" onclick="document.getElementById('scaffold1').style.display='block'">查看答案</button>
        <div id="scaffold1" style="display:none; margin-top:8px; padding:12px; background:#f0fdf4; border-radius:8px; font-size:14px; color:#166534;">
          2k + b = 7 …①；4k + b = 11 …②；②-①：2k = 4，k = 2；代入①：b = 3；y = 2x + 3
        </div>
      </div>
    </div>

    <div class="card teal">
      <h3>✏️ 递进练习 ② <span class="level-tag l2">⭐⭐ 半支架</span></h3>
      <p>直线过 (1, -1) 和 (3, 5)，自己写出完整步骤。</p>
      <div style="margin-left:16px;">
        <p style="font-size:14px;">提示：设 y = kx + b，分别代入两点，解方程组。</p>
        <button style="margin-top:8px; padding:6px 16px; background:var(--teal); color:#fff; border:none; border-radius:8px; cursor:pointer; font-size:13px;" onclick="document.getElementById('scaffold2').style.display='block'">查看答案</button>
        <div id="scaffold2" style="display:none; margin-top:8px; padding:12px; background:#f0fdf4; border-radius:8px; font-size:14px; color:#166534;">
          k + b = -1 …①；3k + b = 5 …②；②-①：2k = 6，k = 3；代入①：b = -4；y = 3x - 4
        </div>
      </div>
    </div>

    <div class="card purple">
      <h3>✏️ 递进练习 ③ <span class="level-tag l3">⭐⭐⭐ 独立完成</span></h3>
      <p>直线过 (-2, 4) 和 (1, -5)，求解析式。</p>
      <div style="margin-left:16px;">
        <button style="margin-top:8px; padding:6px 16px; background:var(--teal); color:#fff; border:none; border-radius:8px; cursor:pointer; font-size:13px;" onclick="document.getElementById('scaffold3').style.display='block'">查看答案</button>
        <div id="scaffold3" style="display:none; margin-top:8px; padding:12px; background:#f0fdf4; border-radius:8px; font-size:14px; color:#166534;">
          -2k + b = 4 …①；k + b = -5 …②；②-①：3k = -9，k = -3；代入②：b = -2；y = -3x - 2
        </div>
      </div>
    </div>

    <!-- 迁移：实际应用 -->
    <div class="card purple">
      <h3>🌍 迁移应用：出租车计费</h3>
      <p>某城市出租车起步价 10 元（3 公里内），超过 3 公里后每公里 2.5 元。</p>
      <p><strong>问题</strong>：写出车费 y（元）与里程 x（公里，x≥3）的函数关系式。</p>
      <p style="font-size:14px; color:var(--text-secondary);">提示：起步价相当于 b，每公里费用相当于 k。</p>
      <img src="assets/linear-scene.png" alt="城市街道中的一次函数" style="display:block;max-width:100%;margin:14px auto 0;border-radius:10px;box-shadow:0 2px 12px rgba(0,0,0,0.08)">
    </div>
    <div class="card purple">
      <h4>📝 学习记录单：建立函数模型</h4>
      <p>我的分析：</p>
      <textarea id="app1" placeholder="起步价 10 元相当于___，每公里 2.5 元相当于___，所以 y=___"></textarea>
      <button class="submit-btn" onclick="showRef('app-ref')">提交并查看参考答案</button>
      <div class="reference" id="app-ref">
        ✅ 参考答案：起步价 10 元相当于 b=10，每公里 2.5 元相当于 k=2.5。<br>
        所以 <strong>y = 2.5(x-3) + 10 = 2.5x + 2.5</strong>（前 3 公里已含在起步价中）
      </div>
    </div>
  </section>

  <div class="page-nav">
    <button onclick="scrollToSection('module-3')">← 模块 3</button>
    <span class="current">模块 4 / 4</span>
    <button onclick="scrollToSection('synthesis')">综合任务 →</button>
  </div>

  <!-- ═══ 综合任务 ═══ -->
  <section class="section" id="synthesis">
    <h2 class="section-title">🏆 综合任务</h2>
    <div class="card blue">
      <h3><span class="level-tag l1">⭐ 基础巩固</span>（必做）</h3>
      <p>1. 判断 y=-x+3 是否为一次函数。若是，指出 k 和 b 的值。</p>
      <p>2. 画出 y=-x+3 的图像（用两点法）。</p>
    </div>
    <div class="card amber">
      <h3><span class="level-tag l2">⭐⭐ 拓展应用</span>（选做）</h3>
      <p>一次函数 y=kx+b 的图像经过点 (0, -2) 和 (3, 4)：</p>
      <p>① 求函数解析式；② 判断点 (6, 10) 是否在该函数图像上。</p>
    </div>
    <div class="card purple">
      <h3><span class="level-tag l3">⭐⭐⭐ 创新挑战</span>（选做）</h3>
      <p>手机套餐 A：月租 20 元，通话 0.1 元/分钟；套餐 B：无月租，通话 0.3 元/分钟。</p>
      <p>① 分别写出两种套餐的月费与通话时间的关系式；</p>
      <p>② 通话多长时间时两种套餐费用相同？</p>
      <p>③ 什么情况下选 A 更划算？什么情况下选 B 更划算？</p>
    </div>
  </section>

  <!-- ═══ 后测 ═══ -->
  <section class="section" id="posttest">
    <h2 class="section-title">📝 后测</h2>
    <div class="card blue">
      <h3>第 1 题</h3>
      <p style="margin-bottom:12px;">一次函数 y=kx+b 中，k<0、b>0 时，图像经过哪些象限？</p>
      <button class="quiz-option" onclick="checkPost1(this,'A')">A. 一、二、四象限</button>
      <button class="quiz-option" onclick="checkPost1(this,'B')">B. 一、二、三象限</button>
      <button class="quiz-option" onclick="checkPost1(this,'C')">C. 一、三、四象限</button>
      <button class="quiz-option" onclick="checkPost1(this,'D')">D. 二、三、四象限</button>
      <div class="feedback" id="post1-fb"></div>
    </div>
    <div class="card teal">
      <h3>第 2 题</h3>
      <p style="margin-bottom:12px;">一次函数过点 (1,5) 和 (3,9)，求解析式。</p>
      <button class="quiz-option" onclick="checkPost2(this,'A')">A. y = 2x + 3</button>
      <button class="quiz-option" onclick="checkPost2(this,'B')">B. y = 3x + 2</button>
      <button class="quiz-option" onclick="checkPost2(this,'C')">C. y = 2x + 5</button>
      <button class="quiz-option" onclick="checkPost2(this,'D')">D. y = 4x + 1</button>
      <div class="feedback" id="post2-fb"></div>
    </div>
    <div class="card amber">
      <h3>第 3 题</h3>
      <p style="margin-bottom:12px;">小明说 y=2x²+1 是一次函数，对吗？</p>
      <button class="quiz-option" onclick="checkPost3(this,'A')">A. 不对，x 的最高次数是 2，是二次函数</button>
      <button class="quiz-option" onclick="checkPost3(this,'B')">B. 对的，因为有 x 和常数</button>
      <button class="quiz-option" onclick="checkPost3(this,'C')">C. 不对，因为没有 b</button>
      <button class="quiz-option" onclick="checkPost3(this,'D')">D. 对的，因为系数是 2</button>
      <div class="feedback" id="post3-fb"></div>
    </div>
  </section>

  <!-- ═══ 小结 ═══ -->
  <section class="section" id="summary">
    <h2 class="section-title">📌 课堂小结</h2>
    <div class="card blue">
      <h3>核心知识回顾</h3>
      <ul>
        <li><strong>定义</strong>：y = kx + b（k ≠ 0），k 决定方向和坡度，b 决定与 y 轴的交点</li>
        <li><strong>图像</strong>：一条直线，用两点法画出——取 (0, b) 和 (-b/k, 0)</li>
        <li><strong>k 的意义</strong>：k &gt; 0 上升，k &lt; 0 下降；|k| 越大越陡</li>
        <li><strong>b 的意义</strong>：b 是纵截距，令 x=0 得 y=b，交点 (0, b)</li>
        <li><strong>待定系数法</strong>：设→代→解→写，四步求解析式</li>
        <li><strong>记忆锚点</strong>：k 是坡度，b 是起点；正比例不收起步价，一次函数收起步价</li>
      </ul>
    </div>
  </section>

  <!-- ═══ 知识图谱 ═══ -->
  <section class="section" id="knowledge-graph">
    <h2 class="section-title">🗺️ 知识图谱</h2>
    <p style="color:var(--text-secondary); margin-bottom:16px;">三列视图：前序知识 → 核心子知识点 → 后续知识。实线节点可点击跳转，虚线表示暂无课件。</p>
    <div id="kg-container" style="width:100%; min-height:500px; border:1px solid var(--border); border-radius:12px; overflow:hidden; position:relative;">
      <svg id="kg-svg" width="100%" height="100%" style="min-height:500px;"></svg>
    </div>
  </section>

  <script>
    // ═══ 音频播放列表 ═══
    const audioPlaylist = [
      { id: "seg01", sectionId: "module-1", title: "模块1：什么是一次函数", src: "./tts/seg01_zh.mp3", subtitle: "y=kx+b的定义，k是坡度b是起点…" },
      { id: "seg02", sectionId: "module-2", title: "模块2：图像怎么画", src: "./tts/seg02_zh.mp3", subtitle: "两点法：令x=0和y=0找交点…" },
      { id: "seg03", sectionId: "module-3", title: "模块3：k和b的秘密", src: "./tts/seg03_zh.mp3", subtitle: "k决定方向，b决定起点位置…" },
      { id: "seg04", sectionId: "module-4", title: "模块4：待定系数法", src: "./tts/seg04_zh.mp3", subtitle: "设→代→解→写，四步求解析式…" }
    ];

    // ═══ 知识图谱数据（三列布局：前序 | 核心子知识点链 | 后续） ═══
    const knowledgeGraphData = {
      currentNode: "linear-function",
      currentLabel: "一次函数 y=kx+b",

      // 核心子知识点（对应课件教学模块）
      coreSubTopics: [
        { id: "sub-definition", label: "一次函数的定义 y=kx+b" },
        { id: "sub-graph",      label: "两点法画图像" },
        { id: "sub-kb",         label: "k和b的几何意义" },
        { id: "sub-method",     label: "待定系数法" },
        { id: "sub-equation",   label: "一次函数与方程组" },
        { id: "sub-application",label: "实际应用建模" }
      ],

      // 前序知识（connectsTo 精准指向核心子知识点）
      prerequisites: [
        { id: "variable-and-function", label: "变量与函数",      hasCourseware: true, url: "../math-variable-function/index.html", connectsTo: ["sub-definition"] },
        { id: "coordinate-system",     label: "平面直角坐标系",  hasCourseware: false, url: "", connectsTo: ["sub-definition", "sub-graph"] },
        { id: "proportional-function", label: "正比例函数",      hasCourseware: false, url: "", connectsTo: ["sub-definition", "sub-kb"] },
        { id: "linear-equation",       label: "一元一次方程",    hasCourseware: false, url: "", connectsTo: ["sub-method", "sub-equation"] }
      ],

      // 后续知识（connectsFrom 精准指向核心子知识点）
      nextTopics: [
        { id: "linear-equation-system-graph", label: "一次函数与方程图解", hasCourseware: false, url: "", connectsFrom: ["sub-equation"] },
        { id: "inverse-proportional-function", label: "反比例函数",       hasCourseware: false, url: "", connectsFrom: ["sub-kb"] },
        { id: "quadratic-function",            label: "二次函数",         hasCourseware: true,  url: "../math-quadratic-function/index.html", connectsFrom: ["sub-application"] }
      ]
    };

    // ═══ 导航高亮 + 滚动进度 ═══
    const sections = document.querySelectorAll('.section, .hero');
    const navLinks = document.querySelectorAll('.nav-bar a');
    const progressBar = document.getElementById('progressBar');

    window.addEventListener('scroll', () => {
      const scrollTop = window.scrollY;
      const docHeight = document.documentElement.scrollHeight - window.innerHeight;
      progressBar.style.width = (scrollTop / docHeight * 100) + '%';

      let current = '';
      sections.forEach(sec => {
        if (sec.offsetTop - 120 <= scrollTop) current = sec.id;
      });
      navLinks.forEach(link => {
        link.classList.toggle('active', link.getAttribute('href') === '#' + current);
      });
    });

    function scrollToSection(id) {
      document.getElementById(id)?.scrollIntoView({ behavior: 'smooth', block: 'start' });
    }
    navLinks.forEach(link => {
      link.addEventListener('click', e => {
        e.preventDefault();
        const id = link.getAttribute('href').slice(1);
        scrollToSection(id);
      });
    });

    // ═══ Canvas 绘图函数 ═══
    function drawGraph(canvasId, k, b, showQuadrants) {
      const canvas = document.getElementById(canvasId);
      if (!canvas) return;
      const ctx = canvas.getContext('2d');
      const W = canvas.width, H = canvas.height;
      const cx = W / 2, cy = H / 2;
      const scale = 40; // pixels per unit

      ctx.clearRect(0, 0, W, H);

      // Grid
      ctx.strokeStyle = '#e2e8f0';
      ctx.lineWidth = 0.5;
      for (let i = -10; i <= 10; i++) {
        ctx.beginPath(); ctx.moveTo(cx + i * scale, 0); ctx.lineTo(cx + i * scale, H); ctx.stroke();
        ctx.beginPath(); ctx.moveTo(0, cy - i * scale); ctx.lineTo(W, cy - i * scale); ctx.stroke();
      }

      // Axes
      ctx.strokeStyle = '#64748b';
      ctx.lineWidth = 1.5;
      ctx.beginPath(); ctx.moveTo(0, cy); ctx.lineTo(W, cy); ctx.stroke();
      ctx.beginPath(); ctx.moveTo(cx, 0); ctx.lineTo(cx, H); ctx.stroke();

      // Axis labels
      ctx.fillStyle = '#64748b';
      ctx.font = '12px system-ui';
      ctx.fillText('x', W - 15, cy - 8);
      ctx.fillText('y', cx + 8, 15);
      ctx.fillText('O', cx - 14, cy + 15);

      // Tick marks
      ctx.font = '11px system-ui';
      ctx.textAlign = 'center';
      for (let i = -6; i <= 6; i++) {
        if (i === 0) continue;
        ctx.fillText(i, cx + i * scale, cy + 16);
        ctx.textAlign = 'right';
        ctx.fillText(i, cx - 8, cy - i * scale + 4);
        ctx.textAlign = 'center';
      }

      // Function line
      if (k !== 0) {
        ctx.strokeStyle = '#3b82f6';
        ctx.lineWidth = 2.5;
        ctx.beginPath();
        const x1 = -10, y1 = k * x1 + b;
        const x2 = 10, y2 = k * x2 + b;
        const px1 = cx + x1 * scale, py1 = cy - y1 * scale;
        const px2 = cx + x2 * scale, py2 = cy - y2 * scale;
        ctx.moveTo(px1, py1);
        ctx.lineTo(px2, py2);
        ctx.stroke();

        // Mark (0,b) point
        ctx.fillStyle = '#ef4444';
        ctx.beginPath();
        ctx.arc(cx, cy - b * scale, 5, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = '#ef4444';
        ctx.font = '12px system-ui';
        ctx.textAlign = 'left';
        ctx.fillText(`(0, ${b})`, cx + 8, cy - b * scale - 8);

        // Mark (-b/k, 0) point
        const xInt = -b / k;
        if (Math.abs(xInt) <= 10) {
          ctx.fillStyle = '#22c55e';
          ctx.beginPath();
          ctx.arc(cx + xInt * scale, cy, 5, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = '#22c55e';
          ctx.font = '12px system-ui';
          ctx.fillText(`(${xInt.toFixed(1)}, 0)`, cx + xInt * scale + 8, cy + 20);
        }

        // Label the equation
        ctx.fillStyle = '#3b82f6';
        ctx.font = 'bold 14px system-ui';
        ctx.textAlign = 'left';
        const eqText = `y = ${k === 1 ? '' : k === -1 ? '-' : k}x${b === 0 ? '' : b > 0 ? ' + ' + b : ' - ' + Math.abs(b)}`;
        ctx.fillText(eqText, 20, 30);
      }

      // Quadrant info
      if (showQuadrants && k !== 0) {
        updateQuadrantInfo(k, b);
      }
    }

    function updateQuadrantInfo(k, b) {
      const el = document.getElementById('quadrantInfo');
      if (!el) return;
      let quads = [];
      if (k > 0 && b > 0) quads = ['一', '二', '三'];
      if (k > 0 && b < 0) quads = ['一', '三', '四'];
      if (k > 0 && b === 0) quads = ['一', '三'];
      if (k < 0 && b > 0) quads = ['一', '二', '四'];
      if (k < 0 && b < 0) quads = ['二', '三', '四'];
      if (k < 0 && b === 0) quads = ['二', '四'];
      el.textContent = `k=${k}, b=${b} → 经过第${quads.join('、')}象限`;
    }

    // Canvas 1 controls
    const sliderK1 = document.getElementById('sliderK1');
    const sliderB1 = document.getElementById('sliderB1');
    function updateGraph1() {
      const k = parseFloat(sliderK1.value);
      const b = parseFloat(sliderB1.value);
      document.getElementById('valK1').textContent = k;
      document.getElementById('valB1').textContent = b;
      drawGraph('graphCanvas1', k, b, false);
    }
    sliderK1.addEventListener('input', updateGraph1);
    sliderB1.addEventListener('input', updateGraph1);
    updateGraph1();

    // Canvas 2 controls
    const sliderK2 = document.getElementById('sliderK2');
    const sliderB2 = document.getElementById('sliderB2');
    function updateGraph2() {
      const k = parseFloat(sliderK2.value);
      const b = parseFloat(sliderB2.value);
      document.getElementById('valK2').textContent = k;
      document.getElementById('valB2').textContent = b;
      drawGraph('graphCanvas2', k, b, true);
    }
    sliderK2.addEventListener('input', updateGraph2);
    sliderB2.addEventListener('input', updateGraph2);
    updateGraph2();

    // ═══ 答题逻辑 ═══
    function disableSiblings(btn) {
      btn.parentElement.querySelectorAll('.quiz-option').forEach(b => b.disabled = true);
    }

    // 前测
    function checkPre1(btn, ans) {
      disableSiblings(btn);
      const fb = document.getElementById('pre1-fb');
      if (ans === 'A') { btn.classList.add('correct'); fb.className = 'feedback show correct'; fb.textContent = '✅ 正确！正比例函数 y=kx 的图像是过原点的直线。'; }
      else { btn.classList.add('wrong'); fb.className = 'feedback show wrong'; fb.textContent = '❌ 再想想。正比例函数 y=kx（k≠0）的图像是一条直线，而且一定过原点 (0,0)。'; }
    }
    function checkPre2(btn, ans) {
      disableSiblings(btn);
      const fb = document.getElementById('pre2-fb');
      if (ans === 'A') { btn.classList.add('correct'); fb.className = 'feedback show correct'; fb.textContent = '✅ 正确！点 (0, 3) 的 x=0，在 y 轴上；y=3>0，在正半轴。'; }
      else { btn.classList.add('wrong'); fb.className = 'feedback show wrong'; fb.textContent = '❌ 点 (0, 3) 中 x=0 说明在 y 轴上，y=3>0 说明在正半轴。'; }
    }

    // 模块1
    function checkM1(btn, ans) {
      disableSiblings(btn);
      const fb = document.getElementById('m1-fb');
      if (ans === 'A') {
        btn.classList.add('correct');
        fb.className = 'feedback show correct';
        fb.innerHTML = '✅ y=2x+3 中 k=2≠0，是一次函数。<br>⚠️ 注意：E. y=-3x 也是一次函数（b=0，是特殊的一次函数——正比例函数）。B 是二次函数，C 是反比例函数，D 是常数函数（k=0）。';
      } else {
        btn.classList.add('wrong');
        const explanations = { B: 'y=x² 中 x 的指数是 2，是二次函数', C: 'y=5/x 中 x 在分母上，是反比例函数', D: 'y=5 相当于 y=0·x+5，k=0 不满足 k≠0，是常数函数', E: 'y=-3x 其实是一次函数（b=0 的特殊情形），请重新考虑' };
        fb.className = 'feedback show wrong';
        fb.textContent = '❌ ' + (explanations[ans] || '再想想');
      }
    }

    // 模块2
    function checkM2(btn, ans) {
      disableSiblings(btn);
      const fb = document.getElementById('m2-fb');
      if (ans === 'A') { btn.classList.add('correct'); fb.className = 'feedback show correct'; fb.innerHTML = '✅ 正确！令 x=0 → y=-4，得 (0,-4)；令 y=0 → 2x-4=0 → x=2，得 (2,0)。'; }
      else { btn.classList.add('wrong'); fb.className = 'feedback show wrong'; fb.innerHTML = '❌ 步骤：令 x=0 → y=2×0-4=-4，点(0,-4)；令 y=0 → 2x-4=0 → x=2，点(2,0)。'; }
    }

    // 模块3
    function checkM3(btn, ans) {
      disableSiblings(btn);
      const fb = document.getElementById('m3-fb');
      if (ans === 'D') { btn.classList.add('correct'); fb.className = 'feedback show correct'; fb.innerHTML = '✅ 两个都说错了！<br>① k=-2<0，直线从左到右应该下降，不是上升。<br>② b=3 是纵截距，令 x=0 得 y=3，与 y 轴交点为 (0,3)，不是 (3,0)。'; }
      else { btn.classList.add('wrong'); fb.className = 'feedback show wrong'; fb.innerHTML = '❌ 两个都说错了：<br>① k=-2<0 应该下降，小明说"上升"是错的。<br>② b=3 是纵截距，交点是 (0,3)，不是 (3,0)。小明把 y 轴交点和 x 轴交点搞混了。'; }
    }

    // 后测
    function checkPost1(btn, ans) {
      disableSiblings(btn);
      const fb = document.getElementById('post1-fb');
      if (ans === 'A') { btn.classList.add('correct'); fb.className = 'feedback show correct'; fb.textContent = '✅ k<0 下降，b>0 从正 y 轴出发，经过一、二、四象限。'; }
      else { btn.classList.add('wrong'); fb.className = 'feedback show wrong'; fb.textContent = '❌ 分两步：k<0 直线下降（从左上到右下），b>0 从 y 轴正半轴出发，所以经过一、二、四象限。'; }
    }
    function checkPost2(btn, ans) {
      disableSiblings(btn);
      const fb = document.getElementById('post2-fb');
      if (ans === 'A') { btn.classList.add('correct'); fb.className = 'feedback show correct'; fb.textContent = '✅ 代入两点：k+b=5, 3k+b=9，解得 k=2, b=3，y=2x+3。'; }
      else { btn.classList.add('wrong'); fb.className = 'feedback show wrong'; fb.textContent = '❌ 设 y=kx+b，代入(1,5)和(3,9)得 k+b=5 和 3k+b=9，相减得 2k=4，k=2，b=3。'; }
    }
    function checkPost3(btn, ans) {
      disableSiblings(btn);
      const fb = document.getElementById('post3-fb');
      if (ans === 'A') { btn.classList.add('correct'); fb.className = 'feedback show correct'; fb.textContent = '✅ 一次函数要求 x 的最高次幂为 1，y=2x²+1 中 x 的次数是 2，是二次函数。'; }
      else { btn.classList.add('wrong'); fb.className = 'feedback show wrong'; fb.textContent = '❌ 判断函数类型看 x 的最高次幂：y=2x²+1 中 x² 的指数是 2，所以是二次函数，不是一次函数。'; }
    }

    // ═══ 学习记录单 ═══
    function showRef(id) {
      document.getElementById(id).classList.add('show');
    }

    // ═══ 知识图谱渲染（三列布局：前序 | 核心子知识点链 | 后续） ═══
    (function renderKnowledgeGraph() {
      if (typeof knowledgeGraphData === 'undefined') return;
      const svg = document.getElementById('kg-svg');
      if (!svg) return;
      const d = knowledgeGraphData;
      const NS = 'http://www.w3.org/2000/svg';
      const el = (tag) => document.createElementNS(NS, tag);

      const C = {
        pre: '#06b6d4', core: '#f59e0b', sub: '#3b82f6', next: '#10b981',
        preBg: 'rgba(6,182,212,0.12)', coreBg: 'rgba(245,158,11,0.18)',
        subBg: 'rgba(59,130,246,0.12)', nextBg: 'rgba(16,185,129,0.12)',
        noCw: '#94a3b8', noCwBg: 'rgba(148,163,184,0.08)'
      };

      const preNodes = d.prerequisites || [];
      const subNodes = d.coreSubTopics || [];
      const nextNodes = d.nextTopics || [];
      const maxRows = Math.max(preNodes.length, subNodes.length + 1, nextNodes.length);
      const ROW_H = 70, PAD_TOP = 60, NODE_H = 44, NODE_RX = 10;
      const W = 1100, H = Math.max(500, PAD_TOP + maxRows * ROW_H + 40);
      const COL = { pre: 120, core: 550, next: 970 };
      const NW = { pre: 190, core: 270, next: 220 };

      svg.setAttribute('viewBox', `0 0 ${W} ${H}`);
      svg.innerHTML = '';

      // defs
      const defs = el('defs');
      ['pre','core','sub','next'].forEach(k => {
        const marker = el('marker');
        marker.setAttribute('id', `kg-arr-${k}`);
        marker.setAttribute('viewBox', '0 0 10 6');
        marker.setAttribute('refX', '10'); marker.setAttribute('refY', '3');
        marker.setAttribute('markerWidth', '8'); marker.setAttribute('markerHeight', '6');
        marker.setAttribute('orient', 'auto');
        const p = el('path'); p.setAttribute('d', 'M0,0 L10,3 L0,6Z'); p.setAttribute('fill', C[k]);
        marker.appendChild(p); defs.appendChild(marker);
      });
      const filter = el('filter'); filter.setAttribute('id', 'kg-glow');
      const blur = el('feGaussianBlur'); blur.setAttribute('stdDeviation', '3'); blur.setAttribute('result', 'blur');
      const merge = el('feMerge');
      const mn1 = el('feMergeNode'); mn1.setAttribute('in', 'blur');
      const mn2 = el('feMergeNode'); mn2.setAttribute('in', 'SourceGraphic');
      merge.appendChild(mn1); merge.appendChild(mn2);
      filter.appendChild(blur); filter.appendChild(merge);
      defs.appendChild(filter);
      const noCwMarker = el('marker');
      noCwMarker.setAttribute('id', 'kg-arr-nocw');
      noCwMarker.setAttribute('viewBox', '0 0 10 6');
      noCwMarker.setAttribute('refX', '10'); noCwMarker.setAttribute('refY', '3');
      noCwMarker.setAttribute('markerWidth', '8'); noCwMarker.setAttribute('markerHeight', '6');
      noCwMarker.setAttribute('orient', 'auto');
      const np = el('path'); np.setAttribute('d', 'M0,0 L10,3 L0,6Z'); np.setAttribute('fill', C.noCw);
      noCwMarker.appendChild(np); defs.appendChild(noCwMarker);
      svg.appendChild(defs);

      // 列标题
      function addTitle(x, y, text, color) {
        const t = el('text');
        t.setAttribute('x', x); t.setAttribute('y', y);
        t.setAttribute('fill', color); t.setAttribute('font-size', '14');
        t.setAttribute('text-anchor', 'middle'); t.setAttribute('font-weight', '600');
        t.textContent = text; svg.appendChild(t);
      }
      addTitle(COL.pre, 30, '前序知识', '#64748b');
      addTitle(COL.core, 30, '核心知识', C.core);
      addTitle(COL.next, 30, '后续知识', '#64748b');

      // 绘制节点
      function drawNode(cx, cy, w, h, label, opts) {
        const { fill, stroke, strokeW, fontSize, fontWeight, fontColor, rx, glow, dash, clickUrl } = Object.assign(
          { fill: '#fff', stroke: '#ccc', strokeW: 1.5, fontSize: 14, fontWeight: '600', fontColor: '#333', rx: NODE_RX, glow: false, dash: false, clickUrl: '' }, opts);
        const g = el('g');
        g.setAttribute('class', 'kg-node' + (clickUrl ? ' has-cw' : (dash ? ' no-cw' : '')));
        const rect = el('rect');
        rect.setAttribute('x', cx - w/2); rect.setAttribute('y', cy - h/2);
        rect.setAttribute('width', w); rect.setAttribute('height', h);
        rect.setAttribute('rx', rx); rect.setAttribute('fill', fill);
        rect.setAttribute('stroke', stroke); rect.setAttribute('stroke-width', strokeW);
        if (dash) rect.setAttribute('stroke-dasharray', '6 3');
        if (glow) rect.setAttribute('filter', 'url(#kg-glow)');
        g.appendChild(rect);
        const txt = el('text');
        txt.setAttribute('x', cx); txt.setAttribute('y', cy + 5);
        txt.setAttribute('fill', fontColor); txt.setAttribute('font-size', fontSize);
        txt.setAttribute('text-anchor', 'middle'); txt.setAttribute('font-weight', fontWeight);
        txt.textContent = label.length > 14 ? label.slice(0, 14) + '…' : label;
        g.appendChild(txt);
        if (clickUrl) {
          g.style.cursor = 'pointer';
          g.addEventListener('click', () => window.open(clickUrl, '_blank'));
        }
        svg.appendChild(g);
        return { cx, cy, left: cx - w/2, right: cx + w/2, top: cy - h/2, bottom: cy + h/2 };
      }

      // 贝塞尔曲线
      function drawCurve(x1, y1, x2, y2, color, markerKey) {
        const cpX = (x1 + x2) / 2;
        const path = el('path');
        path.setAttribute('d', `M${x1},${y1} C${cpX},${y1} ${cpX},${y2} ${x2},${y2}`);
        path.setAttribute('fill', 'none'); path.setAttribute('stroke', color);
        path.setAttribute('stroke-width', '1.5'); path.setAttribute('opacity', '0.55');
        path.setAttribute('class', 'kg-edge');
        path.setAttribute('marker-end', `url(#kg-arr-${markerKey})`);
        svg.appendChild(path);
      }

      // 前序节点
      const prePos = {};
      preNodes.forEach((n, i) => {
        const cy = PAD_TOP + i * ROW_H + NODE_H/2;
        const hasCw = n.hasCourseware && n.url;
        const isDash = !n.hasCourseware;
        prePos[n.id] = drawNode(COL.pre, cy, NW.pre, NODE_H, n.label, {
          fill: isDash ? C.noCwBg : C.preBg, stroke: isDash ? C.noCw : C.pre,
          fontColor: isDash ? C.noCw : C.pre, dash: isDash, clickUrl: hasCw ? n.url : ''
        });
      });

      // 核心主节点
      const coreMainY = PAD_TOP + NODE_H/2;
      const coreMain = drawNode(COL.core, coreMainY, NW.core + 10, NODE_H + 8, d.currentLabel || '当前课件', {
        fill: C.coreBg, stroke: C.core, strokeW: 2.5,
        fontSize: 17, fontWeight: '700', fontColor: C.core, rx: 12, glow: true
      });

      // 核心子节点链
      const subPos = {};
      subNodes.forEach((n, i) => {
        const cy = PAD_TOP + (i + 1) * ROW_H + NODE_H/2;
        subPos[n.id] = drawNode(COL.core, cy, NW.core, NODE_H - 2, n.label, {
          fill: C.subBg, stroke: C.sub, fontColor: C.sub
        });
      });

      // 核心内部链式连线
      if (subNodes.length > 0) {
        const firstSub = subPos[subNodes[0].id];
        const chainLine = el('path');
        chainLine.setAttribute('d', `M${COL.core},${coreMain.bottom} L${COL.core},${firstSub.top}`);
        chainLine.setAttribute('fill', 'none'); chainLine.setAttribute('stroke', C.core);
        chainLine.setAttribute('stroke-width', '1.5'); chainLine.setAttribute('opacity', '0.6');
        chainLine.setAttribute('class', 'kg-chain');
        chainLine.setAttribute('marker-end', `url(#kg-arr-core)`);
        svg.appendChild(chainLine);
      }
      for (let i = 0; i < subNodes.length - 1; i++) {
        const from = subPos[subNodes[i].id], to = subPos[subNodes[i+1].id];
        const line = el('path');
        line.setAttribute('d', `M${COL.core},${from.bottom} L${COL.core},${to.top}`);
        line.setAttribute('fill', 'none'); line.setAttribute('stroke', C.sub);
        line.setAttribute('stroke-width', '1.2'); line.setAttribute('opacity', '0.5');
        line.setAttribute('class', 'kg-chain');
        line.setAttribute('marker-end', `url(#kg-arr-sub)`);
        svg.appendChild(line);
      }

      // 后续节点
      const nextPos = {};
      nextNodes.forEach((n, i) => {
        const cy = PAD_TOP + i * ROW_H + NODE_H/2;
        const hasCw = n.hasCourseware && n.url;
        const isDash = !n.hasCourseware;
        nextPos[n.id] = drawNode(COL.next, cy, NW.next, NODE_H, n.label, {
          fill: isDash ? C.noCwBg : C.nextBg, stroke: isDash ? C.noCw : C.next,
          fontColor: isDash ? C.noCw : C.next, dash: isDash, clickUrl: hasCw ? n.url : ''
        });
      });

      // 前序 → 核心 精准连线
      preNodes.forEach(n => {
        const from = prePos[n.id];
        if (!from) return;
        const targets = n.connectsTo || [];
        const isDash = !n.hasCourseware;
        const edgeColor = isDash ? C.noCw : C.pre;
        const markerKey = isDash ? 'nocw' : 'pre';
        if (targets.length === 0) {
          drawCurve(from.right, from.cy, coreMain.left, coreMain.cy, edgeColor, markerKey);
        } else {
          targets.forEach(tid => {
            const to = subPos[tid] || coreMain;
            drawCurve(from.right, from.cy, to.left, to.cy, edgeColor, markerKey);
          });
        }
      });

      // 核心 → 后续 精准连线
      nextNodes.forEach(n => {
        const to = nextPos[n.id];
        if (!to) return;
        const sources = n.connectsFrom || [];
        const isDash = !n.hasCourseware;
        const edgeColor = isDash ? C.noCw : C.next;
        const markerKey = isDash ? 'nocw' : 'next';
        if (sources.length === 0) {
          drawCurve(coreMain.right, coreMain.cy, to.left, to.cy, edgeColor, markerKey);
        } else {
          sources.forEach(sid => {
            const from = subPos[sid] || coreMain;
            drawCurve(from.right, from.cy, to.left, to.cy, edgeColor, markerKey);
          });
        }
      });
    })();

    // ═══ 音频播放器引擎 ═══
    (function initAudioPlayer() {
      if (typeof audioPlaylist === 'undefined' || !audioPlaylist.length) return;
      const bar = document.createElement('div');
      bar.className = 'audio-bar';
      bar.innerHTML = `
        <span class="audio-track-title"></span>
        <button class="audio-ctrl-btn play-btn">▶</button>
        <div class="progress-track"><div class="progress-fill" style="width:0%"></div></div>
        <span class="time-display">0:00</span>
        <button class="speed-btn">1x</button>
        <span class="audio-subtitle"></span>
      `;
      document.body.appendChild(bar);
      const audio = new Audio();
      let currentIdx = -1;
      const speeds = [0.5, 1, 1.25, 1.5, 2];
      let speedIdx = 1;
      function playTrack(i, autoplay) {
        if (i < 0 || i >= audioPlaylist.length) return;
        currentIdx = i;
        audio.src = audioPlaylist[i].src;
        if (autoplay !== false) audio.play();
        bar.classList.add('active');
        document.body.classList.add('audio-playing');
        bar.querySelector('.audio-track-title').textContent = audioPlaylist[i].title || '';
        bar.querySelector('.play-btn').textContent = autoplay !== false ? '⏸' : '▶';
        bar.querySelector('.audio-subtitle').textContent = audioPlaylist[i].subtitle || '';
      }
      const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
          if (!entry.isIntersecting) return;
          const secId = entry.target.id;
          const idx = audioPlaylist.findIndex(item => item.sectionId === secId);
          if (idx !== -1 && idx !== currentIdx) playTrack(idx);
        });
      }, { threshold: 0.4 });
      audioPlaylist.forEach(item => {
        const sec = document.getElementById(item.sectionId);
        if (sec) observer.observe(sec);
      });
      bar.querySelector('.play-btn').addEventListener('click', () => {
        if (audio.paused) { if (currentIdx < 0) playTrack(0); else audio.play(); bar.querySelector('.play-btn').textContent = '⏸'; }
        else { audio.pause(); bar.querySelector('.play-btn').textContent = '▶'; }
      });
      bar.querySelector('.speed-btn').addEventListener('click', () => {
        speedIdx = (speedIdx + 1) % speeds.length;
        audio.playbackRate = speeds[speedIdx];
        bar.querySelector('.speed-btn').textContent = speeds[speedIdx] + 'x';
      });
      audio.addEventListener('timeupdate', () => {
        const pct = audio.duration ? (audio.currentTime / audio.duration * 100) : 0;
        bar.querySelector('.progress-fill').style.width = pct + '%';
        const m = Math.floor(audio.currentTime / 60), s = Math.floor(audio.currentTime % 60);
        bar.querySelector('.time-display').textContent = m + ':' + String(s).padStart(2, '0');
      });
      audio.addEventListener('ended', () => {
        if (currentIdx < audioPlaylist.length - 1) playTrack(currentIdx + 1);
        else { bar.querySelector('.play-btn').textContent = '▶'; }
      });
      bar.querySelector('.progress-track').addEventListener('click', e => {
        if (!audio.duration) return;
        const rect = e.currentTarget.getBoundingClientRect();
        audio.currentTime = ((e.clientX - rect.left) / rect.width) * audio.duration;
      });
    })();
  </script>

<!-- ⭐ v6.11 AI 学伴配置（必须在 ai-tutor.js 加载前定义） -->
<script>
window.__TEACHANY_TUTOR_CONFIG__ = {
  courseTitle: "一次函数",
  subject: "math",
  grade: 8,
  learningObjectives: []
};
</script>
<!-- ⭐ v6.11 AI 学伴脚本（左下角 FAB · 首次点击引导配置 API） -->
<script src="./ai-tutor.js" defer></script>

</body>
</html>
