<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>工业革命 · 初中历史 G9 · TeachAny v5.34.8</title>
<!-- TeachAny Routing Metadata -->
<meta name="course-id" content="history-industrial-revolution">
<meta name="course-node" content="history-industrial-revolution">
<meta name="course-subject" content="history">
<meta name="course-title" content="工业革命">
<meta name="course-prereqs" content="">
<meta name="course-next" content="">
<link rel="stylesheet" href="./ai-tutor.css">
<style>
/* === Middle School Template (Blue-Teal) === */
:root {
  --bg: #f8fafc;
  --primary: #3b82f6;
  --secondary: #06b6d4;
  --accent: #f59e0b;
  --card: #ffffff;
  --card-solid: #ffffff;
  --ink: #1e293b;
  --text2: #64748b;
  --border: rgba(148,163,184,0.2);
  --success: #10b981;
  --danger: #ef4444;
  --radius: 14px;
  --glow: 0 4px 20px rgba(59,130,246,0.1);
}
*{box-sizing:border-box;margin:0;padding:0}
html{scroll-behavior:smooth}
body{font-family:'PingFang SC','Microsoft YaHei',sans-serif;background:var(--bg);color:var(--ink);line-height:1.85;font-size:15px}

/* Progress Bar */
.progress-bar{position:fixed;top:0;left:0;height:3px;width:0;z-index:300;background:linear-gradient(90deg,var(--primary),var(--secondary),var(--accent));transition:width .3s}

/* Nav */
.nav-bar{position:sticky;top:0;z-index:200;display:flex;align-items:center;gap:6px;padding:10px 16px;background:linear-gradient(135deg,#3b82f6,#06b6d4);overflow-x:auto;white-space:nowrap}
.nav-bar .brand{font-weight:800;font-size:15px;color:#fff;margin-right:8px;letter-spacing:.5px}
.nav-bar a{text-decoration:none;color:rgba(255,255,255,0.85);padding:5px 12px;border-radius:999px;font-size:13px;transition:.2s}
.nav-bar a:hover,.nav-bar a.active{background:rgba(255,255,255,0.25);color:#fff}

/* Hero */
.hero{padding:48px 20px 36px;background:linear-gradient(180deg,#f8fafc 0%,#e0f2fe 100%);text-align:center}
.hero h1{font-size:clamp(26px,5vw,42px);line-height:1.3;margin-bottom:10px;background:linear-gradient(135deg,var(--primary),var(--secondary));-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text;}
.hero .subtitle{font-size:1.05rem;color:var(--text2);margin-bottom:16px}
.hero .tags{display:flex;gap:8px;justify-content:center;flex-wrap:wrap}
.hero .tag{padding:4px 14px;border-radius:999px;font-size:12px;font-weight:600}
.tag-subj{background:rgba(59,130,246,0.12);color:var(--primary)}
.tag-grade{background:rgba(6,182,212,0.12);color:var(--secondary)}
.tag-type{background:rgba(245,158,11,0.12);color:var(--accent)}

/* Container */
.container{max-width:900px;margin:0 auto;padding:0 20px}

/* Section */
.section{margin:40px 0}
.section-title{font-size:1.35rem;font-weight:700;margin-bottom:16px;padding-left:14px;border-left:4px solid var(--primary);color:var(--ink)}

/* Cards */
.card{background:var(--card);border-radius:var(--radius);padding:22px;margin-bottom:16px;box-shadow:var(--glow);border:1px solid var(--border)}
.card h3{font-size:1.05rem;margin-bottom:10px;color:var(--primary)}
.card p{margin-bottom:8px;font-size:15px}

/* Highlight */
.highlight{background:linear-gradient(120deg,rgba(245,158,11,0.18),rgba(245,158,11,0.05));padding:2px 6px;border-radius:4px;font-weight:600;color:var(--accent)}

/* ABT Box */
.abt-box{display:grid;grid-template-columns:1fr 1fr 1fr;gap:12px;margin:16px 0}
.abt-card{padding:14px;border-radius:var(--radius);text-align:center}
.abt-and{background:rgba(59,130,246,0.08);border:1px solid rgba(59,130,246,0.2)}
.abt-but{background:rgba(245,158,11,0.08);border:1px solid rgba(245,158,11,0.2)}
.abt-therefore{background:rgba(16,185,129,0.08);border:1px solid rgba(16,185,129,0.2)}
.abt-label{font-size:11px;font-weight:700;text-transform:uppercase;margin-bottom:5px;letter-spacing:1px}
.abt-and .abt-label{color:var(--primary)}
.abt-but .abt-label{color:var(--accent)}
.abt-therefore .abt-label{color:var(--success)}
.abt-text{font-size:13.5px;color:var(--ink);line-height:1.7}

/* Timeline */
.timeline{position:relative;padding-left:28px;margin:16px 0}
.timeline::before{content:'';position:absolute;left:8px;top:0;bottom:0;width:3px;background:linear-gradient(180deg,var(--primary),var(--secondary),var(--accent));border-radius:2px}
.tl-item{position:relative;margin-bottom:14px;padding:14px 16px;background:var(--card);border-radius:var(--radius);border:1px solid var(--border)}
.tl-item::before{content:'';position:absolute;left:-24px;top:16px;width:12px;height:12px;border-radius:50%;background:var(--primary);border:3px solid var(--bg)}
.tl-year{font-size:12px;font-weight:700;color:var(--accent);margin-bottom:3px}
.tl-title{font-size:14.5px;font-weight:600;margin-bottom:3px}
.tl-desc{font-size:13.5px;color:var(--text2);line-height:1.7}

/* Fact Box */
.fact-box{background:rgba(245,158,11,0.06);border:1px solid rgba(245,158,11,0.18);border-radius:var(--radius);padding:16px 18px;margin:14px 0}
.fact-box .fact-label{font-size:11px;font-weight:700;color:var(--accent);margin-bottom:5px;letter-spacing:1px}
.fact-box p{font-size:13.5px;margin-bottom:3px;color:var(--ink)}

/* Source Card */
.source-card{background:var(--card);border-radius:var(--radius);padding:18px;margin:12px 0;border:1px solid var(--border)}
.source-card .source-label{font-size:11px;font-weight:700;color:var(--accent);text-transform:uppercase;letter-spacing:1px;margin-bottom:6px}
.source-card blockquote{font-size:14px;font-style:italic;color:var(--text2);line-height:1.8;border-left:3px solid var(--secondary);padding-left:12px;margin:6px 0}
.source-card .source-note{font-size:12px;color:var(--text2);margin-top:6px}

/* Quiz */
.quiz-card{background:var(--card);border-radius:var(--radius);padding:22px;margin-bottom:16px;border:1px solid var(--border);box-shadow:var(--glow)}
.quiz-card h3{font-size:1rem;margin-bottom:10px;color:var(--ink)}
.quiz-q{font-size:15px;margin-bottom:12px;font-weight:500}
.quiz-opts{display:flex;flex-direction:column;gap:8px}
.quiz-opt{padding:10px 14px;border:2px solid var(--border);border-radius:10px;cursor:pointer;font-size:14px;transition:.2s;display:flex;align-items:center;gap:8px;user-select:none}
.quiz-opt:hover{border-color:var(--primary);background:rgba(59,130,246,0.05)}
.quiz-opt.correct{border-color:var(--success);background:rgba(16,185,129,0.1);color:var(--success)}
.quiz-opt.wrong{border-color:var(--danger);background:rgba(239,68,68,0.08);color:var(--danger)}
.quiz-opt.selected{border-color:var(--primary);background:rgba(59,130,246,0.08)}
.quiz-feedback{margin-top:10px;padding:12px 14px;border-radius:8px;font-size:13px;display:none;line-height:1.7}
.quiz-feedback.show{display:block}
.fb-correct{background:rgba(16,185,129,0.1);color:var(--success);border:1px solid rgba(16,185,129,0.2)}
.fb-wrong{background:rgba(239,68,68,0.08);color:var(--danger);border:1px solid rgba(239,68,68,0.15)}

/* Invention Grid */
.invention-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(140px,1fr));gap:12px;margin:16px 0}
.invention-card{background:var(--card);border-radius:var(--radius);padding:16px 12px;text-align:center;cursor:pointer;transition:.2s;box-shadow:0 2px 12px rgba(0,0,0,0.06);border:2px solid transparent}
.invention-card:hover{transform:translateY(-4px);box-shadow:0 6px 20px rgba(59,130,246,0.12);border-color:var(--primary)}
.invention-card .icon{font-size:40px;margin-bottom:6px}
.invention-card .name{font-size:13.5px;font-weight:600;color:var(--ink)}
.invention-card .year{font-size:11.5px;color:var(--text2);margin-top:3px}
.detail-panel{background:rgba(6,182,212,0.06);border-radius:12px;padding:14px 16px;margin:12px 0;display:none;font-size:13.5px;line-height:1.8;color:var(--ink)}
.detail-panel.show{display:block}

/* Drag Sort */
.drag-container{display:flex;flex-direction:column;gap:8px;margin:16px 0}
.drag-item{display:flex;align-items:center;gap:12px;padding:10px 14px;background:var(--card);border-radius:12px;cursor:grab;box-shadow:0 2px 8px rgba(0,0,0,0.06);border:2px solid transparent;transition:.2s;font-size:14px}
.drag-item:active{cursor:grabbing;border-color:var(--primary)}
.drag-item .emoji{font-size:22px}
.drag-item .name{font-weight:600;color:var(--ink)}
.drag-item.correct-pos{border-color:var(--success);background:rgba(16,185,129,0.06)}
.drag-item.wrong-pos{border-color:var(--danger);background:rgba(239,68,68,0.05)}

/* Compare Table */
.compare-table{width:100%;border-collapse:separate;border-spacing:0;margin:16px 0;font-size:14px;border-radius:var(--radius);overflow:hidden;border:1px solid var(--border)}
.compare-table th{background:rgba(59,130,246,0.1);color:var(--primary);padding:10px 14px;text-align:left;font-weight:600;font-size:13px}
.compare-table td{padding:10px 14px;border-top:1px solid var(--border);color:var(--ink)}
.compare-table tr:hover td{background:rgba(59,130,246,0.03)}

/* Toggle Button */
.toggle-btn{padding:6px 16px;border:1px solid var(--border);border-radius:8px;background:var(--card);cursor:pointer;font-size:13px;color:var(--text2);transition:.2s;margin-top:8px}
.toggle-btn:hover{border-color:var(--primary);color:var(--primary)}
.solution{display:none;margin-top:10px;padding:14px;background:rgba(59,130,246,0.05);border-radius:8px;border-left:3px solid var(--primary);font-size:14px;line-height:1.8}
.solution.open{display:block}

/* Test Banner */
.test-banner{background:linear-gradient(135deg,rgba(6,182,212,0.1),rgba(59,130,246,0.06));border:1px solid rgba(6,182,212,0.18);border-radius:var(--radius);padding:18px;text-align:center;margin:20px 0}
.test-banner h3{color:var(--secondary);margin-bottom:6px}

/* Footer */
.footer{padding:32px 20px;text-align:center;color:var(--text2);font-size:13px;border-top:1px solid var(--border);margin-top:40px}

/* Module Figures (v5.34.8 L3/#34 插图基线) */
.module-figure{margin:16px 0 20px;border-radius:var(--radius);overflow:hidden;background:var(--card);box-shadow:var(--glow);border:1px solid var(--border)}
.module-figure img{width:100%;display:block;max-height:520px;object-fit:cover;background:#f1f5f9}
.module-figure figcaption{padding:10px 16px;font-size:13px;color:var(--text2);line-height:1.7;background:linear-gradient(180deg,rgba(59,130,246,0.04),transparent)}
.module-figure figcaption strong{color:var(--primary);font-weight:600}

/* Interactive UK Map (v5.34.9 增强) */
.map-wrap{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:14px;align-items:start}
.uk-map{width:100%;height:auto;max-height:520px;border-radius:12px;background:#dbeafe;box-shadow:0 4px 18px rgba(15,23,42,0.08)}
.uk-map .hotspot{cursor:pointer;transition:transform .18s ease}
.uk-map .hotspot:hover{transform:scale(1.15) translateY(-2px)}
.uk-map .hotspot circle{transition:.2s}
.uk-map .hotspot:hover circle{stroke:#fbbf24;stroke-width:3}
.uk-map .hotspot.visited circle{stroke:#10b981;stroke-width:3}
.factor-detail{background:var(--card);border:1px solid var(--border);border-radius:12px;padding:16px;min-height:320px;box-shadow:var(--glow)}
.factor-detail .fd-placeholder{color:var(--text2);font-size:14px;text-align:center;padding:40px 10px;line-height:1.9}
.factor-detail h4{font-size:1.05rem;color:var(--primary);margin-bottom:8px;display:flex;align-items:center;gap:8px}
.factor-detail p{font-size:13.5px;line-height:1.85;margin-bottom:8px;color:var(--ink)}
.factor-detail .fd-tag{display:inline-block;padding:3px 10px;border-radius:999px;font-size:11px;font-weight:600;margin-right:6px;margin-bottom:8px}
.fd-tag.t-coal{background:rgba(15,23,42,0.08);color:#0f172a}
.fd-tag.t-enclosure{background:rgba(5,150,105,0.12);color:#059669}
.fd-tag.t-manchester{background:rgba(220,38,38,0.12);color:#dc2626}
.fd-tag.t-colonies{background:rgba(124,58,237,0.12);color:#7c3aed}
.fd-tag.t-science{background:rgba(234,88,12,0.12);color:#ea580c}
@media(max-width:720px){.map-wrap{grid-template-columns:1fr}.factor-detail{min-height:180px}}

/* Steam Engine Canvas Animation (v5.34.9) */
.steam-anim-wrap{margin-top:12px}
.steam-anim-wrap canvas{width:100%;max-width:640px;height:auto;display:block;margin:0 auto;border-radius:12px;background:linear-gradient(180deg,#fef9c3,#fde68a);border:2px solid #d97706;box-shadow:0 4px 18px rgba(217,119,6,0.2)}
.steam-controls{display:flex;gap:10px;align-items:center;margin-top:12px;flex-wrap:wrap;justify-content:center}
.steam-rpm{font-size:12.5px;color:var(--text2);font-weight:600;font-variant-numeric:tabular-nums}
.steam-rpm span{color:var(--primary);font-weight:700}

/* Cotton Chart (v5.34.9 · 2026-04-19) */
.chart-wrap{margin-top:12px}
.chart-wrap canvas{width:100%;max-width:720px;height:auto;display:block;margin:0 auto;border-radius:12px;background:#fafaf9;border:1px solid var(--border);box-shadow:0 4px 18px rgba(15,23,42,0.06);cursor:crosshair}
.chart-legend{display:flex;gap:16px;justify-content:center;flex-wrap:wrap;margin-top:10px;font-size:12px;color:var(--text2)}
.chart-legend .dot{display:inline-block;width:10px;height:10px;border-radius:50%;margin-right:5px;vertical-align:middle}
.chart-fact{margin-top:12px;padding:14px 16px;background:var(--card);border:1px solid var(--border);border-radius:10px;min-height:80px;box-shadow:var(--glow)}
.chart-fact .cf-placeholder{color:var(--text2);font-size:13px;text-align:center;padding:14px 0}
.chart-fact h5{font-size:14px;color:var(--primary);margin-bottom:6px;display:flex;align-items:center;gap:8px}
.chart-fact p{font-size:13.5px;line-height:1.8;color:var(--ink);margin-bottom:6px}
.chart-fact .cf-year{display:inline-block;padding:2px 10px;background:linear-gradient(135deg,var(--accent),#fbbf24);color:#fff;border-radius:999px;font-size:11px;font-weight:700;margin-right:6px}

/* Audio Badge & Panel (v5.34.8 L3) */
.audio-badge{position:fixed;right:18px;bottom:18px;z-index:250;width:56px;height:56px;border-radius:50%;background:linear-gradient(135deg,var(--primary),var(--secondary));color:#fff;border:none;font-size:24px;cursor:pointer;box-shadow:0 6px 20px rgba(59,130,246,0.35);transition:.2s}
.audio-badge:hover{transform:translateY(-2px);box-shadow:0 10px 26px rgba(59,130,246,0.45)}
.audio-badge.playing{animation:badgePulse 1.6s ease-in-out infinite}
@keyframes badgePulse{0%,100%{box-shadow:0 6px 20px rgba(59,130,246,0.35)}50%{box-shadow:0 6px 28px rgba(6,182,212,0.65)}}
.audio-panel{position:fixed;right:18px;bottom:84px;z-index:249;width:min(320px,calc(100vw - 36px));background:var(--card);border:1px solid var(--border);border-radius:16px;padding:14px 16px;box-shadow:0 12px 40px rgba(15,23,42,0.18);transform-origin:bottom right;transform:scale(.9);opacity:0;pointer-events:none;transition:.2s}
.audio-panel.open{transform:scale(1);opacity:1;pointer-events:auto}
.ap-header{display:flex;justify-content:space-between;align-items:center;margin-bottom:8px}
.ap-title{font-weight:700;font-size:14px;color:var(--ink)}
.ap-close{background:none;border:none;font-size:18px;cursor:pointer;color:var(--text2);padding:4px 8px;border-radius:6px}
.ap-close:hover{background:rgba(0,0,0,0.04);color:var(--ink)}
.ap-seg-title{font-size:13px;color:var(--primary);margin-bottom:6px;font-weight:600}
.ap-progress{height:5px;background:rgba(0,0,0,0.06);border-radius:3px;margin:8px 0 4px;cursor:pointer;position:relative}
.ap-progress-fill{height:100%;background:linear-gradient(90deg,var(--primary),var(--secondary));border-radius:3px;width:0;transition:width .1s}
.ap-time{display:flex;justify-content:space-between;font-size:11px;color:var(--text2);margin-bottom:8px}
.ap-controls{display:flex;justify-content:center;gap:12px;margin:6px 0}
.ap-btn{width:36px;height:36px;border-radius:50%;border:1px solid var(--border);background:var(--card);color:var(--ink);cursor:pointer;font-size:14px;display:flex;align-items:center;justify-content:center;transition:.15s}
.ap-btn:hover{border-color:var(--primary);color:var(--primary)}
.ap-btn.ap-play{width:42px;height:42px;background:linear-gradient(135deg,var(--primary),var(--secondary));color:#fff;border:none}
.ap-subtitle{font-size:12px;color:var(--text2);text-align:center;line-height:1.6;margin:6px 0 8px}
.seg-btns{display:grid;grid-template-columns:repeat(2,1fr);gap:6px;margin-top:8px}
.seg-btn{padding:6px 8px;border:1px solid var(--border);border-radius:8px;background:var(--card);font-size:11.5px;cursor:pointer;color:var(--text2);transition:.15s;text-align:left;line-height:1.4}
.seg-btn:hover{border-color:var(--primary);color:var(--primary)}
.seg-btn.active{background:rgba(59,130,246,0.1);border-color:var(--primary);color:var(--primary);font-weight:600}

/* Responsive */
@media(max-width:600px){
  .abt-box{grid-template-columns:1fr}
  .invention-grid{grid-template-columns:repeat(2,1fr)}
}
</style>
</head>
<body>

<!-- Progress Bar -->
<div class="progress-bar" id="progressBar"></div>

<!-- Navigation -->
<nav class="nav-bar">
  <strong class="brand">TeachAny</strong>
  <a href="#intro">导入</a>
  <a href="#objectives">目标</a>
  <a href="#pretest">前测</a>
  <a href="#module1">为何英国</a>
  <a href="#module2">关键发明</a>
  <a href="#module3">社会影响</a>
  <a href="#posttest">后测</a>
  <a href="#extension">拓展</a>
</nav>

<!-- Hero -->
<header class="hero">
  <h1>🏭 工业革命</h1>
  <p class="subtitle">18世纪60年代—19世纪中期，人类生产方式的根本变革</p>
  <div class="tags">
    <span class="tag tag-subj">世界历史</span>
    <span class="tag tag-grade">初二·初三</span>
    <span class="tag tag-type">新概念课</span>
  </div>
  <img src="assets/steam-engine.png" alt="蒸汽机" style="display:block;max-width:680px;margin:24px auto 0;border-radius:14px;box-shadow:0 4px 24px rgba(0,0,0,0.10)">
</header>

<div class="container">

<!-- ===== ABT Introduction ===== -->
<section class="section" id="intro">
  <h2 class="section-title">课程导入</h2>
  <div class="abt-box">
    <div class="abt-card abt-and">
      <div class="abt-label">And · 已知</div>
      <div class="abt-text">你已经知道，在工业革命之前，人类的生产主要靠人力、畜力和简单手工工具，生产效率低下</div>
    </div>
    <div class="abt-card abt-but">
      <div class="abt-label">But · 问题</div>
      <div class="abt-text">但18世纪后期，英国突然出现"生产力爆发"——同样的时间，产量提高了几十倍甚至上百倍，这是怎么做到的？</div>
    </div>
    <div class="abt-card abt-therefore">
      <div class="abt-label">Therefore · 新知</div>
      <div class="abt-text">我们需要理解工业革命的核心发明、发生原因，以及它如何彻底改变了人类社会的面貌</div>
    </div>
  </div>

  <div class="source-card">
    <div class="source-label">📖 原始材料</div>
    <blockquote>"从那时起，世界被改变了。煤炭和钢铁取代了木材和肌肉，蒸汽机取代了手工劳动。人类进入了一个全新的时代。"<br>——后世史学家对工业革命的评价</blockquote>
    <div class="source-note">⚠️ 思考：为什么说"世界被改变了"？工业革命之前和之后，普通人的生活有什么根本不同？</div>
  </div>
</section>

<!-- ===== Learning Objectives ===== -->
<section class="section" id="objectives">
  <h2 class="section-title">🎯 学习目标</h2>
  <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:12px;margin:16px 0">
    <div style="padding:14px 16px;border-radius:var(--radius);border-left:3px solid var(--primary);background:var(--card);font-size:14px;box-shadow:var(--glow)">
      <div style="font-size:12px;font-weight:700;color:var(--primary);margin-bottom:4px">目标一 · 识记</div>
      说出工业革命的起止时间、首发国家，列举5项关键发明及发明者
    </div>
    <div style="padding:14px 16px;border-radius:var(--radius);border-left:3px solid var(--secondary);background:var(--card);font-size:14px;box-shadow:var(--glow)">
      <div style="font-size:12px;font-weight:700;color:var(--secondary);margin-bottom:4px">目标二 · 理解</div>
      解释工业革命为何始于英国（5大因素），理解"需求拉动+技术推动"的双重动力
    </div>
    <div style="padding:14px 16px;border-radius:var(--radius);border-left:3px solid var(--accent);background:var(--card);font-size:14px;box-shadow:var(--glow)">
      <div style="font-size:12px;font-weight:700;color:var(--accent);margin-bottom:4px">目标三 · 评价</div>
      分析工业革命对社会结构和自然环境的双重影响，培养多元评价历史事件的视角
    </div>
  </div>
</section>

<!-- ===== Pre-test ===== -->
<section class="section" id="pretest">
  <h2 class="section-title">📋 前测：你知道多少？</h2>
  <div class="test-banner">
    <h3>🎯 3道题，测测你的起点</h3>
    <p style="font-size:14px;color:var(--text2)">答完不评分，只为了解你的基础</p>
  </div>

  <div class="quiz-card" data-quiz="pre1" data-bloom="1">
    <h3>第1题</h3>
    <div class="quiz-q">工业革命大约开始于哪个年代？</div>
    <div class="quiz-opts">
      <div class="quiz-opt" data-val="A" onclick="handleQuiz(this,'pre1','B','A')">A. 15世纪</div>
      <div class="quiz-opt" data-val="B" onclick="handleQuiz(this,'pre1','B','B')">B. 18世纪60年代</div>
      <div class="quiz-opt" data-val="C" onclick="handleQuiz(this,'pre1','B','C')">C. 19世纪中期</div>
      <div class="quiz-opt" data-val="D" onclick="handleQuiz(this,'pre1','B','D')">D. 20世纪初</div>
    </div>
    <div class="quiz-feedback" id="fb-pre1"></div>
  </div>

  <div class="quiz-card" data-quiz="pre2" data-bloom="1">
    <h3>第2题</h3>
    <div class="quiz-q">工业革命最早爆发于哪个国家？</div>
    <div class="quiz-opts">
      <div class="quiz-opt" data-val="A" onclick="handleQuiz(this,'pre2','B','A')">A. 法国</div>
      <div class="quiz-opt" data-val="B" onclick="handleQuiz(this,'pre2','B','B')">B. 英国</div>
      <div class="quiz-opt" data-val="C" onclick="handleQuiz(this,'pre2','B','C')">C. 德国</div>
      <div class="quiz-opt" data-val="D" onclick="handleQuiz(this,'pre2','B','D')">D. 美国</div>
    </div>
    <div class="quiz-feedback" id="fb-pre2"></div>
  </div>

  <div class="quiz-card" data-quiz="pre3" data-bloom="2">
    <h3>第3题</h3>
    <div class="quiz-q">工业革命的核心动力发明是？</div>
    <div class="quiz-opts">
      <div class="quiz-opt" data-val="A" onclick="handleQuiz(this,'pre3','A','A')">A. 蒸汽机</div>
      <div class="quiz-opt" data-val="B" onclick="handleQuiz(this,'pre3','A','B')">B. 发电机</div>
      <div class="quiz-opt" data-val="C" onclick="handleQuiz(this,'pre3','A','C')">C. 内燃机</div>
      <div class="quiz-opt" data-val="D" onclick="handleQuiz(this,'pre3','A','D')">D. 计算机</div>
    </div>
    <div class="quiz-feedback" id="fb-pre3"></div>
  </div>
</section>

<!-- ===== Module 1: Why Britain? ===== -->
<section class="section" id="module1">
  <h2 class="section-title">模块一：为何始于英国？</h2>

  <figure class="module-figure">
    <img src="./assets/manchester-skyline.png" alt="19 世纪曼彻斯特工业城市远景：林立的砖砌烟囱与运河">
    <figcaption><strong>曼彻斯特：工业革命的"心脏"。</strong>从 1760 年代开始，英国的几个中部城市因密集的棉纺织厂和蒸汽驱动机械，迅速从小镇扩张为吞云吐雾的大工业区。为什么是英国？请带着这张画面往下读。</figcaption>
  </figure>

  <!-- ===== Interactive Map: 5 Factors Behind Industrial Revolution ===== -->
  <div class="card">
    <h3>🗺️ 互动地图：英国的"五要素地理"</h3>
    <p>点击地图上的任意标记，了解它如何推动工业革命。试着找齐 5 个要素！</p>
    <div class="map-wrap">
      <svg class="uk-map" viewBox="0 0 420 520" xmlns="http://www.w3.org/2000/svg" aria-labelledby="mapTitle">
        <title id="mapTitle">英国工业革命五要素分布图</title>
        <!-- Ocean -->
        <rect x="0" y="0" width="420" height="520" fill="#dbeafe"/>
        <!-- Wavy sea decor -->
        <g opacity="0.35" stroke="#93c5fd" stroke-width="1.2" fill="none">
          <path d="M0 60 Q20 54 40 60 T80 60 T120 60 T160 60"/>
          <path d="M250 420 Q270 414 290 420 T330 420 T370 420 T410 420"/>
        </g>
        <!-- Ireland silhouette -->
        <path d="M60 220 Q55 200 70 185 Q95 175 120 190 Q130 215 115 240 Q100 260 80 255 Q62 248 60 220 Z"
              fill="#f1f5f9" stroke="#94a3b8" stroke-width="1.5" opacity="0.85"/>
        <text x="85" y="225" font-size="10" fill="#64748b" text-anchor="middle">爱尔兰</text>
        <!-- Great Britain main island silhouette (approximated) -->
        <path d="M200 80
                 Q230 70 255 90
                 Q265 120 248 140
                 Q258 170 245 195
                 Q270 220 260 250
                 Q280 280 260 310
                 Q275 340 250 370
                 Q245 400 220 420
                 Q195 430 180 410
                 Q155 395 165 365
                 Q140 340 155 310
                 Q140 280 160 255
                 Q145 220 170 195
                 Q155 160 175 135
                 Q180 105 200 80 Z"
              fill="#fef3c7" stroke="#b45309" stroke-width="2"/>
        <!-- Scotland label -->
        <text x="215" y="110" font-size="10" fill="#78350f" text-anchor="middle" font-weight="600">苏格兰</text>
        <!-- England label -->
        <text x="215" y="300" font-size="11" fill="#78350f" text-anchor="middle" font-weight="700">英 格 兰</text>
        <!-- Wales label -->
        <text x="175" y="330" font-size="9" fill="#78350f" text-anchor="middle">威尔士</text>
        <!-- Thames / London hint -->
        <path d="M225 370 Q240 375 255 370" stroke="#60a5fa" stroke-width="1.5" fill="none" opacity="0.6"/>

        <!-- Continental Europe hint (right edge) -->
        <path d="M390 180 Q405 200 400 260 Q408 310 395 360 Q402 410 390 450 L420 450 L420 180 Z"
              fill="#e2e8f0" stroke="#94a3b8" stroke-width="1.2" opacity="0.7"/>
        <text x="405" y="310" font-size="10" fill="#64748b" text-anchor="middle" transform="rotate(90 405 310)">欧洲大陆</text>

        <!-- ===== 5 Hotspots ===== -->
        <!-- 1. Coal & Iron — Newcastle (NE England) -->
        <g class="hotspot" data-factor="coal" tabindex="0" onclick="showFactor('coal')">
          <circle cx="230" cy="220" r="14" fill="#0f172a" stroke="#fff" stroke-width="2"/>
          <text x="230" y="225" font-size="14" fill="#fff" text-anchor="middle">⛏️</text>
          <text x="230" y="248" font-size="10" fill="#0f172a" text-anchor="middle" font-weight="600">煤铁资源</text>
        </g>
        <!-- 2. Enclosure — Midlands (Central England) -->
        <g class="hotspot" data-factor="enclosure" tabindex="0" onclick="showFactor('enclosure')">
          <circle cx="210" cy="285" r="14" fill="#059669" stroke="#fff" stroke-width="2"/>
          <text x="210" y="290" font-size="14" fill="#fff" text-anchor="middle">🌾</text>
          <text x="210" y="313" font-size="10" fill="#065f46" text-anchor="middle" font-weight="600">圈地运动</text>
        </g>
        <!-- 3. Manchester/Liverpool cotton hub (West England) -->
        <g class="hotspot" data-factor="manchester" tabindex="0" onclick="showFactor('manchester')">
          <circle cx="175" cy="265" r="14" fill="#dc2626" stroke="#fff" stroke-width="2"/>
          <text x="175" y="270" font-size="13" fill="#fff" text-anchor="middle">🏭</text>
          <text x="175" y="255" font-size="10" fill="#7f1d1d" text-anchor="middle" font-weight="600">曼彻斯特</text>
        </g>
        <!-- 4. Colonies/Ports — London (SE) -->
        <g class="hotspot" data-factor="colonies" tabindex="0" onclick="showFactor('colonies')">
          <circle cx="250" cy="370" r="14" fill="#7c3aed" stroke="#fff" stroke-width="2"/>
          <text x="250" y="375" font-size="14" fill="#fff" text-anchor="middle">⚓</text>
          <text x="250" y="397" font-size="10" fill="#5b21b6" text-anchor="middle" font-weight="600">伦敦·殖民地港口</text>
        </g>
        <!-- 5. Royal Society & Politics — Oxford/Cambridge area -->
        <g class="hotspot" data-factor="science" tabindex="0" onclick="showFactor('science')">
          <circle cx="225" cy="335" r="14" fill="#ea580c" stroke="#fff" stroke-width="2"/>
          <text x="225" y="340" font-size="13" fill="#fff" text-anchor="middle">🔬</text>
          <text x="225" y="358" font-size="10" fill="#9a3412" text-anchor="middle" font-weight="600">科学 + 稳定政治</text>
        </g>

        <!-- Legend -->
        <g transform="translate(15, 460)">
          <rect x="0" y="0" width="260" height="48" rx="6" fill="#fff" opacity="0.85" stroke="#cbd5e1"/>
          <text x="12" y="18" font-size="10.5" fill="#475569" font-weight="600">💡 点击 5 个热点 → 查看该因素如何推动工业革命</text>
          <text x="12" y="36" font-size="10" fill="#64748b">已点击：<tspan id="clickedCount" font-weight="700" fill="#0f172a">0</tspan> / 5</text>
        </g>
      </svg>
      <div class="factor-detail" id="factorDetail">
        <div class="fd-placeholder">👆 点击地图上的彩色圆点查看详情</div>
      </div>
    </div>
  </div>

  <div class="card">
    <h3>🤔 核心问题：条件叠加的"完美风暴"</h3>
    <p>工业革命不是单一发明造成的，而是<span class="highlight">5大条件在英国同时具备</span>，才引爆了生产力革命。让我们逐一分析：</p>
  </div>

  <!-- Factor 1: Agricultural Revolution -->
  <div class="card">
    <h3>🌾 因素一：农业革命（劳动力供给）</h3>
    <p>18世纪英国农业改良，<span class="highlight">圈地运动</span>使大量农民失去土地，被迫进入城市成为自由劳动力——为工厂提供了<span class="highlight">充足的雇佣工人</span>。</p>
    <div class="fact-box">
      <div class="fact-label">🔑 核心逻辑</div>
      <p>农业效率提高 → 剩余劳动力出现 → 农民进城务工 → 工厂有了工人</p>
    </div>
  </div>

  <!-- Factor 2: Colonial Trade & Capital -->
  <div class="card">
    <h3>💰 因素二：殖民贸易与资本积累</h3>
    <p>英国通过殖民扩张和海外贸易，<span class="highlight">积累了大量资本</span>，同时获得了廉价原料（棉花）和广阔市场——工厂主有资金建厂，有地方卖产品。</p>
    <div class="fact-box">
      <div class="fact-label">🔑 核心逻辑</div>
      <p>殖民掠夺 + 海外贸易 → 资本积累 → 有钱投资建厂 → 有市场消化产品</p>
    </div>
  </div>

  <!-- Factor 3: Coal & Resources -->
  <div class="card">
    <h3>⛏️ 因素三：煤炭资源与地理位置</h3>
    <p>英国煤炭储量丰富且靠近地表，<span class="highlight">廉价能源</span>为蒸汽机提供了燃料基础。同时英国海岸线长，河流众多，<span class="highlight">水力和海运</span>便利。</p>
    <div class="fact-box">
      <div class="fact-label">🔑 核心逻辑</div>
      <p>煤炭丰富 → 蒸汽机有燃料 → 工厂不依赖水力 → 可以建在任何地方</p>
    </div>
  </div>

  <!-- Factor 4: Political Stability -->
  <div class="card">
    <h3>🏛️ 因素四：政治稳定与专利保护</h3>
    <p>1688年<span class="highlight">光荣革命</span>后，英国政治长期稳定，君主立宪制保护了私有财产和专利权利——发明者的利益有保障，激发了技术创新的积极性。</p>
    <div class="fact-box">
      <div class="fact-label">🔑 核心逻辑</div>
      <p>政治稳定 → 专利法保护发明 → 发明可以赚钱 → 更多人投身技术创新</p>
    </div>
  </div>

  <!-- Factor 5: Scientific Thinking -->
  <div class="card">
    <h3>🔬 因素五：科学思维与工匠传统</h3>
    <p>英国有<span class="highlight">经验主义科学传统</span>，工匠和科学家乐于将理论应用于实际生产——这不是"纯科学"，而是"能用的科学"。</p>
    <div class="fact-box">
      <div class="fact-label">🔑 核心逻辑</div>
      <p>科学理论 + 工匠经验 → 技术发明 → 应用于生产 → 提高效率</p>
    </div>
  </div>

  <!-- Synthesis -->
  <div class="card">
    <h3>🔗 五因素综合：缺一不可</h3>
    <div style="overflow-x:auto">
      <table class="compare-table">
        <tr><th>条件</th><th>作用</th><th>如果缺失？</th></tr>
        <tr><td><strong>劳动力</strong></td><td>提供工厂工人</td><td>工厂没人干活</td></tr>
        <tr><td><strong>资本</strong></td><td>提供建厂资金</td><td>有技术没钱投产</td></tr>
        <tr><td><strong>能源</strong></td><td>提供动力燃料</td><td>机器没动力来源</td></tr>
        <tr><td><strong>制度</strong></td><td>保护发明激励</td><td>没人愿意搞发明</td></tr>
        <tr><td><strong>科学</strong></td><td>提供技术基础</td><td>发明缺乏理论指导</td></tr>
      </table>
    </div>
    <div class="source-card" style="margin-top:16px">
      <div class="source-label">📖 史料分析</div>
      <blockquote>"英国是幸运的——它同时拥有了煤炭、殖民地、稳定的政府和一群痴迷于改进机械的人。这些条件单独出现在其他国家，但从未像在英国这样同时具备。"<br>——经济史学家 David Landes</blockquote>
      <div class="source-note">思考：这说明了工业革命爆发的什么条件？为什么其他国家没有同时具备这些条件？</div>
    </div>
  </div>

  <!-- Module 1 Quiz -->
  <div class="quiz-card" data-quiz="m1q1" data-bloom="2">
    <h3>练习：五因素</h3>
    <div class="quiz-q">圈地运动对工业革命的主要作用是？</div>
    <div class="quiz-opts">
      <div class="quiz-opt" data-val="A" onclick="handleQuiz(this,'m1q1','A','A')">A. 提供了大量自由劳动力</div>
      <div class="quiz-opt" data-val="B" onclick="handleQuiz(this,'m1q1','A','B')">B. 提供了大量资本</div>
      <div class="quiz-opt" data-val="C" onclick="handleQuiz(this,'m1q1','A','C')">C. 促进了农业技术进步</div>
      <div class="quiz-opt" data-val="D" onclick="handleQuiz(this,'m1q1','A','D')">D. 扩大了海外市场</div>
    </div>
    <div class="quiz-feedback" id="fb-m1q1"></div>
  </div>
</section>

<!-- ===== Module 2: Key Inventions ===== -->
<section class="section" id="module2">
  <h2 class="section-title">模块二：关键发明与时间线</h2>

  <figure class="module-figure">
    <img src="./assets/steam-engine.png" alt="瓦特改良蒸汽机：横梁、飞轮、气缸与连杆的经典结构">
    <figcaption><strong>瓦特蒸汽机（1769 年改良）。</strong>这台机器是整个工业革命的"动力心脏"——它第一次让人类摆脱了风、水、畜力的限制，工厂可以建在任何有煤有人的地方。观察图中巨大的飞轮和长长的横梁，它们把直线往复运动转化为圆周运动，成为所有后续机械设备的原型。</figcaption>
  </figure>

  <!-- ===== Animated Steam Engine Canvas ===== -->
  <div class="card">
    <h3>⚙️ 动画演示：蒸汽机如何工作？</h3>
    <p>点击"启动 / 暂停"，观察蒸汽如何推动活塞做功，活塞又如何带动飞轮旋转——这就是 250 年前改变世界的机器原理。</p>
    <div class="steam-anim-wrap">
      <canvas id="steamCanvas" width="640" height="320"></canvas>
      <div class="steam-controls">
        <button class="toggle-btn" onclick="toggleSteam()" id="steamBtn">▶️ 启动动画</button>
        <button class="toggle-btn" onclick="changeSpeed()" id="speedBtn">速度：正常</button>
        <span class="steam-rpm">转速：<span id="rpmDisplay">0</span> RPM · 效率 <span id="effDisplay">0</span>%</span>
      </div>
      <p style="font-size:12.5px;color:var(--text2);margin-top:10px;line-height:1.8">
        <span style="color:#dc2626">🔴 红色区域 = 燃烧的煤；</span>
        <span style="color:#06b6d4">🔵 蓝色线 = 蒸汽流动路径；</span>
        <span style="color:#f59e0b">🟡 黄色部件 = 活塞做功冲程；</span>
        <span style="color:#10b981">🟢 绿色飞轮 = 最终输出动力。</span>
      </p>
    </div>
  </div>

  <!-- ===== Interactive Data Chart: Cotton Consumption 1750-1860 ===== -->
  <div class="card">
    <h3>📈 互动数据图：英国棉花消耗量（1750—1860）</h3>
    <p>一张图看懂"生产力爆发"到底有多夸张——110 年间，英国每年消耗的棉花从 <strong>3 百万磅</strong> 涨到 <strong>10 亿磅</strong>，增长了约 300 倍。点击曲线上的历史事件节点，看看每次跳跃背后是谁在推动。</p>
    <div class="chart-wrap">
      <canvas id="cottonChart" width="720" height="380"></canvas>
      <div class="chart-legend">
        <span><span class="dot" style="background:#dc2626"></span>关键发明</span>
        <span><span class="dot" style="background:#7c3aed"></span>政策事件</span>
        <span><span class="dot" style="background:#059669"></span>经济里程碑</span>
      </div>
      <div class="chart-fact" id="chartFact">
        <div class="cf-placeholder">👆 点击彩色圆点，看该年发生了什么</div>
      </div>
    </div>
    <p style="font-size:12.5px;color:var(--text2);margin-top:10px">📚 数据来源：Deane & Cole《British Economic Growth 1688—1959》、Mitchell《British Historical Statistics》</p>
  </div>

  <div class="card">
    <h3>🔧 点击探索：工业革命的5大关键发明</h3>
    <p>工业革命不是一夜之间发生的，而是一系列关键发明逐步推进的结果。点击下面的卡片，了解每项发明的年代、发明者和历史意义：</p>
  </div>

  <div class="invention-grid" id="inventionGrid">
    <div class="invention-card" onclick="showDetail('spin')">
      <div class="icon">🧵</div>
      <div class="name">珍妮纺纱机</div>
      <div class="year">1765年</div>
    </div>
    <div class="invention-card" onclick="showDetail('steam')">
      <div class="icon">⚙️</div>
      <div class="name">改良蒸汽机</div>
      <div class="year">1785年</div>
    </div>
    <div class="invention-card" onclick="showDetail('iron')">
      <div class="icon">🏗️</div>
      <div class="name">焦炭炼铁法</div>
      <div class="year">1709年</div>
    </div>
    <div class="invention-card" onclick="showDetail('train')">
      <div class="icon">🚂</div>
      <div class="name">蒸汽火车</div>
      <div class="year">1825年</div>
    </div>
    <div class="invention-card" onclick="showDetail('telegraph')">
      <div class="icon">📡</div>
      <div class="name">电报</div>
      <div class="year">1837年</div>
    </div>
  </div>

  <div class="detail-panel" id="detailPanel"></div>

  <!-- Timeline Drag Sort -->
  <div class="card">
    <h3>📅 时间线排序挑战</h3>
    <p>请将下面的发明按<span class="highlight">时间先后顺序</span>拖拽排列（最早在上，最晚在下）：</p>
    <div class="drag-container" id="dragContainer">
      <div class="drag-item" draggable="true" data-order="0" id="drag-0">
        <span class="emoji">🏗️</span><span class="name">焦炭炼铁法（1709）</span>
      </div>
      <div class="drag-item" draggable="true" data-order="1" id="drag-1">
        <span class="emoji">🧵</span><span class="name">珍妮纺纱机（1765）</span>
      </div>
      <div class="drag-item" draggable="true" data-order="2" id="drag-2">
        <span class="emoji">⚙️</span><span class="name">改良蒸汽机（1785）</span>
      </div>
      <div class="drag-item" draggable="true" data-order="3" id="drag-3">
        <span class="emoji">📡</span><span class="name">电报（1837）</span>
      </div>
      <div class="drag-item" draggable="true" data-order="4" id="drag-4">
        <span class="emoji">🚂</span><span class="name">蒸汽火车（1825）</span>
      </div>
    </div>
    <button class="toggle-btn" onclick="checkTimeline()">检查排序 ✓</button>
    <div id="timelineResult" style="margin-top:10px;font-size:14px;display:none"></div>
  </div>

  <!-- Module 2 Quiz -->
  <div class="quiz-card" data-quiz="m2q1" data-bloom="1">
    <h3>练习：发明与发明者</h3>
    <div class="quiz-q">改良蒸汽机的发明者是？</div>
    <div class="quiz-opts">
      <div class="quiz-opt" data-val="A" onclick="handleQuiz(this,'m2q1','B','A')">A. 哈格里夫斯</div>
      <div class="quiz-opt" data-val="B" onclick="handleQuiz(this,'m2q1','B','B')">B. 瓦特</div>
      <div class="quiz-opt" data-val="C" onclick="handleQuiz(this,'m2q1','B','C')">C. 史蒂芬森</div>
      <div class="quiz-opt" data-val="D" onclick="handleQuiz(this,'m2q1','B','D')">D. 富尔顿</div>
    </div>
    <div class="quiz-feedback" id="fb-m2q1"></div>
  </div>
</section>

<!-- ===== Module 3: Social Impact ===== -->
<section class="section" id="module3">
  <h2 class="section-title">模块三：社会双重影响</h2>

  <figure class="module-figure">
    <img src="./assets/textile-mill.png" alt="19 世纪纺织厂内景：一排排纺纱机、棉絮飞扬、女工和童工在操作">
    <figcaption><strong>纺织厂里的童工与女工（约 1800 年）。</strong>工业革命让英国的布料产量在 50 年内增长 50 倍，但代价是这样的场景——童工日工作 12-14 小时，棉絮与煤灰充斥肺腔。评价工业革命，不能只看 GDP 数字，还要看这样一张照片里每个人的表情。</figcaption>
  </figure>

  <div class="card">
    <h3>⚖️ 评价原则：历史事件的双重性</h3>
    <p>工业革命是人类历史的<span class="highlight">重大转折点</span>，但它既不是"纯好事"，也不是"纯坏事"——我们需要用<span class="highlight">多元视角</span>来评价：</p>
  </div>

  <!-- Positive Impacts -->
  <div class="card">
    <h3>✅ 积极影响</h3>
    <div style="overflow-x:auto">
      <table class="compare-table">
        <tr><th>领域</th><th>具体变化</th><th>历史意义</th></tr>
        <tr><td><strong>生产力</strong></td><td>机器生产取代手工劳动，产量爆炸式增长</td><td>人类摆脱"马尔萨斯陷阱"（粮食增长赶不上人口）</td></tr>
        <tr><td><strong>社会结构</strong></td><td>工业资产阶级和无产阶级诞生</td><td>现代社会阶级格局的形成</td></tr>
        <tr><td><strong>城市化</strong></td><td>工厂集中→城市迅速扩张</td><td>现代城市文明的基础</td></tr>
        <tr><td><strong>交通通讯</strong></td><td>铁路、电报、轮船的出现</td><td>世界市场形成，全球化起步</td></tr>
        <tr><td><strong>生活方式</strong></td><td>商品丰富、物价下降、时间观念强化</td><td>现代生活方式的雏形</td></tr>
      </table>
    </div>
  </div>

  <!-- Negative Impacts -->
  <div class="card">
    <h3>❌ 消极影响</h3>
    <div style="overflow-x:auto">
      <table class="compare-table">
        <tr><th>领域</th><th>具体问题</th><th>历史影响</th></tr>
        <tr><td><strong>工人处境</strong></td><td>工时长（12-16小时）、工资低、无保障</td><td>催生工人运动与马克思主义</td></tr>
        <tr><td><strong>童工问题</strong></td><td>儿童在危险环境中工作，无受教育机会</td><td>推动后来劳工立法改革</td></tr>
        <tr><td><strong>城市问题</strong></td><td>住房拥挤、卫生恶劣、传染病流行</td><td>现代城市规划的起源动力</td></tr>
        <tr><td><strong>环境破坏</strong></td><td>煤炭大量燃烧，空气和水源污染</td><td>现代环境问题的历史根源</td></tr>
        <tr><td><strong>殖民扩张</strong></td><td>工业国为掠夺原料和市场加强殖民</td><td>亚非拉地区被卷入资本主义体系</td></tr>
      </table>
    </div>
  </div>

  <!-- Source Analysis -->
  <div class="card">
    <h3>📜 史料研读：工人的声音</h3>
    <div class="source-card">
      <div class="source-label">史料一 · 工人证词（1832年英国议会调查）</div>
      <blockquote>"我6岁就进纺织厂做工，每天工作14小时。车间里煤尘弥漫，机器没有防护罩，我的手指被轧断了两根。但我别无选择——家里需要我赚的每一便士。"<br>——英国纺织厂童工 玛丽·史密斯（时年12岁）</blockquote>
      <div class="source-note">思考：这段史料反映了工业革命时期的什么问题？为什么会出现这些问题？</div>
    </div>
    <div class="source-card">
      <div class="source-label">史料二 · 经济学者的观察</div>
      <blockquote>"工业革命让英国在50年里的生产力增长，超过了此前人类历史的总和。但它也让一代工人在血汗工厂里付出了代价。进步从来不是没有代价的。"<br>——经济史学家 阿诺德·汤因比</blockquote>
      <div class="source-note">思考：汤因比的话体现了评价历史事件的什么原则？你同意他的观点吗？</div>
    </div>
    <button class="toggle-btn" onclick="toggleSolution(this)">显示分析思路 ▼</button>
    <div class="solution">
      <p><strong>史料一分析</strong>：反映了工业革命时期童工问题的严重性——低龄化、超时劳动、危险环境、无劳动保障。根本原因：资本追求利润最大化 + 缺乏劳动立法保护 + 大量贫困家庭需要儿童补贴家用。</p>
      <p><strong>史料二分析</strong>：体现了"历史辩证法"评价原则——既要看到工业革命的巨大进步性（生产力飞跃），也要看到其代价（工人阶级的苦难）。这种"双重评价"是历史思维的核心。</p>
    </div>
  </div>

  <!-- Module 3 Quiz -->
  <div class="quiz-card" data-quiz="m3q1" data-bloom="3">
    <h3>练习：综合分析</h3>
    <div class="quiz-q">下列哪项最能全面评价工业革命？</div>
    <div class="quiz-opts">
      <div class="quiz-opt" data-val="A" onclick="handleQuiz(this,'m3q1','C','A')">A. 工业革命完全是进步的，因为它极大提高了生产力</div>
      <div class="quiz-opt" data-val="B" onclick="handleQuiz(this,'m3q1','C','B')">B. 工业革命完全是负面的，因为它造成了工人苦难</div>
      <div class="quiz-opt" data-val="C" onclick="handleQuiz(this,'m3q1','C','C')">C. 工业革命具有双重性——既推动了生产力飞跃，也造成了社会和环境问题</div>
      <div class="quiz-opt" data-val="D" onclick="handleQuiz(this,'m3q1','C','D')">D. 工业革命的影响被夸大了，其实它没有改变什么</div>
    </div>
    <div class="quiz-feedback" id="fb-m3q1"></div>
  </div>

  <div class="quiz-card" data-quiz="m3q2" data-bloom="4">
    <h3>思考题：迁移应用</h3>
    <div class="quiz-q">今天的人工智能革命，和200年前的工业革命有哪些相似之处？有哪些不同？（开放题，点击查看思考框架）</div>
    <button class="toggle-btn" onclick="toggleSolution(this)">显示思考框架 ▼</button>
    <div class="solution">
      <p><strong>相似点思考方向</strong>：</p>
      <ul style="padding-left:20px;font-size:14px;line-height:1.8">
        <li>都是"通用技术"引发的生产力革命</li>
        <li>都会造成部分职业消失、新职业诞生</li>
        <li>都带来了社会焦虑和制度调整压力</li>
        <li>都有"乐观派"和"悲观派"的争论</li>
      </ul>
      <p><strong>不同点思考方向</strong>：</p>
      <ul style="padding-left:20px;font-size:14px;line-height:1.8">
        <li>工业革命以"动力"解放体力劳动，AI以"智能"解放脑力劳动</li>
        <li>工业革命用了近百年才扩散，AI的扩散速度远快于工业革命</li>
        <li>工业革命造成了明显的社会不平等，AI尚未造成如此明显的物质不平等</li>
      </ul>
    </div>
  </div>
</section>

<!-- ===== Post-test ===== -->
<section class="section" id="posttest">
  <h2 class="section-title">📋 后测：检验学习成果</h2>
  <div class="test-banner">
    <h3>🎯 3道题，看看你学到了多少</h3>
    <p style="font-size:14px;color:var(--text2)">这次会显示正确答案和解析</p>
  </div>

  <div class="quiz-card" data-quiz="post1" data-bloom="2">
    <h3>第1题</h3>
    <div class="quiz-q">工业革命中，使工厂摆脱水力限制、可以建在任何地方的关键发明是？</div>
    <div class="quiz-opts">
      <div class="quiz-opt" data-val="A" onclick="handleQuiz(this,'post1','B','A')">A. 珍妮纺纱机</div>
      <div class="quiz-opt" data-val="B" onclick="handleQuiz(this,'post1','B','B')">B. 改良蒸汽机</div>
      <div class="quiz-opt" data-val="C" onclick="handleQuiz(this,'post1','B','C')">C. 蒸汽火车</div>
      <div class="quiz-opt" data-val="D" onclick="handleQuiz(this,'post1','B','D')">D. 电报</div>
    </div>
    <div class="quiz-feedback" id="fb-post1"></div>
  </div>

  <div class="quiz-card" data-quiz="post2" data-bloom="3">
    <h3>第2题</h3>
    <div class="quiz-q">工业革命后，英国被称为"世界工厂"，这主要是因为？</div>
    <div class="quiz-opts">
      <div class="quiz-opt" data-val="A" onclick="handleQuiz(this,'post2','C','A')">A. 英国生产了全世界最多的农产品</div>
      <div class="quiz-opt" data-val="B" onclick="handleQuiz(this,'post2','C','B')">B. 英国拥有全世界最多的殖民地</div>
      <div class="quiz-opt" data-val="C" onclick="handleQuiz(this,'post2','C','C')">C. 英国工业生产能力遥遥领先，向全世界输出工业品</div>
      <div class="quiz-opt" data-val="D" onclick="handleQuiz(this,'post2','C','D')">D. 英国的工厂数量全世界最多</div>
    </div>
    <div class="quiz-feedback" id="fb-post2"></div>
  </div>

  <div class="quiz-card" data-quiz="post3" data-bloom="4">
    <h3>第3题（开放题）</h3>
    <div class="quiz-q">有历史学家说："工业革命是一把双刃剑。"结合本课所学，谈谈你对这句话的理解（100字以内）。</div>
    <button class="toggle-btn" onclick="toggleSolution(this)">显示参考答案思路 ▼</button>
    <div class="solution">
      <p><strong>参考答案思路</strong>：</p>
      <p>"双刃剑"指工业革命既有巨大进步意义，也有严重负面后果：</p>
      <ul style="padding-left:20px;font-size:14px;line-height:1.8">
        <li><strong>进步性</strong>：生产力飞跃、现代社会阶级形成、城市文明兴起、世界市场建立</li>
        <li><strong>负面性</strong>：工人被剥削（工时长、工资低、无保障）、童工问题严重、城市环境恶化、环境污染开始</li>
        <li><strong>结论</strong>：评价历史事件要全面，不能只看到一面。</li>
      </ul>
    </div>
  </div>
</section>

<!-- ===== Extension ===== -->
<section class="section" id="extension">
  <h2 class="section-title">🚀 拓展学习</h2>
  
  <div class="card">
    <h3>📚 推荐阅读</h3>
    <ul style="padding-left:20px;font-size:14px;line-height:1.9;color:var(--ink)">
      <li>《全球通史》（斯塔夫里阿诺斯）—— 工业革命的世界史意义</li>
      <li>《工业革命：历史反思》（Joel Mokyr）—— 为何英国？深度分析</li>
      <li>《资本论》（马克思）第一卷—— 工厂制度与工人阶级的形成</li>
    </ul>
  </div>

  <div class="card">
    <h3>💬 历史学家的三段箴言</h3>
    <p style="font-size:13px;color:var(--text2);margin-bottom:10px">这三句话浓缩了三种看待工业革命的视角，读懂它们，你就读懂了这段历史的复杂性。</p>
    <div class="source-card" style="border-left:4px solid var(--primary)">
      <div class="source-label">🌍 全球史视角</div>
      <blockquote>"工业革命是人类继农业革命之后最伟大的经济转折点，它彻底改变了地球的面貌与人类的命运。"<br>——斯塔夫里阿诺斯《全球通史》</blockquote>
    </div>
    <div class="source-card" style="border-left:4px solid var(--accent)">
      <div class="source-label">⚙️ 制度经济视角</div>
      <blockquote>"为何是英国？因为这里同时具备了科学传统、产权保护、市场规模和精英的求知精神——其他国家只有其中两三项。"<br>——乔尔·莫基尔《工业革命：历史反思》</blockquote>
    </div>
    <div class="source-card" style="border-left:4px solid var(--danger)">
      <div class="source-label">✊ 阶级批判视角</div>
      <blockquote>"工厂制度的产生，使工人成为机器的附属品。当机器开动时，工人只是它的活体零件。"<br>——马克思《资本论》第一卷（改写）</blockquote>
    </div>
    <p style="font-size:12.5px;color:var(--text2);margin-top:10px;font-style:italic">💡 课堂讨论：这三位学者从不同立场出发，但都在说同一件事——<strong>工业革命既是进步也是痛苦</strong>。你的答案会更倾向哪一位的视角？为什么？</p>
  </div>

  <div class="card">
    <h3>🤔 延伸思考</h3>
    <ul style="padding-left:20px;font-size:14px;line-height:1.9;color:var(--ink)">
      <li>工业革命的技术创新模式，对今天的中国"创新驱动发展"有什么启示？</li>
      <li>工业革命时期的环境污染，和今天的气候变化问题有什么相似之处？</li>
      <li>如果工业革命首先发生在中国，历史会怎样不同？（反事实思考）</li>
    </ul>
  </div>
</section>

</div><!-- /container -->

<!-- Footer -->
<footer class="footer">
  <p>TeachAny v5.34.8 · 工业革命课件 · 初中历史（初二·初三）</p>
  <p style="margin-top:6px">遵循 <a href="#" style="color:var(--primary)">TeachAny 课件规范</a> 制作 · 互动课件 · 史料研读 · 多元评价</p>
</footer>

<!-- AI Tutor CSS (loaded from ai-tutor.css) -->
<!-- AI Tutor JS (loaded from ai-tutor.js) -->
<script src="./ai-tutor.js"></script>

<!-- Main Script -->
<script>
// ========== Quiz Handler ==========
function handleQuiz(el, quizId, answer, selected) {
  const card = el.closest('.quiz-card');
  const opts = card.querySelectorAll('.quiz-opt');
  const fb = document.getElementById('fb-' + quizId);
  
  opts.forEach(o => {
    o.classList.remove('selected','correct','wrong');
  });
  el.classList.add('selected');
  
  if (selected === answer) {
    el.classList.add('correct');
    fb.className = 'quiz-feedback fb-correct show';
    fb.innerHTML = getCorrectFeedback(quizId);
  } else {
    el.classList.add('wrong');
    // Also show the correct answer
    opts.forEach(o => {
      if (o.dataset.val === answer) o.classList.add('correct');
    });
    fb.className = 'quiz-feedback fb-wrong show';
    fb.innerHTML = getWrongFeedback(quizId, selected, answer);
  }
}

function getCorrectFeedback(qid) {
  const map = {
    'pre1': '✅ 正确！工业革命始于18世纪60年代（1760s），以珍妮纺纱机的发明为标志之一。',
    'pre2': '✅ 正确！英国是工业革命的发源地，随后才扩散到欧洲大陆和北美。',
    'pre3': '✅ 正确！瓦特改良的蒸汽机是工业革命的核心动力发明，被称为"万能动力机"。',
    'm1q1': '✅ 正确！圈地运动使大量农民失去土地，成为自由劳动者，为工厂提供了充足劳动力。',
    'm2q1': '✅ 正确！瓦特在1769年取得改良蒸汽机的专利，1785年首次应用于棉纺织厂。',
    'm3q1': '✅ 正确！工业革命具有双重性——既推动了巨大进步，也带来了严重问题。这是历史评价的基本原则。',
    'post1': '✅ 正确！改良蒸汽机使工厂不再依赖水力，可以建在任何有煤炭的地方，这是工业革命的关键突破。',
    'post2': '✅ 正确！"世界工厂"指英国工业生产能力全球第一，其产品行销全世界。'
  };
  return map[qid] || '✅ 答对了！';
}

function getWrongFeedback(qid, selected, answer) {
  const map = {
    'pre1': '❌ 不对。工业革命始于18世纪60年代（1760s），不是15世纪（文艺复兴）也不是19世纪中期（工业革命基本完成）。',
    'pre2': '❌ 不对。英国是工业革命的发源地。法国、德国、美国的工业革命都晚于英国。',
    'pre3': '❌ 不对。蒸汽机是工业革命的核心动力。发电机和内燃机是第二次工业革命的发明，计算机是20世纪的发明。',
    'm1q1': '❌ 不对。圈地运动的主要作用是使农民失去土地，成为自由劳动力。资本积累主要来自殖民贸易。',
    'm2q1': '❌ 不对。哈格里夫斯发明珍妮纺纱机，史蒂芬森发明蒸汽火车，富尔顿发明蒸汽轮船。',
    'm3q1': '❌ 不对。评价历史事件要坚持"双重性"原则，不能只看到进步性或只看到负面。',
    'post1': '❌ 不对。改良蒸汽机提供了万能动力，使工厂可以建在任何地方。珍妮纺纱机只是纺织业的改进。',
    'post2': '❌ 不对。"世界工厂"强调的是工业生产能力，不是殖民地数量。'
  };
  return map[qid] || '❌ 答错了，再想想看！';
}

// ========== Invention Detail ==========
const inventionData = {
  'spin': {
    title: '🧵 珍妮纺纱机（1765年）',
    inventor: '詹姆斯·哈格里夫斯',
    detail: '珍妮纺纱机是工业革命的起点发明之一。它用一台机器同时纺出多根纱线（最初8根，后来发展到80根），使纺纱效率提高了8-80倍。这直接导致了棉纺织品价格大幅下降，市场需求暴涨，进而推动了整个纺织业的技术革新。<br><br><strong>历史意义</strong>：标志着工厂制度的萌芽，是"从工具到机器"的关键一步。'
  },
  'steam': {
    title: '⚙️ 改良蒸汽机（1785年）',
    inventor: '詹姆斯·瓦特',
    detail: '瓦特并不是蒸汽机的发明者（纽科门蒸汽机更早），但他的改良（分离冷凝器、双动式、离心调速器）使蒸汽机的效率提高了3倍，耗煤量降低了60%。这使蒸汽机可以应用于纺织、冶金、交通等各行各业，成为"万能动力机"。<br><br><strong>历史意义</strong>：使工厂摆脱了对水力的依赖，可以建在任何地方。人类从此进入了"蒸汽时代"。'
  },
  'iron': {
    title: '🏗️ 焦炭炼铁法（1709年）',
    inventor: '亚伯拉罕·达比',
    detail: '用焦炭（煤炭干馏产物）代替木炭炼铁，使生铁产量大幅提高、成本大幅下降。这为后来的机器制造、铁路建设提供了充足的廉价钢铁。<br><br><strong>历史意义</strong>：这是工业革命最早的关键技术突破之一，比珍妮纺纱机早了56年。没有焦炭炼铁法，就没有足够的钢铁来建造机器和铁路。'
  },
  'train': {
    title: '🚂 蒸汽火车（1825年）',
    inventor: '乔治·史蒂芬森',
    detail: '1825年，史蒂芬森建造的"旅行者号"机车在英国斯托克顿—达灵顿铁路上运行，这是世界上第一条公共铁路。铁路运输使大宗货物的远距离运输成本下降了90%以上，彻底改变了物流格局。<br><br><strong>历史意义</strong>：铁路是工业革命的"血管"，它把原材料、产品和人口快速连接起来，加速了城市化和全国统一市场的形成。'
  },
  'telegraph': {
    title: '📡 电报（1837年）',
    inventor: '塞缪尔·莫尔斯',
    detail: '莫尔斯发明电报和莫尔斯电码，使信息可以在几秒钟内跨越千里。在此之前，信息传递速度和马车一样快（每天约100公里）。电报的出现，使全球即时通讯成为可能。<br><br><strong>历史意义</strong>：电报是"信息时代"的先驱。它缩短了世界的时间距离，为后来的电话、互联网奠定了基础。'
  }
};

function showDetail(key) {
  const data = inventionData[key];
  const panel = document.getElementById('detailPanel');
  if (!data) return;
  panel.innerHTML = '<h4 style="margin-bottom:8px;color:var(--primary)">' + data.title + '</h4>' +
    '<p style="font-size:13px;color:var(--text2);margin-bottom:6px">发明者：' + data.inventor + '</p>' +
    '<p>' + data.detail + '</p>';
  panel.classList.add('show');
  panel.scrollIntoView({behavior:'smooth',block:'nearest'});
}

// ========== Timeline Drag Sort ==========
let dragOrder = [0, 1, 2, 4, 3]; // correct order: 1709, 1765, 1785, 1825, 1837 → indices: 2, 0, 1, 4, 3

function initDrag() {
  const container = document.getElementById('dragContainer');
  const items = Array.from(container.children);
  // Shuffle
  for (let i = items.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    container.appendChild(items[j]);
    items.splice(j, 1);
  }
}

function checkTimeline() {
  const container = document.getElementById('dragContainer');
  const items = Array.from(container.children);
  const correct = [2, 0, 1, 3, 4]; // correct DOM order: coke-iron, jenny, steam, train, telegraph
  let allCorrect = true;
  
  items.forEach((item, idx) => {
    const expectedIdx = correct[idx];
    // Check if this item's data-order matches expected
    const actualOrder = parseInt(item.dataset.order);
    item.classList.remove('correct-pos','wrong-pos');
    if (actualOrder === expectedIdx) {
      item.classList.add('correct-pos');
    } else {
      item.classList.add('wrong-pos');
      allCorrect = false;
    }
  });
  
  const result = document.getElementById('timelineResult');
  result.style.display = 'block';
  if (allCorrect) {
    result.innerHTML = '<span style="color:var(--success);font-weight:600">✅ 完全正确！你已掌握工业革命关键发明的时间顺序。</span>';
  } else {
    result.innerHTML = '<span style="color:var(--danger)">❌ 还有错误，请重新拖拽排列。提示：正确顺序是焦炭炼铁（1709）→ 珍妮纺纱机（1765）→ 改良蒸汽机（1785）→ 蒸汽火车（1825）→ 电报（1837）</span>';
  }
}

// Make items draggable
function setupDrag() {
  const container = document.getElementById('dragContainer');
  let dragItem = null;
  
  container.querySelectorAll('.drag-item').forEach(item => {
    item.addEventListener('dragstart', function(e) {
      dragItem = this;
      setTimeout(() => this.style.opacity = '0.4', 0);
    });
    item.addEventListener('dragend', function() {
      setTimeout(() => this.style.opacity = '1', 0);
      dragItem = null;
    });
    item.addEventListener('dragover', function(e) {
      e.preventDefault();
    });
    item.addEventListener('drop', function(e) {
      e.preventDefault();
      if (dragItem && dragItem !== this) {
        const items = Array.from(container.children);
        const fromIdx = items.indexOf(dragItem);
        const toIdx = items.indexOf(this);
        if (fromIdx < toIdx) {
          container.insertBefore(dragItem, this.nextSibling);
        } else {
          container.insertBefore(dragItem, this);
        }
      }
    });
  });
}

// ========== Toggle Solution ==========
function toggleSolution(btn) {
  const sol = btn.nextElementSibling;
  if (sol.classList.contains('open')) {
    sol.classList.remove('open');
    btn.textContent = btn.textContent.replace('▲','▼');
  } else {
    sol.classList.add('open');
    btn.textContent = btn.textContent.replace('▼','▲');
  }
}

// ========== Progress Bar ==========
function updateProgress() {
  const winH = window.innerHeight;
  const docH = document.documentElement.scrollHeight - winH;
  const scrolled = window.scrollY;
  const pct = docH > 0 ? (scrolled / docH) * 100 : 0;
  document.getElementById('progressBar').style.width = pct + '%';
}
window.addEventListener('scroll', updateProgress);

// ========== Nav Active State ==========
const sections = document.querySelectorAll('.section');
const navLinks = document.querySelectorAll('.nav-bar a');
window.addEventListener('scroll', () => {
  let current = '';
  sections.forEach(sec => {
    const top = sec.offsetTop - 80;
    if (window.scrollY >= top) current = sec.id;
  });
  navLinks.forEach(a => {
    a.classList.remove('active');
    if (a.getAttribute('href') === '#' + current) a.classList.add('active');
  });
});

// ========== Init ==========
setupDrag();

// ========== Steam Engine Canvas Animation ==========
(function initSteamEngine() {
  const canvas = document.getElementById('steamCanvas');
  if (!canvas) return;
  const ctx = canvas.getContext('2d');
  let running = false;
  let angle = 0; // 飞轮旋转角度
  let speedMode = 1; // 0=slow, 1=normal, 2=fast
  const speedMap = [0.015, 0.035, 0.08];
  const speedNames = ['慢速', '正常', '快速'];
  const rpmMap = [14, 34, 76];
  const effMap = [8, 18, 28];

  function draw() {
    const W = canvas.width, H = canvas.height;
    ctx.clearRect(0, 0, W, H);

    // 背景：工厂内景
    const grad = ctx.createLinearGradient(0, 0, 0, H);
    grad.addColorStop(0, '#fef9c3');
    grad.addColorStop(1, '#fde68a');
    ctx.fillStyle = grad;
    ctx.fillRect(0, 0, W, H);

    // ==== 锅炉（左侧）====
    ctx.fillStyle = '#78350f';
    ctx.fillRect(40, 120, 120, 160);
    ctx.fillStyle = '#1f2937';
    ctx.fillRect(40, 260, 120, 20);
    // 锅炉标签
    ctx.fillStyle = '#fff';
    ctx.font = 'bold 13px sans-serif';
    ctx.textAlign = 'center';
    ctx.fillText('锅炉', 100, 200);

    // 煤火（红色动画）
    const fireFlicker = running ? Math.sin(angle * 8) * 0.2 + 0.8 : 0.5;
    ctx.fillStyle = `rgba(220,38,38,${fireFlicker})`;
    ctx.fillRect(55, 240, 90, 30);
    ctx.fillStyle = `rgba(249,115,22,${fireFlicker * 0.8})`;
    for (let i = 0; i < 5; i++) {
      const x = 65 + i * 18;
      const flameH = running ? 10 + Math.sin(angle * 6 + i) * 6 : 4;
      ctx.beginPath();
      ctx.moveTo(x, 240);
      ctx.quadraticCurveTo(x + 4, 230 - flameH, x + 9, 240);
      ctx.fill();
    }
    ctx.fillStyle = '#7f1d1d';
    ctx.font = '10px sans-serif';
    ctx.fillText('🔥 煤', 100, 292);

    // 烟囱 + 烟雾
    ctx.fillStyle = '#78350f';
    ctx.fillRect(88, 60, 24, 60);
    if (running) {
      for (let i = 0; i < 4; i++) {
        const sx = 100 + Math.sin(angle * 2 + i) * 12;
        const sy = 50 - i * 14 - (angle * 20 % 40);
        const sr = 10 + i * 3;
        ctx.fillStyle = `rgba(100,116,139,${0.4 - i * 0.08})`;
        ctx.beginPath();
        ctx.arc(sx, sy, sr, 0, Math.PI * 2);
        ctx.fill();
      }
    }

    // ==== 蒸汽管（锅炉 → 气缸，蓝色流动）====
    ctx.strokeStyle = '#06b6d4';
    ctx.lineWidth = 8;
    ctx.beginPath();
    ctx.moveTo(160, 150);
    ctx.lineTo(260, 150);
    ctx.stroke();
    // 蒸汽流动粒子
    if (running) {
      for (let i = 0; i < 6; i++) {
        const px = 160 + ((angle * 60 + i * 18) % 100);
        ctx.fillStyle = 'rgba(6,182,212,0.8)';
        ctx.beginPath();
        ctx.arc(px, 150, 3, 0, Math.PI * 2);
        ctx.fill();
      }
    }

    // ==== 气缸 + 活塞（中部，黄色做功）====
    ctx.fillStyle = '#475569';
    ctx.fillRect(260, 130, 120, 40);
    // 活塞位置（随 angle 往复）
    const pistonX = 270 + (Math.sin(angle) * 0.5 + 0.5) * 80;
    ctx.fillStyle = '#f59e0b';
    ctx.fillRect(pistonX, 135, 20, 30);
    // 活塞杆（连接到飞轮）
    ctx.strokeStyle = '#1e293b';
    ctx.lineWidth = 4;
    ctx.beginPath();
    ctx.moveTo(pistonX + 20, 150);
    // 连杆到飞轮上的偏心轮点
    const flyX = 490, flyY = 180;
    const crankX = flyX + Math.cos(angle) * 35;
    const crankY = flyY + Math.sin(angle) * 35;
    ctx.lineTo(crankX, crankY);
    ctx.stroke();
    ctx.fillStyle = '#fff';
    ctx.font = 'bold 11px sans-serif';
    ctx.fillText('气缸', 320, 155);

    // ==== 飞轮（右侧，绿色）====
    ctx.save();
    ctx.translate(flyX, flyY);
    ctx.rotate(angle);
    // 外圈
    ctx.fillStyle = '#10b981';
    ctx.beginPath();
    ctx.arc(0, 0, 55, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = '#065f46';
    ctx.beginPath();
    ctx.arc(0, 0, 48, 0, Math.PI * 2);
    ctx.fill();
    // 辐条
    ctx.strokeStyle = '#10b981';
    ctx.lineWidth = 5;
    for (let i = 0; i < 6; i++) {
      ctx.beginPath();
      ctx.moveTo(0, 0);
      const a = (i / 6) * Math.PI * 2;
      ctx.lineTo(Math.cos(a) * 45, Math.sin(a) * 45);
      ctx.stroke();
    }
    // 中心
    ctx.fillStyle = '#1f2937';
    ctx.beginPath();
    ctx.arc(0, 0, 10, 0, Math.PI * 2);
    ctx.fill();
    // 偏心轮点（连杆接入处）
    ctx.translate(0, 0);
    ctx.restore();
    // 偏心轮点（绝对坐标）
    ctx.fillStyle = '#dc2626';
    ctx.beginPath();
    ctx.arc(crankX, crankY, 5, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = '#065f46';
    ctx.font = 'bold 12px sans-serif';
    ctx.textAlign = 'center';
    ctx.fillText('飞轮', flyX, flyY + 80);
    ctx.font = '10px sans-serif';
    ctx.fillStyle = '#475569';
    ctx.fillText('（输出动力）', flyX, flyY + 95);

    // 标题
    ctx.fillStyle = '#78350f';
    ctx.font = 'bold 14px sans-serif';
    ctx.textAlign = 'left';
    ctx.fillText('⚙️ 瓦特改良蒸汽机工作原理', 12, 22);

    // 能量流示意
    if (running) {
      ctx.fillStyle = 'rgba(120,53,15,0.6)';
      ctx.font = '10px sans-serif';
      ctx.fillText('煤 → 热 → 蒸汽 → 机械功', 12, 40);
    }
  }

  function tick() {
    if (running) {
      angle += speedMap[speedMode];
    }
    draw();
    requestAnimationFrame(tick);
  }

  window.toggleSteam = function () {
    running = !running;
    document.getElementById('steamBtn').textContent = running ? '⏸️ 暂停动画' : '▶️ 启动动画';
    document.getElementById('rpmDisplay').textContent = running ? rpmMap[speedMode] : 0;
    document.getElementById('effDisplay').textContent = running ? effMap[speedMode] : 0;
  };

  window.changeSpeed = function () {
    speedMode = (speedMode + 1) % 3;
    document.getElementById('speedBtn').textContent = '速度：' + speedNames[speedMode];
    if (running) {
      document.getElementById('rpmDisplay').textContent = rpmMap[speedMode];
      document.getElementById('effDisplay').textContent = effMap[speedMode];
    }
  };

  tick(); // 启动渲染循环，动画内部判断是否 running
})();

// ========== Cotton Consumption Chart (1750-1860) ==========
(function initCottonChart() {
  const canvas = document.getElementById('cottonChart');
  if (!canvas) return;
  const ctx = canvas.getContext('2d');
  const W = canvas.width, H = canvas.height;

  // 英国原棉消耗量（百万磅），来源 Deane & Cole / Mitchell
  // [年份, 消耗量(百万磅), 事件id]
  const DATA = [
    { year: 1750, value: 3,   event: null },
    { year: 1760, value: 4,   event: null },
    { year: 1770, value: 5,   event: 'jenny' },
    { year: 1780, value: 7,   event: null },
    { year: 1790, value: 31,  event: 'watt' },
    { year: 1800, value: 56,  event: 'power-loom' },
    { year: 1810, value: 124, event: null },
    { year: 1820, value: 152, event: null },
    { year: 1830, value: 248, event: 'railway' },
    { year: 1833, value: 280, event: 'factory-act' },
    { year: 1840, value: 459, event: null },
    { year: 1850, value: 588, event: 'great-exhib' },
    { year: 1860, value: 1050,event: null }
  ];

  // 事件详情（点击后弹出）
  const EVENTS = {
    jenny: {
      color: '#dc2626', year: 1770, title: '🧵 珍妮纺纱机普及',
      body: '哈格里夫斯的珍妮纺纱机（1765）开始大规模使用，一个工人可以同时操作 8—16 个纱锭，纺纱效率翻了十几倍。注意看曲线——真正的陡峭增长还没开始，因为<strong>还缺一个通用动力</strong>。'
    },
    watt: {
      color: '#dc2626', year: 1790, title: '⚙️ 瓦特蒸汽机全面商用',
      body: '1781 年瓦特解决了旋转输出问题后，蒸汽机大规模进入棉纺厂。<strong>1770—1790 年短短 20 年，棉花消耗从 5 百万磅涨到 31 百万磅</strong>——这是工业革命真正的"引爆点"。'
    },
    'power-loom': {
      color: '#dc2626', year: 1800, title: '🪡 蒸汽织布机（Power Loom）',
      body: '卡特赖特发明的蒸汽织布机让"织"也跟上了"纺"的速度。从此纺和织都摆脱了手工——<strong>工厂成为生产的主导形式</strong>，传统家庭手工业迅速衰落。'
    },
    railway: {
      color: '#059669', year: 1830, title: '🚂 利物浦-曼彻斯特铁路通车',
      body: '世界上第一条客货两运的蒸汽铁路。<strong>原棉从利物浦港到曼彻斯特工厂的时间从 36 小时缩短到 1.5 小时</strong>，物流效率革命直接推动棉产量再次跳跃。'
    },
    'factory-act': {
      color: '#7c3aed', year: 1833, title: '📜《工厂法》首次限制童工',
      body: '规定 9 岁以下禁止进厂、9—13 岁每天不超过 9 小时。这是<strong>国家第一次以法律形式干预资本主义生产</strong>，但你看曲线——法案之后棉花消耗并没有下降，反而继续飞涨，因为机器和成年工仍在加班。'
    },
    'great-exhib': {
      color: '#059669', year: 1850, title: '🏛️ 伦敦世博会·"世界工厂"确立',
      body: '1851 年伦敦举办人类历史上第一届世界博览会（水晶宫），英国向全世界展示其工业霸权。<strong>此时英国的棉产量占全世界 50%</strong>，"世界工厂"名号名副其实。'
    }
  };

  // ==== 坐标映射 ====
  const PAD = { l: 60, r: 30, t: 30, b: 50 };
  const xMin = 1750, xMax = 1860;
  const yMin = 0, yMax = 1100;
  const plotW = W - PAD.l - PAD.r;
  const plotH = H - PAD.t - PAD.b;

  function xScale(year) { return PAD.l + ((year - xMin) / (xMax - xMin)) * plotW; }
  function yScale(value) { return PAD.t + plotH - (value / yMax) * plotH; }

  // 事件节点的位置
  const eventPoints = DATA.filter(d => d.event).map(d => ({
    x: xScale(d.year), y: yScale(d.value),
    year: d.year, value: d.value, event: d.event,
    color: EVENTS[d.event].color
  }));

  let animProgress = 0; // 0 → 1 绘制进度
  let animStarted = false;
  let hoveredEvent = null;

  function draw() {
    ctx.clearRect(0, 0, W, H);

    // 背景网格
    ctx.strokeStyle = '#e5e7eb';
    ctx.lineWidth = 1;
    // 水平网格（y 轴）
    const yTicks = [0, 200, 400, 600, 800, 1000];
    yTicks.forEach(v => {
      const y = yScale(v);
      ctx.beginPath(); ctx.moveTo(PAD.l, y); ctx.lineTo(W - PAD.r, y); ctx.stroke();
    });
    // 垂直网格（x 轴，每 10 年）
    for (let yr = 1750; yr <= 1860; yr += 20) {
      const x = xScale(yr);
      ctx.beginPath(); ctx.moveTo(x, PAD.t); ctx.lineTo(x, H - PAD.b); ctx.stroke();
    }

    // Y 轴标签
    ctx.fillStyle = '#475569';
    ctx.font = '11px sans-serif';
    ctx.textAlign = 'right';
    ctx.textBaseline = 'middle';
    yTicks.forEach(v => {
      ctx.fillText(v === 0 ? '0' : v + 'M', PAD.l - 8, yScale(v));
    });
    // X 轴标签
    ctx.textAlign = 'center';
    ctx.textBaseline = 'top';
    for (let yr = 1750; yr <= 1860; yr += 20) {
      ctx.fillText(String(yr), xScale(yr), H - PAD.b + 8);
    }

    // 坐标轴
    ctx.strokeStyle = '#64748b'; ctx.lineWidth = 1.5;
    ctx.beginPath();
    ctx.moveTo(PAD.l, PAD.t); ctx.lineTo(PAD.l, H - PAD.b); ctx.lineTo(W - PAD.r, H - PAD.b);
    ctx.stroke();

    // 轴标题
    ctx.fillStyle = '#1e293b';
    ctx.font = 'bold 12px sans-serif';
    ctx.textAlign = 'left';
    ctx.fillText('单位：百万磅', PAD.l - 10, PAD.t - 14);
    ctx.textAlign = 'right';
    ctx.fillText('年份 →', W - PAD.r, H - 8);

    // 标题
    ctx.font = 'bold 14px sans-serif';
    ctx.textAlign = 'left';
    ctx.fillStyle = '#0f172a';
    ctx.fillText('英国原棉年消耗量 1750—1860', PAD.l, 18);

    // ==== 绘制曲线（带动画渐进）====
    const cutoffIdx = Math.floor(DATA.length * animProgress);
    const partialProgress = (DATA.length * animProgress) - cutoffIdx;

    // 面积填充
    if (cutoffIdx > 0) {
      ctx.fillStyle = 'rgba(59,130,246,0.15)';
      ctx.beginPath();
      ctx.moveTo(xScale(DATA[0].year), H - PAD.b);
      for (let i = 0; i < cutoffIdx; i++) {
        ctx.lineTo(xScale(DATA[i].year), yScale(DATA[i].value));
      }
      if (cutoffIdx < DATA.length) {
        const a = DATA[cutoffIdx - 1], b = DATA[cutoffIdx];
        const px = xScale(a.year) + (xScale(b.year) - xScale(a.year)) * partialProgress;
        const py = yScale(a.value) + (yScale(b.value) - yScale(a.value)) * partialProgress;
        ctx.lineTo(px, py);
        ctx.lineTo(px, H - PAD.b);
      } else {
        ctx.lineTo(xScale(DATA[cutoffIdx - 1].year), H - PAD.b);
      }
      ctx.closePath(); ctx.fill();
    }

    // 曲线本身
    ctx.strokeStyle = '#3b82f6';
    ctx.lineWidth = 3;
    ctx.lineJoin = 'round';
    ctx.beginPath();
    DATA.forEach((d, i) => {
      if (i >= cutoffIdx) return;
      const x = xScale(d.year), y = yScale(d.value);
      if (i === 0) ctx.moveTo(x, y);
      else ctx.lineTo(x, y);
    });
    if (cutoffIdx > 0 && cutoffIdx < DATA.length) {
      const a = DATA[cutoffIdx - 1], b = DATA[cutoffIdx];
      const px = xScale(a.year) + (xScale(b.year) - xScale(a.year)) * partialProgress;
      const py = yScale(a.value) + (yScale(b.value) - yScale(a.value)) * partialProgress;
      ctx.lineTo(px, py);
    }
    ctx.stroke();

    // 数据点小圆
    DATA.forEach((d, i) => {
      if (i >= cutoffIdx) return;
      ctx.fillStyle = '#2563eb';
      ctx.beginPath();
      ctx.arc(xScale(d.year), yScale(d.value), 3, 0, Math.PI * 2);
      ctx.fill();
    });

    // ==== 事件节点（彩色大圆）====
    if (animProgress >= 1) {
      eventPoints.forEach(pt => {
        const isHover = hoveredEvent === pt.event;
        ctx.fillStyle = pt.color;
        ctx.strokeStyle = '#fff';
        ctx.lineWidth = 2;
        ctx.beginPath();
        ctx.arc(pt.x, pt.y, isHover ? 10 : 7, 0, Math.PI * 2);
        ctx.fill(); ctx.stroke();
        // 脉冲光晕
        if (isHover) {
          ctx.strokeStyle = pt.color;
          ctx.lineWidth = 2;
          ctx.globalAlpha = 0.4;
          ctx.beginPath();
          ctx.arc(pt.x, pt.y, 14, 0, Math.PI * 2);
          ctx.stroke();
          ctx.globalAlpha = 1;
        }
        // 年份标签
        ctx.fillStyle = pt.color;
        ctx.font = 'bold 10px sans-serif';
        ctx.textAlign = 'center';
        ctx.textBaseline = 'bottom';
        ctx.fillText(String(pt.year), pt.x, pt.y - 11);
      });
    }

    // 最终值标注
    if (animProgress >= 1) {
      const last = DATA[DATA.length - 1];
      ctx.fillStyle = '#1e293b';
      ctx.font = 'bold 12px sans-serif';
      ctx.textAlign = 'right';
      ctx.textBaseline = 'bottom';
      ctx.fillText(`1860 年 ≈ 10.5 亿磅`, xScale(last.year) - 4, yScale(last.value) - 8);
    }
  }

  function animate() {
    if (animProgress < 1) {
      animProgress += 0.012;
      if (animProgress > 1) animProgress = 1;
      draw();
      requestAnimationFrame(animate);
    } else {
      draw();
    }
  }

  // 首次进入视口时启动动画（性能友好）
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting && !animStarted) {
        animStarted = true;
        animate();
      }
    });
  }, { threshold: 0.3 });
  observer.observe(canvas);
  draw(); // 立即画个空坐标系

  // 点击检测
  canvas.addEventListener('click', (e) => {
    const rect = canvas.getBoundingClientRect();
    const scaleX = W / rect.width, scaleY = H / rect.height;
    const cx = (e.clientX - rect.left) * scaleX;
    const cy = (e.clientY - rect.top) * scaleY;
    for (const pt of eventPoints) {
      const dx = cx - pt.x, dy = cy - pt.y;
      if (dx * dx + dy * dy < 12 * 12) {
        showEvent(pt.event);
        return;
      }
    }
  });

  // 悬停反馈
  canvas.addEventListener('mousemove', (e) => {
    const rect = canvas.getBoundingClientRect();
    const scaleX = W / rect.width, scaleY = H / rect.height;
    const cx = (e.clientX - rect.left) * scaleX;
    const cy = (e.clientY - rect.top) * scaleY;
    let hit = null;
    for (const pt of eventPoints) {
      const dx = cx - pt.x, dy = cy - pt.y;
      if (dx * dx + dy * dy < 12 * 12) { hit = pt.event; break; }
    }
    if (hit !== hoveredEvent) {
      hoveredEvent = hit;
      canvas.style.cursor = hit ? 'pointer' : 'crosshair';
      draw();
    }
  });

  function showEvent(key) {
    const ev = EVENTS[key];
    if (!ev) return;
    const box = document.getElementById('chartFact');
    box.innerHTML = `
      <h5><span class="cf-year">${ev.year}</span> ${ev.title}</h5>
      <p>${ev.body}</p>
    `;
  }
})();

// ========== Interactive UK Map (5 factors) ==========
const FACTOR_DATA = {
  coal: {
    tag: 't-coal', emoji: '⛏️', title: '煤 · 铁：藏在地下的"工业燃料"',
    body: '英国北部（纽卡斯尔、约克郡）有欧洲最大最浅的煤田，同时伴生铁矿。煤是蒸汽机的燃料，铁是机器的骨架——<strong>两种资源在同一地区、同一煤层</strong>，这是其他欧洲国家没有的地理奇迹。18 世纪初英国煤产量已占全欧洲一半。',
    tip: '💡 思考：如果煤矿在阿尔卑斯山，蒸汽机发展会怎样？'
  },
  enclosure: {
    tag: 't-enclosure', emoji: '🌾', title: '圈地运动：把农民"推进"城市',
    body: '16—18 世纪，英国贵族把公共土地围起来改养绵羊（因为羊毛比粮食值钱），失地农民只能进城找工厂工作。<strong>这不是"人道"政策，但它给工业革命准备了一支庞大的自由劳动力队伍</strong>——这正是刚才练习题的答案。',
    tip: '💡 连锁反应：无地 → 进城 → 进厂 → 工资劳动者'
  },
  manchester: {
    tag: 't-manchester', emoji: '🏭', title: '曼彻斯特：世界上第一座工业城市',
    body: '1750 年曼彻斯特只有 1.8 万人，到 1851 年暴涨到 30 万。它靠近兰开夏煤田、利物浦港口、又有阴雨气候（纺棉花不容易断线），成为全球棉纺织业中心。<strong>"曼彻斯特 + 利物浦"这对组合，是工业革命的原点</strong>。',
    tip: '💡 看前面那张曼彻斯特远景图——烟囱就是这里冒出来的'
  },
  colonies: {
    tag: 't-colonies', emoji: '⚓', title: '殖民地与港口：原料进，商品出',
    body: '英国 18 世纪已是海上霸主，从印度、美洲、非洲源源不断运来<strong>棉花、糖、茶</strong>，又把英国工厂的布匹卖到全世界。伦敦港、利物浦港是当时世界最繁忙的港口。没有殖民地作为"原料来源+商品市场"，工厂生产的东西卖给谁？',
    tip: '💡 这是历史的阴暗面：工业革命部分建立在殖民掠夺之上'
  },
  science: {
    tag: 't-science', emoji: '🔬', title: '皇家学会 + 稳定政治：发明家的保护伞',
    body: '1660 年成立的<strong>皇家学会</strong>是全欧洲第一个现代科学组织，瓦特、牛顿都与它深度相关。而 1688 年"光荣革命"确立了<strong>君主立宪制</strong>，财产权受法律保护——这意味着发明家可以放心投资、工厂主不怕被国王没收财产。<strong>这是法国、俄国当时都做不到的制度优势</strong>。',
    tip: '💡 法国也有聪明的发明家，但拿破仑战争一打，全乱套了'
  }
};

const visitedFactors = new Set();
function showFactor(key) {
  const d = FACTOR_DATA[key];
  if (!d) return;
  visitedFactors.add(key);
  // mark visited
  document.querySelectorAll('.hotspot').forEach(h => {
    if (visitedFactors.has(h.dataset.factor)) h.classList.add('visited');
  });
  const cc = document.getElementById('clickedCount');
  if (cc) cc.textContent = visitedFactors.size;
  // render detail
  const box = document.getElementById('factorDetail');
  box.innerHTML = `
    <span class="fd-tag ${d.tag}">要素 ${['coal','enclosure','manchester','colonies','science'].indexOf(key)+1} / 5</span>
    <h4>${d.emoji} ${d.title}</h4>
    <p>${d.body}</p>
    <p style="color:var(--accent);font-size:12.5px;border-top:1px dashed var(--border);padding-top:8px;margin-top:10px">${d.tip}</p>
    ${visitedFactors.size === 5 ? '<p style="background:rgba(16,185,129,0.08);border-radius:8px;padding:10px 12px;color:var(--success);font-weight:600;margin-top:10px">🎉 五要素已集齐！现在你可以回答："工业革命为什么始于英国？"——因为这 5 个条件同时在这里具备。</p>' : ''}
  `;
}
</script>

<!-- ============================================================ -->
<!-- AI Tutor Config (v5.34 硬规则 #45)                          -->
<!-- ============================================================ -->
<script>
window.__TEACHANY_TUTOR_CONFIG__ = {
  courseTitle: '工业革命',
  subject: 'history',
  grade: 9,
  learningObjectives: [
    '说出工业革命的起止时间和首发国家',
    '列举 5 项关键发明及其发明者和年代',
    '解释工业革命为何始于英国（5 大因素）',
    '分析工业革命对社会和环境的双重影响',
    '通过史料阅读培养历史证据意识'
  ],
  getContext: function () {
    // 取当前视口中心附近的 section 文本作为上下文
    var sections = document.querySelectorAll('section.section');
    var y = window.scrollY + window.innerHeight / 2;
    var best = null, bestDist = Infinity;
    sections.forEach(function (s) {
      var d = Math.abs((s.offsetTop + s.offsetHeight / 2) - y);
      if (d < bestDist) { bestDist = d; best = s; }
    });
    if (best && best.innerText) return best.innerText.slice(0, 3000);
    return '';
  }
};
</script>

<!-- ============================================================ -->
<!-- AUDIO NARRATION (L3) — 8 段中文旁白，对应 8 个 section          -->
<!-- ============================================================ -->
<script>
const AUDIO_BASE = 'tts/';
const AUDIO_SEGMENTS = [
  { id: 's01-intro',       label: '课程导入',   anchor: 'intro',      text: '从 1760 年一件衬衫的价格说起——工业革命如何彻底改变了人类生活。' },
  { id: 's02-objectives',  label: '学习目标',   anchor: 'objectives', text: '本节课你应该能做到的 5 件事。' },
  { id: 's03-pretest',     label: '前测',       anchor: 'pretest',    text: '先做个小测试，看看你大脑里已经装了什么。' },
  { id: 's04-module1',     label: '为何英国',   anchor: 'module1',    text: '五个条件拼图：煤铁、殖民地、自由劳动力、稳定政治、科学积累。' },
  { id: 's05-module2',     label: '关键发明',   anchor: 'module2',    text: '飞梭、珍妮机、瓦特蒸汽机——看图了解那台改变世界的机器。' },
  { id: 's06-module3',     label: '双重影响',   anchor: 'module3',    text: '财富与代价并存：童工、污染、尊严被重新定价。' },
  { id: 's07-posttest',    label: '后测',       anchor: 'posttest',   text: '做一组题串联刚才学到的知识点。' },
  { id: 's08-extension',   label: '拓展思考',   anchor: 'extension',  text: 'AI 革命与工业革命的相似与不同。' }
];

let audioCurrentSegIdx = 0;
let audioIsPlaying = false;
let audioPanelBuilt = false;
const audioPlayer = document.createElement('audio');
audioPlayer.id = 'mainAudio';
document.body.appendChild(audioPlayer);

function loadAudioSegment(idx) {
  audioCurrentSegIdx = idx;
  const seg = AUDIO_SEGMENTS[idx];
  audioPlayer.src = AUDIO_BASE + seg.id + '.mp3';
  audioPlayer.load();
  updateAudioUI(idx);
  // 双向同步：切段时滚动到对应 section
  const target = document.getElementById(seg.anchor);
  if (target) {
    const y = target.getBoundingClientRect().top + window.scrollY - 60;
    window.scrollTo({ top: y, behavior: 'smooth' });
  }
}

function updateAudioUI(idx) {
  const seg = AUDIO_SEGMENTS[idx];
  const t = document.getElementById('apSegTitle'); if (t) t.textContent = '正在播放：' + seg.label;
  const s = document.getElementById('apSubtitle'); if (s) s.textContent = seg.text || '';
  const f = document.getElementById('apFill'); if (f) f.style.width = '0%';
  const c = document.getElementById('apCurrent'); if (c) c.textContent = '0:00';
  const o = document.getElementById('apTotal'); if (o) o.textContent = '0:00';
  document.querySelectorAll('.seg-btn').forEach((b, i) => b.classList.toggle('active', i === idx));
}

function togglePlayPause() {
  if (!audioPlayer.src) loadAudioSegment(audioCurrentSegIdx);
  if (audioPlayer.paused) { audioPlayer.play().catch(() => {}); audioIsPlaying = true; }
  else { audioPlayer.pause(); audioIsPlaying = false; }
  document.getElementById('apPlay').textContent = audioIsPlaying ? '⏸' : '▶';
  document.getElementById('audioBadge').classList.toggle('playing', audioIsPlaying);
}

function prevSeg() { if (audioCurrentSegIdx > 0) { loadAudioSegment(audioCurrentSegIdx - 1); if (audioIsPlaying) audioPlayer.play().catch(() => {}); } }
function nextSeg() { if (audioCurrentSegIdx < AUDIO_SEGMENTS.length - 1) { loadAudioSegment(audioCurrentSegIdx + 1); if (audioIsPlaying) audioPlayer.play().catch(() => {}); } }
function seekAudio(e) { if (!audioPlayer.duration) return; const rect = e.currentTarget.getBoundingClientRect(); audioPlayer.currentTime = ((e.clientX - rect.left) / rect.width) * audioPlayer.duration; }
function fmtTime(s) { if (isNaN(s)) return '0:00'; return Math.floor(s / 60) + ':' + (Math.floor(s % 60) + '').padStart(2, '0'); }

audioPlayer.addEventListener('timeupdate', () => {
  if (!audioPlayer.duration) return;
  const pct = (audioPlayer.currentTime / audioPlayer.duration) * 100;
  const f = document.getElementById('apFill'); if (f) f.style.width = pct + '%';
  const c = document.getElementById('apCurrent'); if (c) c.textContent = fmtTime(audioPlayer.currentTime);
  const o = document.getElementById('apTotal'); if (o) o.textContent = fmtTime(audioPlayer.duration);
});
audioPlayer.addEventListener('ended', () => {
  audioIsPlaying = false;
  const pb = document.getElementById('apPlay'); if (pb) pb.textContent = '▶';
  document.getElementById('audioBadge').classList.remove('playing');
  // 自动连播下一段
  if (audioCurrentSegIdx < AUDIO_SEGMENTS.length - 1) {
    nextSeg();
    audioPlayer.play().catch(() => {});
    audioIsPlaying = true;
    if (pb) pb.textContent = '⏸';
    document.getElementById('audioBadge').classList.add('playing');
  }
});

function buildSegmentBtns() {
  if (audioPanelBuilt) return;
  const wrap = document.getElementById('segBtns');
  AUDIO_SEGMENTS.forEach((s, i) => {
    const b = document.createElement('button');
    b.className = 'seg-btn' + (i === 0 ? ' active' : '');
    b.textContent = (i + 1) + '. ' + s.label;
    b.onclick = () => { loadAudioSegment(i); if (audioIsPlaying) audioPlayer.play().catch(() => {}); };
    wrap.appendChild(b);
  });
  audioPanelBuilt = true;
}

function toggleAudioPlayer() {
  const panel = document.getElementById('audioPanel');
  panel.classList.toggle('open');
  if (panel.classList.contains('open')) {
    buildSegmentBtns();
    if (!audioPlayer.src) loadAudioSegment(0);
  }
}
</script>

<!-- AUDIO NARRATION UI (L3) -->
<button class="audio-badge" id="audioBadge" onclick="toggleAudioPlayer()" title="点我听讲解">🎧</button>
<div class="audio-panel" id="audioPanel">
  <div class="ap-header">
    <span class="ap-title">🎧 AI 讲解</span>
    <button class="ap-close" onclick="toggleAudioPlayer()">✕</button>
  </div>
  <div class="ap-seg-title" id="apSegTitle">正在播放：课程导入</div>
  <div class="ap-progress" id="apProgress" onclick="seekAudio(event)">
    <div class="ap-progress-fill" id="apFill"></div>
  </div>
  <div class="ap-time"><span id="apCurrent">0:00</span><span id="apTotal">0:00</span></div>
  <div class="ap-controls">
    <button class="ap-btn" onclick="prevSeg()" title="上一段">⏮</button>
    <button class="ap-btn ap-play" id="apPlay" onclick="togglePlayPause()" title="播放/暂停">▶</button>
    <button class="ap-btn" onclick="nextSeg()" title="下一段">⏭</button>
  </div>
  <div class="ap-subtitle" id="apSubtitle">点击播放，听老师讲给你听～</div>
  <div class="seg-btns" id="segBtns"></div>
</div>

</body>
</html>
